# Approved overnight focus: September 6–7, 2026

Window: September 6 at 22:00 through September 7 at 08:00 America/New_York.
The user accepted this plan in the implementation task. These are targets, not
promises of completion, changes to signed requirements or Phase 1 acceptance.
Baseline: `fe03537f21e9a1cc129d93dae2298f1d2dfd3751`, clean and pushed.

## Priority and acceptance

1. Connect remaining governed evidence-selection and authorization components into
   one tested, closed-by-default saving path. Explicitly identify indispensable
   missing approvals; never manufacture a successful proof or clear a HOLD.
2. Connect exact-content confirmation and save-status feedback to the authoring
   screen, tested with disposable local data. Preserve the pink/orange design and
   exact revision/content binding. Live saving remains disabled.
3. Reserve time for regression/browser verification, fixes, current documentation
   and verified pushes to `codex/phase-1-foundation`.
4. Revision-linked work-list/board integration is stretch work, not a morning promise.

Focus on demonstrable authoring-to-save progress rather than increment count.
Reuse existing packages. Do not start unrelated assurance micro-increments or
broad repeated Critic loops. If a live approval blocks one boundary, record it and
continue independent authorized local/UI work. Never reinterpret a blocker as
permission to enable the blocked action.

## Morning handoff

The existing implementation heartbeat remains active; the app permits only one
heartbeat on this task. At the first safe checkpoint at or after 08:00, report
verified completed functionality, GitHub commits, tests, actual demo availability
and exact user-only blockers. Do not interrupt an active validation run or describe
pending tests as passed. Record the actual time and results in
`docs/overnight/2026-09-07-HANDOFF.md`; avoid repeating that report on later runs.
An unavailable computer/app or usage limit may delay the checkpoint.

## Unchanged boundaries

Implementation, isolated tests, documentation and candidate pushes are authorized.
Human signatures, protected incorporation, independent gate approval, new provider
permissions, destructive real-data actions, live writes, deployment, release and
spending are not. All five R5 findings and signed Phase 1 obligations remain open.
Synthetic provider/identity evidence is not approved real configuration. The
existing read-only runtime App credential handoff is resolved; do not repeat it.

After the morning report, continue the standing authorized plan unless the user
changes direction. This overnight focus does not waive remaining work.
