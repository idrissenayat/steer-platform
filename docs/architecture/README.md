# STEER platform architecture

The canonical production architecture for item 0001 is
`../../intent/0001/ARCHITECTURE.md`. The diagram in this directory is its visual
projection.

Status: **Gate 1 draft**. It defines the production target; it does not claim
that the current Vite/React fixture prototype implements the Phase 1 stack.

The original supplied files remain external source inputs. This directory holds
the corrected project version so Git records subsequent architecture decisions.
