# Plan

1. Bind exact original context and both role profiles to the operation input digest.
2. Preserve it under current encrypted draft rules and actual operation/source reads.
3. Verify immutable recovery, lost acknowledgement, later edits, access loss and
   restored-config composition with historical results in disposable integration.
4. Check migrations without changing the real baseline, document and push.

Next: exact rendered role-request capture and observed provider provenance, durable
reference-only development activities, then editor/API acknowledgement and recovery.

The next renderer must project the Test Agent's allowed context explicitly. The
stored editable snapshot may contain an earlier Exam; preserving it does not permit
sending that Exam, Architect private reasoning, or prior conversation context to
the fresh Test Agent. Bind its actual Brief/Spec predecessor and rendered request
digest to its step before dispatch; never hash an arbitrary fixture placeholder.
