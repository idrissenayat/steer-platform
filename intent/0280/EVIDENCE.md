# Intent 0280 execution evidence

Status: verified synthetic integration and regression; live acceptance remains open.
Base: `b45f0666fd848178a91adac0646e062e307bc389` (0279).

The shared authenticated workflow now accepts an explicit candidate-revision test
direction. Its actual registry/identity/factory services, 34-source corpus, fixed
Temporal sequencing and encrypted records are unchanged. Only test selection and
assertions are extended; production source is not changed.

The new target helper rejects missing/ambiguous/noncandidate targets and preserves
the exact versioned Brief path, commit and digest. Both SDK roles are checked for
that declared choice. The save helper verifies prior pointer lineage, allowed CAS
paths, prior bundle/unrelated-file preservation and authenticated old/new exact
reads. A focused `--journey-runtime-revision` selector is distinct from the full
suite and the full runner also includes the new case.

Four focused selector/diagnostic tests pass. Typechecking identified and corrected
a test-only response field access (`reference.manifestDigest`, not a top-level
digest). No production contract or assertion was weakened.

The `--journey-runtime-revision` selection passes its one joined authenticated
candidate-revision check and idempotent migration check. The actual identity and
21-service factory process 34 native sources, two recorded scope reviews, both
separate drafting roles, the human Brief correction and stale-assessment rejection,
exact confirmation, fixed Temporal save and exact old/new reopen. Six synthetic
model calls/reservations and one native Git commit remain unchanged across lost
acknowledgements, replay and runtime reconstruction. Current policy/source/Git-grant
denial and wrong-target digest checks pass without another original or save.
The test removes only its own disposable PostgreSQL container and tmpfs data.
This focused result is not the full SQL suite, live GitHub or signed-in UI acceptance.

Observed synthetic revision preview: 30,332 requests / 18,653 ms. First confirmation
with discarded acknowledgement: 61,096 requests / 37,892 ms. Reconstructed and
repeated confirmation: 61,034 / 41,151 ms and 61,032 / 40,262 ms respectively.
These measurements do not establish an improvement or acceptable live performance.

Prototype/all eight package typechecks and the optimized Next.js 16.3.4 build pass.
The default `--journey-runtime` selection also passes all three joined checks plus
idempotent migrations after the shared test-helper changes. New-distinct behavior
retains six synthetic model reservations, one native commit and exact reopen,
with unchanged 0279 request totals. Both SQL selections clean up only their own
synthetic containers/tmpfs. Neither selection is the full PostgreSQL suite.
The final broad regression passes **1,369/1,369**, with zero failures, cancellations
or skips (153,984 ms; concurrency four, run separately from the SQL selections).
The four focused selector/diagnostic tests are included in that count, not added
to it. The 95-artifact kit, workflow token-scope audit and whitespace checks pass.
All 268 local Markdown links in the seven changed/new documents resolve. The fixed
checklist's 25 unique rows independently recount to 14 verified and 11 pending,
matching its 56% summary.

The user's progress-reporting request is recorded in the
[fixed delivery checklist](../../docs/INTENT-CAPTURE-PROGRESS.md). Its baseline is
13/25 (52%) through 0279. Completed checkpoint C07 advances it to **14/25 (56%),
up four percentage points**, after final regression verification. The existing
active one-minute heartbeat prompt
now requires a percentage, checkpoint count, delta and verification level after
each completed increment. Reporting itself earns no points. No authority or
cadence changed; the OpenAI Docs skill informed the saved-task update.

## Boundaries and next checkpoint

No production source, dependency or migration changes. No real model call/spend,
credential inspection/change, records/D1 adoption, live runtime GitHub write/grant,
gate signature, authentication bypass, deployment, release or user-data deletion.
The protected Architecture, canonical Exam and accepted retention-policy SHA-256
values remain unchanged. User-owned `docs/REAL-USER-ROADMAP.md` and `outputs/` are
untouched and excluded from this increment.

Next complete C08's authenticated new-linked save/reopen, then remaining amendment
and continuation paths and bounded provider-load work. Real records/clock/late
recovery, governed runtime authority, model quality and signed-in I1–I6 acceptance
remain incomplete; the active one-minute loop is not a grant to activate them.
