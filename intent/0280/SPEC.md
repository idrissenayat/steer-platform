# Intent 0280 specification

1. Reuse the full signed-identity/actual 21-service factory with native Git, encrypted
   SQL, both fixed scope workflows, ordered Architect/Test Agent workflow and fixed
   save. Keep 34 sources, the corrected draft, six model reservations, lost replies,
   restarts and all current authority checks. No injected principal/service result.
2. Select exactly one existing versioned candidate Brief from reviewed evidence.
   Bind path, current reviewed commit and content digest. Never infer a candidate
   from a root mirror, choose an ambiguous target or silently fall back to creation.
   Both recorded drafting roles must receive the exact human choice.
3. After the human Brief correction, reject the stale scope assessment, reassess
   the exact latest content and deliberately select the current versioned target.
   Preview must resolve candidate-revision with the actual prior manifest, no
   amendment and no new relationship. Target digest mismatch and source denial
   must block without creating originals, operations, model calls or Git effects.
4. Exact confirmation and discarded/repeated acknowledgements preserve one encrypted
   original. Denied start has no effect. Lost scheduler and native Git replies,
   workflow replay, factory restart and reconciliation must produce only one
   native commit and no additional original or model reservation.
5. Only CANDIDATE.json and the pre-pull Brief mirror are compare-and-swap paths.
   Keep all previous bundle files and other pre-existing blobs unchanged; never
   create or overwrite a root canonical Spec/Exam. Verify the old bundle remains
   readable through authenticated exact read after the new commit, then reopen the
   new bundle exactly after a later branch-head edit. Current read revocation denies.
6. Add an explicit focused runner and include the new case in the full SQL runner.
   Preserve the default new-distinct case and rerun it after shared-helper changes.
   Run focused selector tests, typechecks, build and broad regressions. Report only
   the suites actually run; fixtures do not establish model quality or real UI UX.
7. No production authority, credentials, model spend, real data migration, runtime
   GitHub writes, D1 adoption, gate, deployment, release, deletion or protected/user
   file changes. Linked/new-amendment/continuation saves and live I1–I6 remain open.
