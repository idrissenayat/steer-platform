# Brief

A scope-review worker interrupted after a model response must recover the exact
acknowledged response, not pay for another call or invent a successful outcome.
Persist request and response separately, privately and immutably against the
admitted original, batch, source revision and fenced dispatch. Verify the actual
pinned SDK wire format and citations before acknowledging or releasing a record.

Keep current authority, records, model-budget and runtime-save holds unchanged.
