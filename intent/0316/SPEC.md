# 0316 — Scope-start current-original phase

1. Add a trusted `original` scope records selection. It reads only the exact draft,
   latest revision, scope original, immutable run manifest, budget and terms.
   Never retrieve scope observations/batches/reservations or unrelated records.
   Current execution expiry denies; history/current review selections stay intact.
2. Reuse the canonical metadata, ciphertext, key and content verifier. Current
   source/profile/draft/original/review policies remain independently required.
   Revalidate keys and complete records after dependent work, with current source
   authority through final closure. No permission, result or principal cache.
3. Supply the phase only from trusted runtime composition using its explicit
   current owned-read binding. No request option, environment fallback or expired
   historical proof may enable it. Absent binding keeps the established reader.
4. Initial binding and the first authorization share one read-only phase. Later
   scheduler revalidation and post-scheduling authorization each use a new phase.
   Sequential reads share only
   immutable validated original state inside that phase and recheck present
   authority. The scheduler runs after final closure and may request another fresh
   authorization phase; no phase spans scheduling, admission, puts or dispatch.
5. Preserve exact input/configuration/manifest/draft checks and false authority
   flags. Method replacement, malformed/escaped/parallel/forgotten work, late
   changes and closure deny; actual pending records/key callbacks retain ownership
   until drainage. Keep deadlines, public schemas and tenant boundaries unchanged.
6. Verify original-only SQL selection and exclusion, real native-record equivalence,
   late draft/key/source/records/expiry changes, malformed phases and scheduler
   separation. Run both synthetic authenticated save/reopen directions, targeted
   scope-start SQL checks, focused/broad regression and types. Count every provider
   attempt; no UI, real model quality or full C22 claim from these functional tests.
