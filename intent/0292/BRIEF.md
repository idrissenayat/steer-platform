# Intent 0292 — reduce nested records policy traversal

Continue the user's request to fix the intent-capture bottleneck and move forward.
0291 still measured 14,829 provider attempts for first confirmation and 11,537
for initial drafting start. This increment targets their deeper read-only records
composition, without substituting a preview or weakening saving/recovery checks.

Consolidate caller validation around permission-only queries in current/historical
scope and development readers, and the read-only part of development start.
Keep every independent policy, initial authentication, content/key/SDK checks,
full source readbacks, effect barriers, deadlines and pending-work admission.

Verify the actual authenticated constructor graph using synthetic authority,
native Git, disposable encrypted PostgreSQL and Temporal. Record raw before/after
request counts and retain failures. Partial C22 work earns no completion points:
baseline remains **68% (17/25; 8 remaining)**. No live model use, records adoption,
runtime provider writes/grants, spending, gates, deployment or release is approved.
