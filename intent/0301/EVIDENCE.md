# Combined revision graph: verified experiment, not installed

The original comparison and source hashes below describe `c9a86a2`. The
[final-source-closure follow-up](FINAL-SOURCE-CLOSURE.json) is the newer measurement:
**52 attempts**, including two additional identity checks. It is also test-only.

Both focused native selectors pass the unchanged authenticated generation,
correction, confirmation, native save and exact reopen journey, followed by the
records/source experiment and its negative cases. [Measurements](FEASIBILITY.json)
retain both the separate-reader baseline and graph correction. Application
confirmation is still **7,637 attempts**; this package changes test code only.

## Comparable combined-stage measurements

| Measurement | Separate readers | Revision graph |
| --- | ---: | ---: |
| Total simulated provider attempts, including OIDC/Git bootstrap | 74 | 50 |
| Historical repository attempts | 20 | 12 |
| Identity-provider attempts / JWKS | 53 / 1 | 37 / 1 |
| Fresh caller validations | 48 | 32 |
| Retained revisions / original contexts | 2 / 3 | 2 / 3 |
| Semantic sources / physical files per revision | 34 / 42 | 34 / 42 |
| Unique blob objects, all shared between revisions | 40 | 40 |
| Revision/path policy checks | 252 | 252 |
| Encrypted records / scope SDK / development SDK exchanges | 20 / 4 / 2 | 20 / 4 / 2 |
| Role transactions / SQL statements | 4 / 34 | 4 / 34 |
| Distinct keys / key-provider calls | 1 / 2 | 1 / 2 |
| Fixture records / retained-original source-policy checks | 82 / 136 | 82 / 136 |
| Single local sample, no injected provider delay | 3,116 ms | 1,621 ms |

The 74-to-50 reduction is 32.43% for this portion, not for the application. Each
run has a fresh disposable fixture with the same retained topology; IDs differ.
`providerKinds` describes identity traffic only: add JWKS and historical repository
attempts for the total. Both measured encrypted snapshots contain 607,624 bytes.
The preceding ordinary journey retains six synthetic model calls/reservations and
one native Git mutation; the combined experiment makes no provider mutations.

Separate revision collectors add 52 source attempts against the proposed 30.
The graph adds 28 (12 repository + 16 identity), within that proposed portion for
this fixture. Root/pointer, manifest and document waves cover 68 / 4 / 12 path
references, fetching 34 / 2 / 4 objects in 3 / 1 / 1 bounded batches. Git objects
are shared only within the invocation; tree membership, root selections and each
revision/path grant stay independent. A changed same-path blob remains distinct.

This is the bounded multi-revision correction anticipated by
[0298](../0298/REQUEST-BUDGET-PLAN.md), not permission caching or a stack switch.
It does not accept the whole preview: final cross-component source closure after
later records/key work, production per-purpose policies, ownership/drainage, outer
callbacks and all write-separated phases are still unproven. The old 204-attempt
confirmation control outside previews also remains to correct.

## Verification

- Separate and graph native selectors: idempotent migrations, unchanged joined
  journey and combined assertions pass. Late records-policy/key loss, foreign
  subject, a new revision and a new hold all reject. Intended mutation triggers
  are asserted, not inferred from any rejection. Owned fixtures are cleaned up.
- Corpus, historical-revision, actual OIDC/Git-grant and diagnostic checks: 17
  tests pass in 52,691.38275 ms. This run preceded the new graph implementation.
- Final graph and diagnostic checks: 10 pass in 33,190.231959 ms, no failures,
  cancellations or skips. This includes cross-revision denial before blob dispatch,
  changed source/caller/grants/head/method/binding/cancellation and invalid replies.
  The five diagnostics checks overlap the earlier run: **22 unique focused tests**.
- Final prototype and all eight package typechecks pass (seven cached, package
  duration 1.011 s). A test-only type mismatch was caught and corrected: the new
  revision-aware denial policy now declares the full authority contract rather
  than inheriting the older fixture's path-only parameter. Graph tests were rerun.
- Selector usage text now lists both combined modes; extra/mixed selectors reject.
  Kit validation (95 required artifacts), workflow scope audit and diff checks pass.
  Final audit verifies 16 recorded source hashes, four protected hashes, 439 relative
  links across the controlling/selected evidence documents and the unchanged 17/25
  checklist. Provider-count arithmetic includes all three measured traffic sources.
- The baseline was observed before graph implementation; the graph native run
  preceded only test-type and usage-text fixes. Source hashes identify final code,
  not a claim that the baseline ran after all later files were added.

No production source, signed artifact, credential, real user draft, runtime profile,
records adoption, live model call, runtime GitHub write, deployment or release changes.
The user's roadmap and outputs remain untouched. Full SQL/broad build/C22, browser,
real-model quality and actual repository acceptance are not run or claimed.

**Intent capture: 68% (17/25; eight remaining; +0 percentage points).** Next finish
the cross-component policy/ownership/effect boundaries and integrate the coherent
correction, then run the unchanged complete performance benchmark. A 50-request
test portion is not a completed checkpoint or a new delivery-time estimate.

## Follow-up: source revalidation after dependent records

The graph now awaits the records/key final readback inside its read-only phase,
then rechecks all selections, path grants, current global permissions and head
before returning. A native negative case revokes source access after the full
records readback; the final path policy must observe that exact revocation and
reject. Sharing immutable bytes never shares the authority to return them.

The final combined stage uses **52 attempts**: 39 identity-provider requests,
one JWKS request and 12 historical repository requests. Fresh caller validations
increase from 32 to 34; the new two checks bracket dependent work. All 252 path
grants, 82 records policies, 136 retained-source policies, four SQL transactions /
34 statements, two key calls, 20 encrypted rows and six SDK exchanges remain.
The single local sample is 1,629 ms and the encrypted snapshot is 607,626 bytes;
neither is a full-action or delayed-performance measurement.

The unchanged native journey, added post-record source denial and existing negative
checks pass, followed by owned cleanup. Eleven graph/diagnostic tests pass in
44,680.265916 ms. A TypeScript unknown-value spread in the new test was corrected
with an object assertion; the changed test passes again in 12,427.548 ms. Final
prototype/eight package types pass (seven cached, 1.262 s), as do kit/workflow checks.
The earlier 50-attempt measurement and its source hashes remain historical and
unchanged; the follow-up JSON records four current changed-source hashes.

This closes one final-source ordering gap in the experiment, not complete
distributed lifecycle/authority atomicity or operational per-purpose policy,
key-provider, owner-drain, HTTP/effect-phase integration. Lifecycle expiry through
final closure must still be covered by the integrated owner's deadline. No source
service is installed. Full confirmation remains 7,637; **68% (17/25; +0 points)**.

Follow-up final audit: 16 baseline source hashes verified at `c9a86a2`, four changed
source hashes verified in the working tree, four protected hashes, 446 relative
links, the unchanged 25-row / 17-verified tracker and counted provider arithmetic
pass. Diff whitespace checks pass; no user roadmap or outputs are staged.
