# Plan

1. Share the pinned disposable Temporal bootstrap with the existing workflow suite.
2. Connect the browser-created receipt fixture to the real durable worker/runtime.
3. Verify queued reconstruction, exact current readback, replay, duplicate rejection
   and one projected event before the browser opens its exact source.
4. Run browser, workflow and full regressions; document limits and verify candidate push.
