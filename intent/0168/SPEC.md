# Spec

The actual browser must author, explicitly review and submit one immutable operation.
The native Git fixture creates its Brief and operation marker, loses the response,
and current authenticated status recovers the exact original reference. The worker
target binds that reference and trusted subject/path/branch outside workflow history.
Its receipt callback must make an actual same-origin status request using the current
browser session; no cookie/token or callback is serialized into Temporal history.

Queue the operation without a running worker, reconstruct its runtime, then project
through the actual Temporal activity and owned PostgreSQL projector pool. Verify one
current receipt read, one ingestion event, original workflow run identity, history
replay without repeated effects and retained duplicate-start denial. Read the exact
created revision/digest through the production curated Brief view after service
reconstruction. Creation alone is not projection or source curation.

Reuse one pinned/hash-verified owned Temporal bootstrap for browser and workflow
integrations. Clean only owned servers, workers, generated files and synthetic data.
Keep the API-job fixture for existing nondurable tests; do not silently claim it is
the worker. This integrated test is not live dispatch or a real-provider/gate proof.
All five R5 findings and qualified/independent review and signatures stay open.
