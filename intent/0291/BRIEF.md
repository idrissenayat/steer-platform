# Intent 0291 — flatten read-only policy traversal

The user asked to fix the intent-capture performance bottleneck and move forward,
not keep accumulating isolated hardening checks. Source review at 2,634 provider
attempts and first confirmation at 45,716 are far beyond the fixed C22 budget.

Consolidate repeated caller checks around metadata-only authorization queries,
while preserving fresh caller checks before content IO/effects and before results
escape. Reuse verified immutable commit/tree membership, never permissions or
source bodies. Exercise the actual authenticated constructor graph with synthetic
authority, native Git, disposable encrypted PostgreSQL and Temporal workflows.

Success for this increment is a measured reduction with current-authority,
integrity, uncertain-save and exact-reopen regressions passing. C22 is complete
only when its full existing benchmark protocol passes. No live model spending,
records adoption, runtime GitHub grants/writes, deployment, release or signatures
are authorized. Preserve signed sources, user changes and the pink/orange UI.

Progress baseline: 68% (17/25; 8 remaining). Partial work earns zero points.
