# Plan

1. Add portable decision contracts and bounded curated query with regression tests.
2. Add an in-dialog reader with exact-reference validation and source disclosure.
3. Exercise actual isolated source, storage, authentication and browser rendering,
   including denial, mismatch, clearing and accessible responsive layout.
4. Run repository checks, document observed evidence/limits and verify candidate push.

Protected intent/0001 and GitHub policy files remain unchanged. No live configuration,
signatures, writes, release, deployment, real-record deletion or spending.
