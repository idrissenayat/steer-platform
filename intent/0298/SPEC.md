# Diagnostic specification

1. Exercise the genuine authenticated new-distinct 34-source composition using
   disposable PostgreSQL, native Git, Temporal and synthetic identities/models.
   Keep the source, clarification/correction, independent roles, exact consent,
   save/recovery and old-commit reopen assertions.
2. Profile every HTTP tool invocation, including denials and repeated/recovered
   calls. Conserve identity attempts against the separate transport meter. Keep
   repository attempts in the existing action measurements; do not confuse the
   identity-only trace with total load.
3. Ordinary stacks identify the provider/identity/registry path but lose deep
   asynchronous origins. An explicitly selected test preload may label only the
   12 allowlisted revalidation scheduling sites in memory. Preserve callbacks,
   promises, errors, guards and source line counts. Never install this in ordinary
   startup or production. This is diagnostic instrumentation, not a runtime fix.
4. Keep at most 512 trace groups per invocation, preserving overflow counts.
   Retain only allowlisted production-relative module locations, not raw stacks,
   function names, arguments, absolute paths, source text, tokens or identities.
   Keep asynchronous labels test-only, bounded and concurrency-isolated.
5. Preserve all samples and the unchanged performance protocol. Attribution
   overhead cannot be mixed with p95/delay acceptance. A passing synthetic
   workflow is not live saving, model quality or UI acceptance.
6. Explain the multiplying path, show the mathematical lower bound of the current
   protocol, and define explicit component budgets and stop conditions for the
   correction. Proposed budgets are not measured results or proof of feasibility.
7. No production code, signed architecture/Exam, retention policy, credentials,
   live data, model spending, runtime GitHub grants/writes, gates or deployment
   changes. Overall remains 17/25 (68%).
