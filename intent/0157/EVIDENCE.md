# Development evidence

The frontend check passed 47 tests and its build. The new reference test verifies
exact recorded tuple mapping, omission of identity/operation/authority fields,
noncommitted outcomes and malformed/gate-claiming receipts. Existing strict reader,
fragment, lifecycle and prototype-import boundaries remained green. The native
link uses STEER's existing pink/orange styling and no new component dependency.

The final `pnpm test:auth:browser` run passed all 41 checks with Chromium
151.0.7922.34 and Keycloak 26.7.3. Native seeded Git receipts feed the actual shared
status reader; the production GitHub reader ingests the recorded source into an
explicitly curated disposable PostgreSQL projection. The actual receipt link
opened byte-for-byte matching content at the recorded revision and fingerprint.
Keyboard focus moved to the modal close control and returned to the receipt link.
Current read-grant denial blocked content, restoration worked, and a later selected
projection made the old receipt unavailable without substituting the newer revision.
Current draft facts/digest stayed unchanged. This path uses no browser-response
interception. Existing automated accessibility, responsive, session and shutdown
checks also passed. No additional foreground preview was opened during the background
run, and no separate visual/qualified accessibility review is claimed for this link.

The first browser run passed the new navigation but exposed an older feed test's
fixed four-event assumption. The two new fixture ingests legitimately add two events.
The corrected check explicitly verifies their record keys/revisions/digests and a
six-event cursor; it does not erase history or relax production controls. Only the
fixture's exclusive projection row and ingestion events are removed between test
sections. Its derived feed history remains until the owned database is cleaned up.
The final complete browser rerun passed with that correction. A nullable test href
was also narrowed explicitly after type checking; no type rule was weakened.

Full `pnpm check` passed on Node 24.20.0/pnpm 11.19.0: 95 kit artifacts, workflow
scope audit, seven package typechecks, 88 prototype tests, 437 root controls,
all workspace tests including 270 adapter, 87 API and 47 web tests, and all
package/prototype builds. Eligible Turbo steps were cached. API type checking also
passed after the final browser-fixture correction. Whitespace checks passed and
protected `intent/0001`/`.github` files stayed unchanged.

Both browser runs cleaned up only their owned test Chromium/HTTPS services,
PostgreSQL/Keycloak containers, temporary data and generated credentials. The source
operation remains seeded fixture history, not an authorized platform save. No real
credentials, provider write, live writer profile, human gate signature, deployment,
release or spending changed. All five R5 findings and signed Phase 1 obligations
remain open. The standing overnight loop remains active.
