# Intent

Give the verified identity foundation one explicit startup owner. Assemble the
real GitHub adapter, bounded auth database pool, encrypted session store and
service lifecycle without promoting test fixtures or spreading storage imports
through the API. Keep supplied secrets separate from the versionable profile.
