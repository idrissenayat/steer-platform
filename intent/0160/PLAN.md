# Plan

1. Compose the existing job with explicit profile/secrets and its own projector pool.
2. Test lazy construction, strict configuration, denied identity, sanitized failures,
   overlap rejection and actual drain-before-close behavior without network access.
3. Replace the disposable handwritten sink with this runtime and verify actual
   records, receipt access denial, duplicate/no-rewind semantics and closed pool.
4. Run focused/type/browser/full checks, update documents and verify candidate push.

After this connection, the held readback-to-projection path is integrated in
disposable runtime, not a real authorized create/save journey. The next useful
overnight stretch is a revision-linked authenticated work-list using current curated
catalog/snapshot contracts. Label it as projected artifacts, never lifecycle stages,
approval status or a verified Flight Board. Preserve the pink/orange design and
exact-reference reader. Do not add further unrelated evidence wrappers.

Live write authority/configuration, model conversation, lifecycle/decision data,
all five R5 findings, qualified/independent protected review and human gates remain
outstanding. No deployment, release, provider permission or spending is authorized.
