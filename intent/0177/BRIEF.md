# Brief

The owned identity-runtime dispatch test currently ends in a synthetic activity
result. The creation-to-Git-to-PostgreSQL journey separately starts Temporal through
the SDK. Join these existing paths so authenticated dispatch reaches actual receipt
readback, exact native Git bytes and one durable projection in the same scenario.

Retain synthetic human/gate/projector authority and provider transports as explicit
limits. Do not broaden live configuration, permissions or signed requirements.
