# Specification

## Authority and traceability

GAP-INVENTORY.json has status planning-traceability-not-acceptance and baseline
57980ea3a3aecc35e024e5ae2516cfa457dd2667. Seven exact SHA-256 pins preserve the
canonical Exam, accepted retention-policy bytes, frozen R5 Critic, normative
dimension inventory, lifecycle table, historical finding resolution and amendment
candidate. Source-document requests are requirements to analyze, not authorization
for an action. Historical status/closure text is preserved, not reclassified as a
current independent verdict or a new qualified-human ruling.

All five R5 IDs, severities, exact requiredFix text and affected source IDs match
the frozen Critic. All fourteen historical finding IDs remain in scope. Fourteen
named evidence references identify actual exported functions and literal executable
test names. Their level is synthetic-development-regression; pointer validity does
not establish the completeness or independence of the referenced tests.

## Coverage distinctions

All sixteen lifecycle classes have a source-duration and selection-test mapping.
The 0068 test proves selection/outcome at its original valid-key instant, not every
class's future completed disposition. The current-registry runtime independently
admits four classes under v1–v4 and referenced evidence alone under v5. Indefinite
authoritative artifacts require no destructive completion. The remaining ten
classes are not admitted to these newer profiles; that does not mean every
same-era disposition for them is unimplemented. Assess each required trust era.

| Coverage category | Classes |
|---|---|
| Immutable; no destructive future completion | RC-AUTHORITATIVE-ARTIFACT |
| Current-registry v1–v4 | RC-SECURITY-AUDIT, RC-CORPUS-BASELINE, RC-DECISION-PROOF, RC-LEGAL-SIGNED-LOG |
| Reference-specific current-registry v5 | RC-REFERENCED-EVIDENCE |
| Not admitted to those current-registry profiles | RC-RELEASE-MIGRATION, RC-FAILED-RUN, RC-POSTHOG-RAW, RC-REBUILDABLE, RC-DELETION-EVIDENCE, RC-CORPUS-RAW-WORKING, RC-CORPUS-PROVENANCE, RC-CORPUS-SANITIZED, RC-CORPUS-DERIVED-TEXT, RC-CORPUS-EXPORT |

Full boundary matrices remain not-reconciled. The signed policy's provenance rule
overrides the frozen table's historical closure surrogate: use the later corpus
retirement or maximum verified derived-record deletion over the closed inventory,
plus seven calendar years. Item closure is not a substitute.

Migration dimensions exactly match the frozen source. The Cartesian axes are
2 versions × 6 phases/checkpoints × 5 interleavings × 3 interruptions × 4 rollback
positions × 5 idempotency/concurrency states = 3,600 coordinates. Eight failure
cases and six transplant cases add fourteen. Existing corrected tests do not yet
provide a reconciled execution-ID ledger for that exact set. Therefore
correctedExecutedIdCount is null, not a guessed count or percentage. Legacy
declared/executed evidence is not corrected-matrix acceptance.

Recovery maps all eight cuts and twenty-five corruption cases to the existing
0065 regressions. Those are separate declared dimensions, not an invented required
8×25 cross-product. The first two cuts correctly return UNKNOWN_RECONCILE_PROVIDER
with complete supplied evidence; the remaining six may return RECOVERY_VERIFIED.
The four-row synthetic bound and absence of live recovery evidence stay explicit.

## Finite work packages and validation

Six open work packages contain scope, completion criteria, dependencies and action
boundaries. Dependencies must refer only to earlier packages, preventing cycles.
The next package is GAP-01, the corrected declared-versus-executed ledger. This
inventory does not exhaustively certify all canonical Exam clauses; that mapping
is a remaining deliverable, not a claim made by generating this file.

The regression suite verifies source hashes and exact sets, candidate exports,
test names, actual runtime class admission, matrix arithmetic, recovery outcomes,
dependency order and explicit non-approval claims. No production behavior, real
provider state, gate approval or execution authority changes.
