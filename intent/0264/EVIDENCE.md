# Development evidence — 2026-09-09

Base: `8fd00f49badde5b5fec17aa92c9ec26b5b50a322`.

## Implemented

Read-only preparation diagnostics across explicitly configured scope-review and
development admissions, plus an explicit diagnostic action in the actual Next
run-history panel. Missing original metadata is visible without being mistaken
for no prior attempt. Present metadata never claims verified content or success.
All current-identity, source, lifecycle, role and pagination boundaries remain.

## Verification

The explicit `--admission-discovery` disposable PostgreSQL run passed **six focused
checks plus idempotent migration verification**. Coverage includes genuine encrypted
scope/development original capture, missing originals, restarted readers, pagination,
wrong/changed bindings, privileged roles, malformed records, late capture/edit/hold,
revoked authority, execution expiry and retention expiry. No model call or reservation
was made by the reader. Its SQL trace contains no ciphertext, step/result or mutation
query. Tests corrected an invalid fixture import and a rotated synthetic configuration
whose budget revision had not been rotated; the passing run follows both corrections.

Actual React-graph tests cover missing/present warnings, explicit pages, inaccessible
versus empty results, old selection clearing, source/identity/visibility loss and
expiry. Automated axe checks pass for the diagnostic panel with color-contrast
analysis disabled; this is not a manual accessibility or visual acceptance audit.

The final regression run passed **1,257/1,257 tests**. The prototype type check,
all eight package type checks, optimized Next 16.3.4 build, 95-artifact kit validation
and workflow token-scope audit pass. Protected Architecture, Exam and accepted
retention-policy hashes match the prior checkpoint; `git diff --check` passes.
This increment does not claim a new full SQL-suite run; the last full 359-check
baseline belongs to [0262](../0262/EVIDENCE.md).

The separate `--run-discovery` PostgreSQL run passed **six existing recovery checks
plus idempotent migration verification**, including composed 34-source scope and
both recorded SDK roles, historical document comparison, package preview and lost
confirmation recovery. Its model responses and authority are synthetic; the
focused result is not a full SQL-suite or live-provider claim. Both disposable
runners cleaned up their own containers/tmpfs data.

All **270 local links across ten changed documents** resolve. User-owned roadmap
and outputs were not modified or staged.

## Boundary

The API/data factory is uninstalled. All new tests use synthetic records, identity,
grants and HTTP; no live signed-in UI/provider acceptance occurred. The disposable
runner removes only its synthetic container and tmpfs data. No existing user records
are changed, deleted or migrated. No paid model calls, real GitHub runtime writes,
deployment, gate signature or credential changes occurred.

The API-key skill preserves the resolved credential decision and keeps this work
synthetic. The installed Next client/server guide keeps databases and authority
out of the browser. Protected signed files and user-owned roadmap/outputs remain
outside this change. Trusted destination/lifecycle composition and real I1–I6
acceptance are still open; diagnostics cannot confer their missing authority.
