# Plan

1. Add a separately pinned original-only immediate readiness entry point.
2. Share strict head checks while permitting absent triggers only in this mode.
3. Add source-clock, earliest-trigger, signed hostile-head, format-isolation and
   full disposition/replay controls; preserve all prior observation seals.
4. Save fresh reports, run focused/repository/separate full integration checks,
   update project docs, commit/push and verify the candidate remote.

## Next

Sixteen lifecycle coordinates remain across release/migration records, deletion
evidence, corpus provenance and sanitized corpus. They need explicit current-era
admission plus exact environment-retirement, final-tombstone, complete derived
inventory/last deletion and earlier-trigger/key/residual-copy erasure semantics.
Do not expand a shared class allowlist without those actual proofs and paired
negative tests. Keep existing trust eras and their key windows intact.

The remaining 3,614 migration and one obsolete schema case, all normative variations,
isolated real runtime/store tests, independent/protected review and GAP-01–06/M1–M8
remain separate. This immediate surface is not wired to a live projection or
mutation route. No provider operation, deletion, signature, release, deployment
or spending is authorized. All five formal R5 findings remain open.
