# Development acceptance

Not a protected Exam or an independent review.

- Exact UTF-8 Brief and operation marker are the only additions at the expected head.
- Fresh adapter instances recover the same native Git commit without a new mutation.
- Competing requests cannot append two commits from the same proposed head.
- Stale bases, existing paths, changed-key requests and foreign scope cannot overwrite.
- Mutation errors, lost acknowledgements and invalid readback remain uncertain.
- Token permissions, repository identity, authority pins and freshness fail closed.
- Deleted/recreated markers, bad blobs, partial ancestry, merges, unexpected files,
  moving-head absence and over-budget responses never become verified results.
- Default runtime remains uninstalled, real App read-only, protected files unchanged.
- Evidence separates native Git/fake-provider development checks from real GitHub
  enforcement, full identity/Gate 2 authority, usable UI and production readiness.
