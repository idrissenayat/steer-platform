# Development evidence

Baseline 7f0a5d95c10a366866782d688238063985403399 plus this increment.
Twenty-two focused groups pass: eight release-profile, eight immediate-profile and
six prior long-retention groups. All 389 prior mapped observation seals, counts and
outcomes remain unchanged. Fresh quick execution records 393 passed / 3,643
uncovered; full records 409 passed / 3,627 uncovered, both zero failures.
The separate full integration run passes all three groups (125.8 seconds),
re-executing the synthetic full matrix and matching its saved snapshot.
The initial repository run passed 402 of 403 root controls; one historical 0097
source-layout assertion assumed v5 was the last profile. Its regex now locates the
unchanged v1–v5 fallback independently of the explicit v6 branch, with the historical
inventory preserved. Current v6 admission is separately behavior-tested. The final
repository rerun passes all 403 root controls (137.1 seconds), prototype tests,
95 kit artifacts, security scope audit, typechecks and builds. The targeted five
inventory groups also pass. Unchanged package tasks use Turbo cache and are not
fresh package executions. Commands use isolated Node 24.20.0.

`git diff --check` passes; frozen intent/0001, .github and the lockfile are untouched.
Candidate publication is separately verified against the remote after commit/push;
it is not main incorporation, independent acceptance or Gate 2 approval.

Signed wrong source/actor/retirement/provider/time events pass the historical
event-only proof verifier but fail the new retirement-specific composition.
This distinguishes semantic enforcement from merely corrupt signatures. Complete
and read-only paths also reject missing shutdown conditions; full controls include
receipt omission and exact committed replay. Original key bindings compare equal
and public key material remains unique across historical and current domains.

SOURCE-MAP.json explicitly adds a named synthetic environment and retirement record
missing from the frozen fixture while preserving source times. No live retirement,
traffic/credential check, actual erasure, future observation, qualified hold review,
independent/protected acceptance, signature, release, deployment or spending is
claimed. Frozen sources are not edited.
