# Plan

1. Add a release-only current-v6 profile with a closed trusted retirement selector.
2. Enforce actual archived event/proof binding, release-rails source and both flags
   before waiting/pending or full disposition verification.
3. Exercise exact source clocks, signed semantic faults, original/current key
   separation, class/profile denial, full completion/replay and assertion propagation.
4. Preserve prior observation seals; save fresh reports; run focused/repository/
   separate integration checks; document, commit/push and verify the remote candidate.

## Next

Twelve lifecycle coordinates remain across deletion evidence, corpus provenance
and sanitized corpus. They require distinct final-tombstone semantics, complete
derived-record inventory/latest deletion, and earliest-trigger plus key/residual-copy
erasure with explicit future trust profiles. Do not treat the release-only profile
as admission for these classes or copy ordinary deletion as cryptographic erasure.

Next, exercise corpus provenance's existing compound-history rule in a narrowly
admitted future profile: exactly one retirement, a fresh complete derived-record
manifest, every named verified deletion, and expiry seven years after the later
retirement/final deletion. Missing manifest entries, mismatched corpus/version,
incomplete closure and incorrect later-event selection must deny. Preserve the
frozen table's surrogate-trigger discrepancy in a source map, not in success logic.
The final-tombstone and sanitized key/residual-copy cases remain separate follow-ons.

The existing 0068 candidate checks deletion-receipt digests in event schemas but
does not retrieve those receipts. Its specification explicitly limits completeness
to the trusted manifest assertion. Before claiming complete provenance disposition,
resolve actual receipt-byte availability/verification and their historical binding;
do not relabel event/digest matching alone as proof that derived objects were deleted.

The 3,614 migration and one obsolete schema case, all normative variations, isolated
real runtime/store tests, independent/protected review and GAP-01–06/M1–M8 remain
separate work. No provider operation, deletion, signature, release, deployment or
spending is authorized. All five formal R5 findings remain open.
