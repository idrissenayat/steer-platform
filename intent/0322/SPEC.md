# 0322 — Current assessment phase contract

1. Only the API factory's exact constructed current reader, pinned method and
   scope can supply its private projection to final save review. No public tool,
   environment flag, wrapped reader, history reader or error fallback enables it.
2. The projection encloses initial/repeated/final assessment consumption within
   the source-review phase, never an admission, write or scheduler effect. Its
   complete final native records/key comparison precedes the final draft read
   and enclosing source/corpus closure. All current purpose grants remain fresh.
   The helper runs metadata-purpose queries followed by the exact caller. Only
   that identical callback avoids duplicate invocation on a no-IO reuse; all
   independent callbacks retain both owner boundaries. Selected record/key
   purposes are re-granted through final native scope key/SQL work.
   Open the native lease only at the first assessment read, preserving lazy
   dependent preview authorization. Retain it through actual final drainage.
3. Reject replaced dependencies, malformed or repeated projection callbacks,
   changed source/draft/assessment/records/key/caller authority, and expired leases.
   Preserve independent caller execution and actual pending-work drainage.
4. Verify component contracts and native exact/wrapped-reader comparison, late
   denial and held-key ownership. Run both synthetic authenticated save/reopen
   directions, focused/broad tests, types and kit/scope audit on final source.
5. Record all provider attempts; do not substitute these functional samples for
   the unchanged C22 protocol, real model quality or signed-in UI/save acceptance.
