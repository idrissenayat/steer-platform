# Specification

1. Preserve all 15 exact required schema IDs in a source map with file-byte digest,
   schema ID, explicit version constant or null, selected executor and scope status.
   Three precision successors and eleven retained structural cases are mapped;
   MIGRATION-EVIDENCE stays unmapped because its original graph is not the corrected
   closed v2/v3 migration graph. Do not edit the frozen schema.
2. Reuse exact original positive fixtures and original negative instances where
   supplied. Every mapped schema must be an object with additionalProperties=false
   and reject an added unlisted field. Raw grants retain null-authority and wrong-
   record-class denials; lifecycle events retain missing-record-sha rejection.
3. Use compilePreciseSchema for the three 0070 successors. Exercise occurredAt for
   events and all six declared human time fields, including nested raw authority.
   Nine-digit fractional instants pass; invalid dates, short fractional precision
   and hour 24 fail. These mutations test shape/time only, not copied signatures.
4. Retained formats use the actual offline validator and original record samples.
   Record bundled source digests in every observation. Sample accessibility row
   iteration is explicitly closed and never credited as full matrix execution.
5. Preserve fractional detector-policy thresholds as JSON numbers in observation
   input. The signed-record helper is integer-only and must not be loosened or
   used to coerce schema values. Signature-bearing source fixtures remain unchanged.
6. Record actual validation results, not expected labels. Unknown/obsolete cases
   remain unmapped and assertions propagate. Seal hook source and preserve old reports.

Schema execution does not establish complete corrected schema adequacy, signatures,
later profiles, full graph semantics, manual accessibility or independent approval.
All five formal findings and GAP-01 remain open.
