# Brief

Reconcile original schema case IDs with the actual corrected precision schemas
and retained formats before counting execution. Preserve supplied source fidelity
and distinguish syntax/shape from signatures, authorization and graph semantics.

Use the already implemented 0070 precision compiler for human authority, lifecycle
events and embedded raw grants. Exercise the original positive/negative fixtures,
closed object shapes and every declared exact-time field. For retained formats,
exercise actual validators without claiming a new semantic correction. Keep the
obsolete migration schema explicitly unmapped for later corrected graph work.
