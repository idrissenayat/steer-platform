# 0317 — Drafting-start current-original phase

1. Add explicit development-original metadata discovery under a distinct current
   grant. Select only exact draft/latest revision, development original, operation
   and budget. Never retrieve changing results, observations, steps or reservations.
   Expired current executions deny before ciphertext; history behavior is unchanged.
2. Connect an explicit current records/key binding at the authenticated factory.
   Do not borrow its history binding, activate providers implicitly or add a public
   request option. An absent binding keeps the ordinary reader; invalid bindings deny.
3. Canonical crypto/source validation and exact configuration/profile/input/draft
   equality remain. Revalidate the recorded current scope assessment through the
   existing owned scope service; history or expired scope cannot substitute.
4. Initial binding and first authorization share a read-only records phase. Current
   scope validation and its complete final read finish inside that records phase;
   complete records/key readback then follows. Reopen current scope after that
   readback, with every selected development record/key permission freshly checked
   throughout final scope IO and return. Every sequential use checks present
   grants. Scheduling and scheduler-requested/post-effect validation use separate
   phases, with no permission cache or snapshot carried across effects.
5. Retain exact execution admission checks, current expiry, false authority flags,
   cancellation/drain ownership and malformed/escaped/parallel-work denial.
6. Verify native ordinary/owned equality, late draft/hold/key/source/records/profile/
   scope loss in both closure directions, missing current discovery, post-effect revocation and actual shutdown
   drainage. Run focused/broad/types and both authenticated synthetic save/reopen
   directions. Count all provider attempts; keep unchanged benchmark and live
   model/UI/runtime-save acceptance requirements separate.
