# Plan

1. Add request-bound identity checks around the actual GitHub Brief store.
2. Require a full fresh authority dependency and preserve uncertainty after dispatch.
3. Bound operations and prohibit overlapping/closed/late continuation.
4. Extract the existing native-Git fixture without weakening its storage tests.
5. Exercise registry preview/confirmation/save/status through the real writer/store,
   plus identity, authority, revocation, timeout and recovery negatives.
6. Run focused and full checks, update the ledger and publish the candidate.

## Next route

Complete full current/historical authority composition and actual provider-record
compatibility without upgrading the existing Gate 1 record or inventing qualified
human evidence. Then bind the writer through trusted per-request runtime services,
add exact confirmation/save/status UI, authenticated board and revision-bound
decision review, and verify the combined journey with appropriate configuration.

Keep runtime writes closed pending accepted gate/authority evidence and separately
authorized GitHub write scope. Preserve all five R5 findings, 0120 archival work,
0124 history/protection, model conversation, manual audit and signed pilot duties.
