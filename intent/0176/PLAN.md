# Plan

1. Add exact opt-in runtime ownership and non-MCP request draining.
2. Test pairing/scope, signed identity/current Git grants, revocation and cleanup.
3. Connect local Temporal through the production runtime and HTTP; verify owned
   closure leaves the independent worker/server connection usable.
4. Run regressions/builds, document, commit/push and verify exact remote equality.
