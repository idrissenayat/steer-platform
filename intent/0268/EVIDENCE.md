# Development evidence — 2026-09-09

Base: `5e890f0bd82da829e05c0350bffb7c0d03d9af0f`.

## Implemented

The existing 34-source SQL/recorded-SDK journey now starts from native Git corpus
collection and uses the real new/existing destination router, not a fixed return
value. Synthetic lifecycle policy selects 16 item roots, including a current
pre-pull candidate and an amendment at a later commit than its original target.
The resulting 34 Brief/Spec sources enter immutable encrypted scope admission
before any recorded role runs. All provider responses and policy are synthetic.

Integration exposed two production-contract bugs:

- Exact citation decoding stripped a BOM at the beginning of a document or slice.
  `ignoreBOM: true` preserves those source bytes; omission and split UTF-8 remain
  errors. No observation, signed artifact or source is normalized or rewritten.
- Direction schemas and destination/UI checks expected root Brief paths even
  when the actual corpus returned a versioned candidate Brief. Direction now
  preserves that exact path. Revision/linking verifies the current pointer, full
  bundle and root mirror. An old identical candidate cannot pass. Versioned
  candidate scope cannot silently become an amendment. Projection/writer paths
  remain unchanged.

The actual React package panel recognizes the existing candidate item, preserves
the reviewed path and does not offer amendment selection for that source.

## Verification

Focused native/contract/React checks: 40 passed; native corpus plus UTF-8 citation
checks: 11 passed; all eight package type checks passed. The broad suite passed
all 1,294 regressions, prototype typechecking, the optimized Next 16.3.4 build,
95-artifact kit validation and workflow token-scope audit. The updated ten-test
actual React component suite also passes. Final broad (1,294 tests) and optimized
build rechecks passed again after the last changes.

The separate focused candidate-save SQL/Temporal/native-Git suite passes all 40
checks plus idempotent migration verification. This remains distinct from the
joined native-corpus/SDK/history/confirmation path and is not the full SQL suite.
The joined run passed all six preview cases and lost-acknowledgement confirmation
recovery; its final independent head-race check encountered expired synthetic
session authority (401). Only that test identity is refreshed before the race;
actual API expiry controls are unchanged. The complete joined rerun passed all
six focused run-discovery checks plus idempotent migration verification, including
the final native-head race and retained history after edits. It is not the full
SQL suite. Both disposable SQL runs removed only their own synthetic containers
and tmpfs data.

All 323 local Markdown links across the 12 changed documents resolve, and
`git diff --check` passes. The protected Architecture, canonical Exam and accepted
retention-policy hashes remain unchanged. User-owned roadmap/outputs are excluded
from this increment.

An initial typecheck encountered four duplicate generated `*.d 2.ts` / validator
files in the ignored Next cache. Only those regular, untracked generated copies
were moved recoverably to `/tmp/steer-0268-generated-types.FkjnJq`; source and
user-owned work were untouched. A later test-only literal-number type error and
test syntax typo were corrected before successful focused/type checks.

## Limits and next work

Native Git and disposable SQL are real engines; identities, keys, recorded SDK
provider bodies and governed-policy responses remain synthetic. API confirmation
does not dispatch the save. No live signed-in walkthrough, semantic model-quality
evaluation, real GitHub bundle write or complete I1–I6 acceptance is claimed.
The API-key skill preserves the resolved credential setup and synthetic-only
boundary. User-owned roadmap/outputs and protected signed files remain in place.

Next extend the same joined path from confirmed original through fixed save
execution and verified bundle reopen, then complete governed startup bindings
under existing activation prerequisites. Actual records adoption, model budget,
provider/write authority and signed-in acceptance remain open. No spending,
credential change, runtime provider grant, gate, deployment or release occurs.
