# 0268 implementation specification

1. Seed only an owned temporary native Git object database, then use the real
   corpus/catalog/byte readers before hashing or admitting encrypted SQL scope
   originals. Preserve current candidate and amendment sources under synthetic,
   explicitly separate product/lifecycle/source policy. Never rewrite originals.
2. Compose two recorded scope batches, both recorded SDK drafting roles, encrypted
   history, current final review and the native destination router in human-only
   package-preview HTTP requests. Verify six cases across the five directions:
   new distinct, linked canonical, linked pre-pull candidate, pre-pull revision,
   first amendment and older-target continuation.
3. Keep a versioned reviewed Brief path exact through direction, final review and
   package identity. For revision or linking, independently verify that it is the
   current `CANDIDATE.json` bundle's Brief, with the full verified bundle and root
   mirror. An older identical Brief cannot substitute for the current pointer.
4. Versioned candidate targets cannot become amendments through changed lifecycle
   or proposal selection. The actual React panel derives the existing item from
   that exact reference and does not offer proposal continuation for this case.
   Legacy projection and single-document writer path schemas remain unchanged.
5. Exact UTF-8 citation decoding must preserve BOM bytes at document or slice start.
   Omission, normalization and character-splitting still fail. Old incorrectly
   accepted observations are not rewritten or normalized to restore acceptance.
6. Keep all current source, policy, historical result, tool and human rechecks.
   Changed policy between destination reads or a moved native branch must withhold
   preview. Read-only preview creates no operation, original or reservation.
7. Through the composed preview, test explicit confirmation, lost admission and
   original acknowledgements, one immutable encrypted original and identical API/
   worker operation identity. Confirmation is not dispatch or a Git save.
8. Run focused native/contract/actual-component regressions and the composed
   disposable SQL/SDK path. Record broad regression/build/type checks separately
   from the full SQL suite, visual QA and actual signed-in user acceptance.

No SQL migration or live installation is authorized. D1 adoption, actual governed
services, model budget/provider access, write grants and I1–I6 acceptance remain
separate. Source commits/pushes use only the existing development branch.
