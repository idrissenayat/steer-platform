# Native records read-set feasibility

Continue the [whole-request correction](../0298/REQUEST-BUDGET-PLAN.md), not another
production micro-optimization. Determine whether one bounded collection and final
readback can cover the records needed by the real authenticated synthetic journey:
old/current drafts, original scope and generation inputs, recorded model exchanges,
results, operation checkpoints, reservations, and the confirmed save original.

Use existing PostgreSQL roles/RLS, encryption, deterministic prompt reconstruction,
SDK verification, and actual OIDC/Git-grant lookup against disposable fixtures.
Preserve every existing journey assertion before running this experiment. A
successful experiment is evidence for designing the integrated correction, not
permission to install it or a completed user-workflow checkpoint.

No changes to signed documents, authentication, the selected technology stack,
existing drafts, live databases, real model use, runtime GitHub access, or spending.
The fixed progress tracker remains 17/25 (68%) until an acceptance checkpoint passes.
