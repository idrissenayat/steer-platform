# Brief — Actual agent-led intent development

The user wants the newly provisioned model credential connected to STEER's actual
workflow, not the separate browser-local preview. Start from free text, ask only
necessary follow-ups, and prepare Brief, Spec and separate-context Test Agent Exam
candidates. Keep the current authentication and pink/orange design.

Model spending is not yet authorized. Implement and verify the real code path with
synthetic provider responses, leaving live activation explicit and closed. Preserve
existing user drafts, canonical documents, gate boundaries and GitHub permissions.
