# Intent 0275 specification

1. Preserve all existing generation, correction, reassessment, confirmation and
   exact original recovery assertions. Carry that same confirmed reference and
   corrected document bytes into save; do not regenerate or inject a package.
2. Route start, status and exact read through the actual signed-identity runtime
   and its concrete factory. Add synthetic tool grants before final source review,
   never move the reviewed head to grant save authority after confirmation.
3. Keep a distinct synthetic start policy closed through confirmation. Denied
   start cannot schedule, create a step, mutate Git or add records/reservations.
4. Start one fixed reference-only Temporal workflow via its real client. A lost
   scheduler acknowledgement followed by retry must recover the same workflow,
   without another start or claiming a save.
5. Execute one fixed, single-attempt activity against one preserved original and
   disposable native Git. Simulate a lost Git acknowledgement: workflow completion
   stays uncertain; one native commit and the durable dispatch marker exist.
   Workflow history must not contain documents, credentials or private manifests;
   replay cannot resend.
6. Reconstruct the actual identity/factory before HTTP status recovery. Verify one
   exact provider receipt; status cannot checkpoint or grant retry. Perform the
   separate existing reconciliation, then demonstrate repeated start without resend.
7. Move the synthetic root after save, then reopen the earlier receipt through the
   authenticated application's exact-reader service. All Brief/Spec/Exam and
   manifest bytes must match the confirmed original, including the human edit.
   Current read-policy denial and current Git tool-grant revocation must withhold it.
8. Preserve one encrypted original, two operations and six synthetic model
   reservations. Close all four managed API runtimes, pools and owned worker/harness
   resources; remove only disposable fixture data. No publication-retention action.
9. No real credential, policy/D1 adoption, gate, model spend, provider access,
   deployment, protected source or user-owned roadmap/output changes. The remaining
   confirmation latency, owned model-worker scheduling, governed bindings and
   signed-in I1–I6 acceptance remain separate open work.
