# Verification evidence

Base: `a71a33108b7572cb78a98a91c14278a434218e9f` (0293).

Implemented private forwarding of exact, proven corpus reads through the candidate
catalog. Unknown ports retain full policy checks; replaced proven methods reject.
The captured source read still performs both source grants and fresh current
checks. Parent guards, read counts and actual callback admission remain in force.
Candidate bundle callbacks now optionally report actual entry/settlement to a
parent; this is bookkeeping, not an authorization or public request field.

All **73 focused adapter tests** pass (42,885.699 ms), including native exact-byte
catalog equivalence, policy reduction, unproven/replaced method rejection,
revocation and shutdown. A held final bundle policy keeps the parent's admission
after the child returns cancellation; it releases only after actual settlement.
The existing read-cap test was subsequently expanded to compare the proven path;
the final broad run covers that additional assertion. Prototype and all eight
package typechecks pass (2.167 s).

All **three authenticated joined checks plus idempotent migrations pass**. The
actual constructor graph retains 34 native sources, two scope batches, separate
recorded Architect Brief/Spec and Test Agent Exam, corrections, six synthetic
model calls/reservations, exact confirmation and one native Git commit. Lost
scheduler/provider acknowledgements, replay, reconstruction, exact old-commit
reopen and current policy/Git denial do not create another original, reservation,
model dispatch or save.

| Action | 0293 requests | 0294 requests |
| --- | ---: | ---: |
| Source review | 232 | 210 |
| Scope preparation | 932 | 844 |
| Save review | 930 | 886 |
| Save preview | 5,145 | 5,057 |
| First confirmation (discarded reply) | 10,483 | 10,307 |
| Drafting preparation, initial / repeated | 4,438 / 3,988 | 4,350 / 3,900 |

Repository/body counts are unchanged; only repeated identity checks are reduced.
Reconstructed confirmation includes both token refreshes (10,279 requests);
repeated confirmation uses 10,277. Draft read, scope start/read and drafting-start
counts are unchanged. All twelve action samples, two preparations and nine
starts are retained in [raw measurements](PERFORMANCE.json), with partitions
where the harness provides them. Single undelayed timings are not p95 claims.
No delayed-prefix, full continuation, full SQL or complete C22 rerun is claimed.

Recovery returns committed in 223 ms with 17 requests, 14 SQL phases, three valid
clock observations and no SQL failures or clock reversals.

The final broad run passes **1,462/1,462 tests**, with no failures, cancellations
or skips (148,286.160 ms), including the expanded proven read-cap comparison.
The Next.js 16.3.4 optimized production build passes. The kit, workflow-scope,
source/harness fingerprint, request partition, document-link and protected-source
checks pass. Overall progress remains **68% (17/25; 8 remaining; +0 points)**.

Source review still exceeds 200 requests, and confirmation's 10,307 requests
show why source-only optimization cannot finish C22. The next priority is the
whole save path's repeated records/history read validation, with a coherent
invocation-owned read set and fresh checks/readbacks around effects. No acceptance
ceiling or authority boundary changes to claim progress.
No complete performance checkpoint, real model, signed-in UI or runtime GitHub
save is claimed. The 200-request ceiling is unchanged. The earlier 0289 recovery
failure remains retained; a later passing run is not its explanation or fix.
