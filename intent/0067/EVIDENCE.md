# Development evidence

Baseline 4917106c5d9588eaeb519c4df8d81de2036e5a76 plus this increment.
2026-09-05 UTC. Generated fixture evidence and synthetic signers only.

Five accessibility groups pass. The positive case consumes the complete 32,900
raw rows / 2,664,900 checkpoint keys and recomputes the exact original raw digest.
All 16 original accessibility cases are exercised. These are generated fixtures,
not 32,900 human sessions or a qualified audit. manualAuditComplete remains false.

A pre-key human-provider proof reaches valid true in the actual frozen full
matrix checker, then is rejected by the composed path before pulling any rows.
All six signatures and native time fields, anchor substitutions, assignment/key
expiry and future evaluation boundaries deny without row work. Row timestamps
outside identity/signing intervals fail on the first pull and execute iterator
finally cleanup. Empty/oversized/throwing streams, invalid metadata, unsigned
clock overrides and async iterator inputs return fixed content-free failures.

Two inventory groups verify the exact frozen source hash, all ten signed public
functions, two unsigned helpers, two retired factories and 18 historical
primitive/constant reexports. All successor exports and regression suite paths
exist. Production source contains no direct frozen/candidate references. This is
an explicit coverage inventory, not a full call-graph or security proof.

All seven new groups and root `pnpm check` passed under isolated Node 24.20.0 /
pnpm 11.19.0: kit (95 required artifacts), workflow scope audit, typechecks,
88 prototype tests, 99 root control/correction tests, all seven package suites
and builds. The new full accessibility test ran again in the root suite;
unchanged package tasks reused Turbo cache. No new real browser/provider/manual
audit or storage integration run is claimed. Frozen-lockfile install and
`git diff --check` passed; intent/0001, .github and lockfile diffs were empty.
Publication is identified by the containing Git commit.
No frozen review, protected Exam, production route or real audit record
is edited. Full normative/retention/migration, real integrations, manual evidence
and independent/protected acceptance remain required; no gate is signed.
