# Specification

- Add strict `intent.runs.discover` input/output and one human-only shared HTTP/MCP
  query with its own present tool grant, exact scope echoes and late identity check.
- Discover both captured-original types across preserved revisions of one draft
  using only the restricted draft SQL role and current records authority.
- Verify latest and source revision metadata, original bindings and lifecycle.
  Recheck the page after authority callbacks without holding leases in callbacks.
- Return up to 20 content-free references and a revision-bound keyset cursor.
  No timestamps imply execution order; no empty page implies uniqueness or no attempt.
- Connect explicit selection to separately authorized historical scope findings
  and original Brief/Spec/Exam comparison. Never pass output to adoption or saving.
- Clear metadata/content on page/context/identity changes, failures, visibility
  loss and session/records expiry. Reject late prior-context replies.
- No new dependencies, provider binding, migration, auth bypass, execution,
  model spending or real GitHub runtime saving.

Admissions without captured originals and real signed-in provider/repository
acceptance are not claimed by this metadata recovery increment.
