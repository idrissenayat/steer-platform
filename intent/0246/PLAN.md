# Implementation plan

1. Implement exact browser transport and current-source scope controller.
2. Mount progress, recovery and cited findings in the actual editor without
   replacing its existing draft controls, authentication or visual design.
3. Exercise real React interaction with synthetic HTTP: lost acknowledgements,
   automatic read-only polling, partial/full findings, stale edits, denial, expiry,
   hidden state and unsupported proposed Brief paths. Run structural accessibility.
4. Run full regressions, types/build, architecture-boundary, migration-history,
   kit/scope and document checks; record limits and commit/push owned changes.
5. Continue retained scope-review discovery/recovery after refresh and server-bound
   consumption of assessment evidence by direction/draft preparation, then actual
   authority, semantic evaluation and human save/reopen acceptance.

Live records still require D1 adoption. First-test model spending is unapproved;
runtime Git saving and all existing gate/release limits remain separate. Do not
turn UI installation, synthetic HTTP or source citation validation into approval.
