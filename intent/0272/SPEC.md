# Intent 0272 specification

1. Keep concrete storage, SDK and configuration construction in the existing API
   composition root. Do not relax architectural import boundaries or add a second
   application/profile loader.
2. Require fixed scope, development and candidate execution configuration; exact
   scope/recorded-role profiles; retrieval revision; publication binding; and all
   existing policy, records, key, read, scheduling and cleanup dependencies.
   Match every records coordinate and publication item allowlist. Scope/development
   must use one exact budget binding, not independently spendable caps. Validate
   the scope-profile digest and candidate-save action/null-budget contract.
3. Construct all 21 actual service implementations plus the separate publication
   recorder. Wire owned SQL drafts into current review and save. Wire native corpus
   collection into scope and development preparation, current scope verification
   into assessed drafting/final review, and historical scope verification into
   retained drafting lineage and package preview. Derive preserved role profiles
   from the same recorded SDK profiles used for verification.
4. Use native new/existing destination composition and the same publication
   identity for preview, confirmation, start, status and recording. Construct exact
   bundle and proposal readers. Never accept an injected service inventory,
   destination result or caller-supplied evidence as a construction shortcut.
5. Transfer provided resource ownership on entry only with a valid cleanup method.
   Pin cleanup, close owned services in reverse dependency order, await actual
   resource cleanup once, and preserve failure. Clean up after construction errors.
   The resource owner remains responsible for its real leases, I/O and scheduler
   lifetimes; initialization does not start workflows or provision resources.
6. Preserve the 0271 inventory test and add concrete-factory authenticated SQL
   coverage: exact draft recovery after unknown acknowledgement/reconstruction,
   all four discovery reads, native current Brief/Spec corpus review, revocation,
   foreign products, holds and actual owned database-pool closure. Keep unexercised
   authorities denied and distinguish source review from semantic assessment.
7. Regress factory binding/missing-capability/cleanup failures, run focused SQL,
   broad regressions, types/build and documentation/protected-source checks.
   Commit/push only verified owned changes. This is not live I1–I6 acceptance.
