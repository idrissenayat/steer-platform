# Brief

Connect the Brief document model to the authenticated platform boundary so humans
and agents can consume the same exact source structure. Preserve the existing
curated content-reader permission rather than widening access through a new name.

Require the selected source revision and content fingerprint. Recheck current
identity and both grants after reading/parsing, independently verify content
digests, and expose the same contract through HTTP/OpenAPI and MCP.
