# Implementation plan

1. Verify the 0050 checkpoint and current projection/authentication contracts.
2. Bind the portable model through an explicitly dual-granted read-only tool,
   exact fingerprint selection and shared strict schemas.
3. Add native security/source regression and HTTP/MCP parity tests; exercise the
   compiled browser and agent against isolated real identity/Git/database services.
4. Run repository/frozen-install checks, record exact evidence and limitations,
   update project guides, publish candidate and verify remote commit equality.

Next: bounded authenticated Brief discovery/catalog and rendered backlog/detail
binding. The diagnostic reference panel is not the final originator workflow.
Five R5 findings and canonical gate-proof/formal requirements remain open.
