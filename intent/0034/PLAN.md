# Execution route

1. Verify official SDK v2 availability and current serving contract; pin packages
   and restrict the vendor import to the API transport adapter.
2. Bind discovery/calls to canonical registry schemas/dispatch with strict bearer,
   origin, protocol, body and admission controls.
3. Compose fixed Git-backed OIDC authority and actual operation-drain lifecycle.
4. Test using the official v2 client, then actual local HTTPS, Keycloak and Git;
   retain the browser/Postgres regression suite.
5. Run root checks/frozen install/dependency audit, document evidence and limits,
   commit and push the candidate. Continue combined runtime transport integration
   and durable workflow foundation without enabling real providers or deployment.
