# 0034 · Shared MCP transport

Parent M4. Add the official TypeScript SDK v2 Streamable HTTP endpoint over the
existing canonical tool registry and current Git-backed OIDC authentication.

BRIEF, SPEC, PLAN, development ACCEPTANCE and EVIDENCE accompany this increment.
It is an explicitly composed remote-agent transport, not default platform runtime
activation, OAuth onboarding, a protected Exam or release approval.
