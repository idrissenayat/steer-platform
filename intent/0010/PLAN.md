# Execution route

1. Record the contract and denial cases before code.
2. Add the bounded GitHub transport, scoped token cache and exact-commit reader.
3. Add the read-through authorization resolver and connect shared record schema.
4. Test signer, permission restrictions, integrity/path boundaries and source
   freshness using isolated transport only.
5. Run repository checks, document limitations, commit and push.
6. Continue with ingestion/reconciliation, then real local identity composition.
