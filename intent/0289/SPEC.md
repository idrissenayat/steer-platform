# Intent 0289 specification

1. Add an optional trusted private evidence-window hook to the drafting preparer,
   using the existing guarded preparation helper. Pin the hook for the operation;
   preserve full fallback and initial acquisition, nonvoid/error handling, pending
   work ownership, fixed expiry and all current assessment/source/draft checks.
2. Wrap only each read-only source/policy/source recheck. Do not enclose operation
   admission, original puts/readback or any scheduler/model/Git effect. Open fresh
   after every effect. Install the hook in the actual owned journey's corpus
   composition; no browser/DTO/environment activation or authority override.
3. Compare the native Git/SQL full fallback and window path for exact original,
   body/request counts and preserved effects. Prove windows close before writes,
   late source revocation denies, post-effect drift returns uncertainty without
   resending, and invalid hooks cannot authorize an effect.
4. Run focused preparer/helper tests, explicit preparation SQL selection and the
   full authenticated native/SDK/Temporal save/reopen regression. Capture drafting
   preparation request totals and origins for future comparison; no earlier
   managed-preparation measurement may be invented.
5. Run broad tests, types/build, links, raw hashes and protected hashes. Preserve
   user files, signatures and pink/orange UI. No live records/D1 adoption,
   credential change, model spending, runtime write/grant, deletion, gate,
   deployment or release. No progress points until a whole checkpoint passes.
6. Retain the first joined recovery-unknown observation. Add bounded, content-free
   SQL phase/request diagnostics and run focused save recovery plus the complete
   joined path; do not hide a failed sample, weaken its assertion or call a rerun
   a production fix. Keep its cause open unless evidence establishes it.
