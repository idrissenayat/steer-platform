# Brief — Check existing scope before creating another intent

The user requires comparison with existing intent and included scope to avoid
duplicates. Implement permitted Brief/Spec candidate retrieval with matching source
evidence, revision fingerprints and honest coverage. It must support later explicit
open/update/link/new choices, never automatically merge, discard or create an item.
The complete outcome is defined by the parent journey plan, not this module alone.
