# Development evidence — 2026-09-07

Base: `09c3ab8`. Node 24.20.0.

The existing continuation heartbeat was updated, not duplicated. It is active on
the same task at its existing ten-minute interval and now follows
`docs/INTENT-JOURNEY-PLAN.md`. The stale local-preview/overnight priority is removed.
It preserves unresolved authority, the unapproved model budget and quiet waiting.

## Verified

- Focused HTTP/domain/registry/architecture run: 26/26, including seventeen new
  overlap tests, actual HTTP dispatch and eight existing package-boundary controls.
- Domain, registry and API typechecks, kit validation (95 required artifacts),
  token-scope audit and diff whitespace checks passed.
- Existing domain suite passed 20/20 and registry suite passed 134/134 before the
  additional raw-content-budget case; that final case passed in the focused run.
- Tests cover literal matching and partial keyword overlap, unrelated/generic
  queries, negation retained in evidence, Unicode, instruction-like source, long
  paragraph excerpts, exact Brief/Spec coordinates, changed Spec fingerprints,
  missing/unconfigured documents, every required grant, cross-tenant/repository
  denial, corrupt digests, wrong revisions, private reader errors, revocation,
  catalog changes, candidate/item limits and the 4 MiB analyzed-content limit.
- A test-fixture readonly typing issue was corrected by checking the fixture with
  `satisfies`; production reader immutability was not weakened.

## Limits

This is candidate retrieval through the real typed registry, not semantic duplicate
classification or a connected UI. All outputs explicitly deny semantic completeness
and authoritative clearance. Fresh projection metadata is not a latest-Git-head
guarantee. Full-corpus retrieval, UI evidence/disposition, semantic review, durable
drafting, budget activation, authoritative recheck/save/reopen and I6 acceptance
remain in the parent plan. No source grant, secret, model call, spending, deletion,
runtime GitHub write, protected Exam, signature or deployment was changed.

The unrelated untracked roadmap and outputs were preserved. No frontend source or
service restart was needed for this backend portion.
