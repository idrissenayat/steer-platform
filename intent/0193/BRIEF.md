# Brief

The user rejected a synthetic-only journey and approved persistent local identity
configuration with their real initial administrator account. Configure the
existing production composition root against persistent Keycloak/PostgreSQL and
actual GitHub-read membership. Preserve the existing preview and all write/gate
restrictions. User password selection and certificate trust must not be bypassed.
