# Implementation route

1. Pin a trusted lifecycle reference and validate actual current/history events.
2. Validate independent current inventory/state and compute retention boundaries.
3. Verify full human/raw approval and derive exact grants for every copy.
4. Invoke the shared verifier, validate provider receipts and complete aggregation.
5. Verify separate human/shared authorization and receipt for tombstone commit.
6. Exercise composed positives, replays and hostile paths; run repository checks,
   document remaining coverage, preserve frozen artifacts, commit and verify push.
7. Continue migration graph composition with actual plan and before/after truth,
   then remaining public-oracle timing and lifecycle matrix/registry coverage.
   Complete-package independent review/protected incorporation follows; no
   premature signature, deletion, provider access, spending or release.
