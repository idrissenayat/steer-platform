# Brief: Close the lifecycle composition bypass

R5-001 identified a lifecycle effects path that accepted an event surrogate the
closed event contract rejected and bypassed the shared protected-action contract.
0058–0060 supplied the individual corrections; this increment composes them.

Require the actual validated history, full human/provider approval, authoritative
copy inventory and hold/reference state, every copy's scoped credentials and
CAS evidence, provider receipts, complete aggregate and separately protected
tombstone commit. A missing second copy or tombstone permission must not pass.

Preserve raw working-copy cleanup's 60-second deadline and ordinary retention
expiry rules. Verify supplied evidence only: do not delete data, acquire a token,
reserve a live store, sign a human gate or modify the frozen send-back package.

This delivers a tested development composition, not closure of all R5 findings,
the full historical/future retention matrix, migration correction or Gate 2.
