# Development plan

1. Add portable reference/binding schemas and exact result/source validation.
2. Add an explicit assessed preparation factory and immutable-original rechecks.
3. Connect the existing React scope panel to direction and generation preparation.
4. Test strict selection, empty/incomplete corpus, exact retries, current authority,
   stored role context and start/worker reconstruction using synthetic HTTP/SDK and
   disposable PostgreSQL. Run full regression, integration, types and build.
5. Update the journey/ledger, preserve signed hashes, commit only owned files and
   verify the remote source commit. Continue larger-corpus drafting/context and
   semantic evaluation without treating missing live authority as implemented.

No real records activation, paid calls, runtime saves, deployment or gates.
