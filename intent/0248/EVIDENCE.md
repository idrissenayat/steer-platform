# Development evidence — 2026-09-08

Base: `60626043aa5cd22c5f7589001efe709e8a8f5956`.

## Capability

The actual editor now carries its exact recorded scope assessment through the
human's chosen direction into document preparation. The server obtains findings
from its pinned authorized reader, not browser text. Full findings and citation
bytes are encrypted with the immutable original and hashed into the role inputs.
Both drafting roles see the human's source and direction; the Test Agent receives
the new Brief/Spec but no prior Exam or Architect commentary.

Original restoration revalidates current scope evidence, including downstream
start and worker request paths. Optional historical schema compatibility does not
let the new assessed API factory accept omission. The explicit empty-inventory
alternative is verified server-side and needs no scope model call.

## Verification

- Final full regression: **1,111/1,111 passed**, including four new portable/editor
  checks. The existing actual React journey now asserts assessment before each
  direction, exact reference propagation, clarification/re-assessment, lost start
  recovery and editable candidates. Scope React/axe tests continue to pass.
- Final full PostgreSQL 16.14 integration: **332/332 passed**, including six new
  actual API/SQL checks covering recorded assessment consumption, immutable retry,
  both role contexts, rejected inputs, current profile/access, empty inventory,
  unchanged coverage limits, start and worker request reconstruction. The first
  focused SQL/HTTP/SDK/Temporal run passed **60 checks plus idempotent migration
  validation** before the additional assessed start/worker test. These overlap the
  full integration suite and must not be added to its count.
- Prototype/all eight package typechecks and optimized Next.js 16.3.4 build pass.
  Drizzle history, kit (95 artifacts), workflow scope audit and whitespace checks
  pass. No migration or dependency changes were needed.
- All **143 local documentation links** in the current plan, ledger, implementation,
  workflow contract and increment packet resolve after the final ledger update.

Protected SHA-256 values remain unchanged:

- `intent/0001/ARCHITECTURE.md`:
  `9e1783a5f9870e8a8a2595d23226efa804902b4c472e309bf9f924d8cf61dc65`.
- `intent/0001/EXAM.md`:
  `84ad1d4c14d6614fe4b53509104e379e9ad6dff0462c96109901eefb1e02fd7f`.
- `intent/0001/reviews/domain/round-2/remediation/RETENTION-AND-RECORDS-POLICY.candidate.md`:
  `f8a9cb9acc90e2943181be428cb03bebcce64758a3ac19bf1243e3bbe3894e32`.

## Boundaries

The tests use production React code with synthetic HTTP, actual recorded SDK code
with synthetic model responses, and disposable SQL. No real-browser visual,
narrow-screen, semantic-quality or live human acceptance is claimed. The new
factory stays uninstalled and the old generation-envelope limit stays intact.
Current-only scope revalidation deliberately blocks assessed-original restoration
once its scope review is expired/superseded; historical display remains separate.

No real migration, D1 adoption, credential inspection, paid model call, runtime Git
save, gate, auth bypass, deployment or release occurred. The API-key skill retained
the already resolved choice. User-owned `docs/REAL-USER-ROADMAP.md` and `outputs/`
are excluded. The existing one-minute loop remains active.
