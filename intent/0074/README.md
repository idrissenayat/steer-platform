# 0074 · Raw grant and batch lifecycle integration

Integrates the 0073 pre-terminal grant into the actual 0061 lifecycle candidate.
One human grant covers every exact prepared raw tuple; each copy still traverses
the complete shared action verifier. Full original and current independent batch
reservation chains prevent committed status from replacing authorization proof.
Tombstone authorization remains separate.

Offline evidence only: no atomic store, deletion worker, token or live effect.
Mixed partial-copy recovery remains closed on raw-v2; subsequent 0075 adds an
explicit raw-v3 single-checkpoint path. Read BRIEF, SPEC, PLAN, development
ACCEPTANCE and EVIDENCE. All five R5 findings remain formally open.
