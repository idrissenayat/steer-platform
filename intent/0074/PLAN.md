# Plan

1. Require raw-v2 and derive pre-terminal context from installed lifecycle scope.
2. Replace per-copy human raw approvals with one complete pre-terminal grant.
3. Bind current state and every copy request into an independent batch plan.
4. Verify full original winning and current head/replay/reservation chains.
5. Preserve shared actions, per-copy deadlines, aggregate and separate tombstone.
6. Test first/replay, transplants, forged proofs, competing losers, chronology,
   capacity, holds and crash-cut denial; run repository checks.
7. Document limits, commit and verify the candidate branch push.

## Next bounded raw recovery work

Define a durable partial-copy checkpoint protocol anchored to the original grant,
plan and winning opening chain. Preserve exact completed request/receipt pairs
and authorize only remaining prepared tuples. Independently check a newer current
hold/reference/inventory snapshot without altering historical request inputs or
pretending the original snapshot is current.

Do not accept an arbitrary mixed replay/new set. Bind continuation sequence,
current CAS/replay evidence and remaining one-use requests. Deny competing
checkpoints, extra copies, receipt substitutions, scope changes and late erasure.
Test crash cuts around each copy, aggregate and tombstone. Distinguish offline
verification from an atomic store or runner. No new per-object human approval.

Future-retention/rotation/reference, auxiliary-time and migration/normative
coverage still precede independent/protected review. All five formal R5 findings
remain open. No provider mutation, gate, deployment, release or spending.
