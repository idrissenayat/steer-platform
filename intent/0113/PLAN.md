# Plan

1. Reconcile the four indefinite source aliases with their one exact observation.
2. Add closed fixtures with available commit-time head evidence and no effect inputs.
3. Verify unconditional retention under injected effects plus missing/signed-invalid
   head, trigger, expiry and provider-proof cases; keep source alias counts explicit.
4. Save fresh reports, run focused/repository/separate full integration checks,
   update docs, commit/push the candidate and verify the remote revision.

## Next

Twenty-four lifecycle coordinates remain across release/migration records,
rebuildable projections, deletion evidence, raw working corpus, corpus provenance
and sanitized corpus. Do not treat immediate-trigger preconditions or raw completion
within sixty seconds as ordinary minimum retention waits.

For future profiles, review actual required semantics before extending admission:
release records need verified environment retirement; provenance needs complete derived
inventory and the latest retirement/deletion; sanitized corpus needs the earlier trigger
and cryptographic erasure; deletion evidence needs its final tombstone semantics.
The current fixture's ordinary-copy path selects delete-copy for every non-raw class,
so it cannot be reused unchanged to prove sanitized-corpus cryptographic erasure.
Preserve separate runtime profiles and old key windows; unsupported cases stay uncovered.

3,614 migration and one obsolete schema case remain unmapped. GAP-01–06 and M1–M8
remain separate work. No protected edit, real provider/deletion operation, deployment,
release, spending or signature is authorized; all five formal findings remain open.
