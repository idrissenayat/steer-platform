# Specification

1. Map the four LIFECYCLE-GRAPH labels for RC-AUTHORITATIVE-ARTIFACT only.
   The frozen source evaluates all four at 2026-09-04T12:00:00Z because the boundary
   is null. Report one unique positive input; do not invent different elapsed times.
2. Use a schema-valid record-committed event with valid preceding draft history.
   Inventory and state are signed and available exactly at the source observation.
   Original key lifetimes remain unchanged; no future effect receipt is required.
3. Positive controls have empty copy-action/aggregate/tombstone slots within the
   existing closed graph format. The indefinite branch returns retained-immutable,
   null boundary and typed zero effects after validating its actual head evidence.
4. An authoritative active-state claim still retains, without independently proving
   a new qualified hold. Injected complete-format action/receipt evidence cannot
   change indefinite retention and is not asserted valid, accepted or executed.
5. Missing state/inventory, signed wrong parent/history/future state, wrong trigger,
   request-supplied expiry and an outer-signed event with a bad provider proof deny.
   Tests independently verify signatures for selected hostile records.
6. Every hook executes eleven observations; all four source labels execute the same
   eleven-case suite, with identical observation seals. This is source-ID coverage,
   not four independent time-boundary scenarios or live immutable-storage evidence.
7. The separate readiness factory continues rejecting this indefinite class; no
   mutation eligibility or deletion capability is introduced. Fixtures remain closed.
8. Preserve catalog/pins and historical reports. Quick/full expected counts are
   381/397 passed, zero failed and 3,655/3,639 uncovered. Boundary family: forty
   source IDs executed, twenty-four uncovered. All global acceptance flags stay false.

No live retention, permanence, storage enforcement, deletion, independent acceptance,
protected incorporation or gate signature is claimed. All five formal findings remain open.
