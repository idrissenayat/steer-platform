# Development evidence — 2026-09-08

Base: `81f2ac20e97233417e3303ddfbcd5de9f71bbbb3`.

## Capability

Explicit historical original recovery verifies the captured scope assessment
after expiry or source edits without weakening current-only admission. It returns
the exact private input for later server-side lineage composition, not generated
output authorship or a browser response. See [the guide](../../docs/GENERATION-INPUT-HISTORY.md).

## Verification

Final **1,171/1,171 regression tests** pass, including focused 9/9 original and
historical-scope unit checks. All-package and prototype typechecks, the optimized
Next.js 16.3.4 build, kit (95 required artifacts), workflow token-scope audit and
whitespace checks pass. All 207 local links across 12 increment-related documents
resolve; protected Architecture, canonical Exam and retention-policy hashes are
unchanged.

The full disposable **PostgreSQL 16.14 suite passed 342/342 checks**. A prior focused
scope run passed 65 runtime checks plus idempotent migration verification; it was
not mislabeled as the full suite. New SQL cases recover exact 34-source assessed
generation inputs through reconstructed readers after source edits and execution
expiry. Missing/denied history, changed source evidence, key loss, late revocation
and holds withhold content. Original, operation and revision rows, synthetic model
calls and reservations remain unchanged by reads. Legacy originals and complete
empty-corpus originals also recover without inventing model assessment evidence.

The runner removed only its own synthetic containers and tmpfs data. Real retained
records were not migrated, activated, modified or deleted. No browser/live-user
acceptance is claimed; this increment changes no UI.

An initial typecheck caught negative test fixtures explicitly setting optional
dependencies to undefined under exact-optional-property types. Tests now omit the
dependencies to exercise actual absence; production types and permissions were
not loosened.

## Authority boundary

No default backend installation, records-policy adoption, live migration,
credential inspection, paid model call, runtime Git write, auth bypass, gate,
deployment or release. SQL and recorded SDK responses use synthetic authority;
this does not constitute signed-in human/provider acceptance or full lineage.
No UI, dependency or database migration is changed.

The credential skill preserved the already resolved decision without inspecting
keys or asking again. The existing one-minute loop remains active and unchanged.
