# Plan

1. Bind full original/current chain evidence to a trusted checkpoint slot.
2. Verify opening winner, exact retained bytes and terminal committed consumption.
3. Require independent delivery outcome and fresh non-winning current readback.
4. Exercise lost acknowledgment, signature/lineage/progress/time failures and
   original-byte preservation with no resume or execution authority.
5. Run full checks; document, commit, push and verify remote publication.

## Next

Compose successive checkpoints around exact chain-prefix extension. A previous
current checkpoint must bind the next opening/head and the immutable prior
attempt prefix; progressed evidence cannot re-run a previously consumed step or
replace prior truth. Distinguish unchanged readback from a new checkpoint update.
Then address remaining crash cuts, source adapters, real durable storage/restart
and full normative/source/class/trust-era coverage. Keep explicit current-time
verification; do not extend frozen keys or treat old approvals as current.
Successive observation times can differ. A retained prefix containing a contract
human bundle currently carries its unsigned original audit clock; support current
revalidation without changing that immutable prefix or any signed native time.
Do not silently omit failed/rolled-back contract attempts from the prefix.

All five R5 findings remain open. No protected edit, signature, provider mutation,
real migration/cleanup, release, deployment, spending or resumed execution is authorized.
