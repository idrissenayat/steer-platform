# Plan

1. Verify bounded, scoped identity-service attestations using real Ed25519 and
   exact authentication/signing/recording/key chronology.
2. Join the provider-signed identity digest to current Git identity sources and
   the existing historical/current human-hat verifier.
3. Test authentic-signature identity mismatches, trust/source corruption, time
   boundaries, shared admission, final expiry and real disposable Git history.
4. Run focused/type/full checks, document limits and publish the candidate.

## Next

Complete qualified-domain evidence and full prerequisite/Critic/domain/policy
source composition. Finish actual identity-attestor/receipt issuance and existing
commercial provider-record compatibility under approved bindings; no claim that
this synthetic profile validates the user's existing signature. Then select the
writer through approved runtime configuration, finish destination/head discovery
and save/status UI, authenticated board and revision-bound decision review.

All five R5 findings, 0124 history/protection, 0120 retirement archive, independent
protected review, human gates, real runtime configuration, model conversation,
manual accessibility, release qualification and the signed pilot remain due.
No real provider access, gate signature, write, release, deployment or spending
is authorized by this development increment.
