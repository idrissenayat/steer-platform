# Plan

1. Implement exact pre-terminal preparation and complete human grant checks.
2. Verify positive pass/fail/cancelled outcomes using the same signed authority.
3. Test timing, scope, full proof, provider/tuple and strict-input failures.
4. Run repository checks and document the eligibility-only boundary.
5. Commit and verify the candidate branch push without changing protected files.

## Next: current batch and lifecycle integration

Derive this configuration from the installed lifecycle context, never from a
request-supplied pin. Explicitly version the new raw envelope. Match current
authoritative inventory to the prepared tuples, verify complete actual history
and current holds/references, then verify one authoritative batch reservation
and replay state binding the grant, actual terminal and complete current input.

Derive exact per-copy shared actions from the verified grant without new human
decisions for each object. Preserve distinct scoped credentials, idempotency,
CAS and provider receipts, the inclusive deadline for every key, and separate
tombstone authority. Do not mistake the stable pre-terminal batchBindingDigest
for current execution or one-use consumption authority. Test first execution,
exact replay, competing batches, omitted/extra copies and crash/retry boundaries.

Then finish retention/rotation/reference, remaining precision and migration/
normative coverage before independent/protected review. All five R5 findings
remain open. No provider mutation, live erasure, deployment or spending.
