# Evidence

## Implemented scope

Strict optional held configuration now reaches the actual request-owned writer and
complete source-policy collector through the production identity runtime. A separate
current agent observer is mandatory; human identity is supplied by the existing request
boundary. Normalized policy satisfaction cannot mint authority, request a write token
or mutate Git. Internal immutable diagnostics remain historical and explicitly false
for gate/write authority. Inspection/new assessment and shutdown clear them; shutdown
cannot retain an observation completed by a draining request.

## Verification — 2026-09-07

- Four new focused runtime tests passed: policy-satisfied and blocked chains both
  deny saving; exact current source/pin diagnostics remain non-authorizing; status
  lookup reports not-found; revoked observer/source access denies; invalid/unpaired
  startup config performs no I/O; pending collection drains at shutdown without
  retaining a diagnostic or permitting another request.
- The tests verify actual signed OIDC and App JWTs, native Git current membership,
  cryptographic signer proofs and complete policy collection. Every token request
  remains read-only and actual Git HEAD/files are unchanged. No public proof of
  policy success or source details is returned by the rejected save endpoint.
- The bearer test journey leaves the encrypted browser-session pool unconnected.
  It is not a real-person/Keycloak held-profile flow. Identity JWKS and GitHub network
  ports use synthetic data and have no external fallback; configured observer and
  normalized review facts remain explicitly synthetic.
- Actual HTTP/native Git/PostgreSQL creation regression: three groups passed.
- Actual Temporal/native Git/PostgreSQL integration: 25 checks passed, including the
  existing independent current-human/projector denials, reconstruction and replay.
- Full `pnpm check` passed: 88 prototype tests, 438 repository controls, 99 registry,
  24 data, 61 web, 283 adapter, 95 API, 23 worker and 13 domain tests, all workspace
  typechecks/builds, kit validation and workflow-scope audit. Eligible unchanged
  tasks used local Turbo cache.
- Final actual Keycloak 26.7.3 / Chromium 151.0.7922.34 browser regression passed all
  43 checks, including the 0168 creation-to-durable-worker journey, current access
  denials, exact source links, responsive/keyboard/automated accessibility and owned
  shutdown. This verifies existing/default compositions, not the synthetic held
  profile against real-person/Keycloak gate evidence. Owned services and data closed.

An initial test used an incomplete sample Brief and correctly received HTTP 422 before
reaching policy collection. The test's shutdown wait then waited for an observer that
was never called. That owned test process was interrupted; the fixture was completed
and the wait now rejects if the request finishes before entering its observer. All
four tests passed on rerun. The one leftover native Git directory was identified by
its run time, synthetic commit identity and synthetic runtime grant, then removed.
No production validation or ownership guard was relaxed.

## Limits and cleanup

No live configuration, observer grant, write permission, deployment, release, spending,
real-record deletion, database schema, new dependency or protected artifact changed.
The adapter export is additive; native policy-chain test setup was extracted for reuse.
Owned synthetic Git fixtures and integration services/data were cleaned. No real
provider credentials or person signatures were used. Full governed selection,
independent review provenance and action-time authority remain required; all five R5
findings, qualified/independent protected review and human gate signatures remain open.
