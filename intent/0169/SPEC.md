# Spec

An optional strict `heldBrief` profile binds existing writer and Gate 1/2 policy-source
configuration. It requires an explicit current observer callback; neither side may
exist without the other. Validate scope/platform/decision pins before source I/O.
Keep default runtime behavior unchanged and do not install any live configuration.

The identity service creates one managed held writer per authenticated request. Human
context comes from its actual OIDC or browser-session boundary, not headers claiming
hats/approval. Policy collection uses the distinct configured current agent observer.
All mutation/authority methods remain denied, even when normalized policy is satisfied.
No injected proof, local confirmation or diagnostic can bypass the held factory.

Internal status may expose the last immutable source assessment with its explicit
false authority flags. It is a historical observation, not current readiness or an
authority lease. Later writer inspection/attempts clear it; runtime shutdown clears it
and cannot repopulate it from pending work. No public endpoint, browser display,
persistent record, signature or gate state is added. Drain pending request collectors
before closing runtime resources. The callback's independent identity lifecycle stays
with its trusted owner.

Verify actual signed OIDC and App JWTs, native Git current membership and cryptographic
signer-source collection through the production runtime with synthetic no-network ports.
Exercise policy-satisfied and blocked chains, status readback, revoked observer/source
access, startup rejection and shutdown during collection. Keep all five R5 findings
and independent/qualified protected review and human signatures open.
