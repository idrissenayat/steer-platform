# Development evidence — 2026-09-09

Base: `17022aa70450d4fd85d1d847d553ac8f7f919623`.

## Implemented

A second owned native-corpus/SQL/recorded-SDK journey now continues its actual
confirmed encrypted original through the existing HTTP start, fixed Temporal
activity, native Git receipt recovery and exact HTTP reopen. The first journey
keeps all six destination cases and competing-head rejection. No retained
snapshot is reset or rebased to make the second journey pass.

The new test loses scheduling and Git acknowledgements, checks one activity and
one save, replays history, reads the original-bound receipt, separately reconciles
SQL and reopens the exact older commit after a later root edit. It also checks
start/read denial, unchanged encrypted originals/counts and no new model budget.

## Verification

The isolated `--candidate-journey` run passed its joined check plus idempotent
migration verification. It exercised two recorded scope batches and both actual
drafting SDK roles before package preview/confirmation; recovered the same
scheduled workflow; executed one fixed activity with one native save and a lost
response; verified the original-bound receipt; separately checkpointed SQL; replayed
without resend; and reopened exact older-commit bytes through HTTP after a later
root edit. Current read denial withheld bytes. The retained SDK history remained
verifiable after the human draft changed. No extra model call/reservation occurred.
The owned synthetic SQL/Temporal/Git fixtures were cleaned up. This is not the full
SQL suite or a browser/visual/signed-in acceptance result.

The first type check caught the Temporal harness in the API package, which does
not own those dependencies. It was moved into the worker test package; no new
dependency was added. Prototype and all eight package type checks pass. The broad
suite passes all 1,294 tests; kit validation passes all 95 required artifacts and
the workflow token-scope audit passes. The explicit new focus selector and bounded
diagnostic tests pass (2 tests).
The final prototype/eight-package type recheck and optimized Next 16.3.4 production
build pass after the last changes. No browser/visual acceptance is inferred.

The first expanded run stopped in the existing Architect pre-dispatch path after
approximately seven seconds, before any synthetic model transport call. The SQL
step remained outcome-unknown with its captured request; the fixture was cleaned
up. A broad regression suite was running concurrently. The isolated rerun avoids
that competing load and passed; causation is not established, and no timeout,
evidence check or retry boundary has been relaxed. The expanded `--run-discovery`
recheck passed all seven focused checks plus idempotent migration verification,
including the independent six-case preview/head-race journey and the new joined
save/reopen journey. Its owned synthetic containers and tmpfs data were cleaned up.
Neither focused selection is the full SQL suite.

All 325 local Markdown links in the 12 changed documents resolve, and
`git diff --check` passes. The protected Architecture, canonical Exam and accepted
retention-policy hashes are unchanged. User-owned roadmap/outputs remain excluded.

## Boundaries and next work

Only disposable SQL, Temporal and native Git engines are used. Identities, keys,
SDK provider responses, lifecycle/grant policies and dispatch proofs are synthetic.
The actual factories remain uninstalled. This is not a signed-in UI walkthrough,
semantic model-quality evaluation, live GitHub write or complete I1–I6 acceptance.
The joined save/reopen case is new-distinct work; all six destination cases have
joined preview coverage, not six joined save/reopen runs. Existing adapter tests
do not erase that distinction. Concurrent-load latency also remains uncharacterized.

Next complete governed startup composition and its activation checks without
installing unsigned records policy or granting live model/provider use. D1 records
adoption, the proposed model budget, provider/write authority and actual human
acceptance remain open. Protected signed files and user-owned roadmap/outputs
remain outside this increment.

The activation audit also confirms that the existing trusted `recordPublication`
draft-lifecycle port must be bound to verified publication evidence. This joined
save test does not invoke that records transition: a verified Git receipt and SQL
execution checkpoint alone are not completion of the draft retention lifecycle.
That binding, its trusted time source, denial/recovery behavior and adopted policy
remain required before live use; no clock, hold or records policy is inferred here.
