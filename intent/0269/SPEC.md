# 0269 implementation specification

1. Run a second isolated instance of the native-corpus/encrypted-SQL/recorded-SDK
   journey. Resolve source bytes before original admission. Preserve the first
   journey's six preview cases and competing-head rejection unchanged.
2. Reuse the actual confirmation result, publication configuration, durable
   operation identity and immutable encrypted original. The test worker must load
   that original itself; do not admit another save or rewrite original bytes.
3. Call the existing human-only save-start HTTP service with only its fixed
   reference. Missing tool/current-start authority must stop before scheduling.
4. Lose the first acknowledgement after the real isolated Temporal scheduler
   starts. An identical HTTP request must recover the same workflow without a
   second start. An acknowledged schedule still reports `savedToGit: false`.
5. Execute one fixed worker activity, with one attempt, from the same encrypted
   original and native Git snapshot. Lose the synthetic Git response after the
   actual owned object database commits. Workflow outcome remains unknown and SQL
   remains dispatch-committed; no automatic retry is authorized.
6. Replay the captured Temporal history without another provider send. History
   must not contain draft/document bodies, provider credentials, encrypted original
   envelopes, scope evidence or publication/gate details.
7. Use actual original-bound HTTP receipt recovery to verify the native commit.
   Status reads cannot checkpoint SQL. Test separately authorized reconciliation,
   reconstructed writer calls and repeated fixed scheduling without another save.
8. Advance only the owned temporary root Brief, then reopen the exact committed
   reference through the real HTTP bundle reader. Verify all three document bytes,
   exact manifest content, source digests and no current-head lookup. Denied reads
   release no document or private diagnostic content.
9. Preserve operation/original/reservation counts and encrypted original rows
   across execution/recovery. A stale pre-save preview must be unavailable after
   native Git advances. No extra model calls or budget reservation may occur.
10. Report focused SQL/SDK/Temporal/native-Git evidence separately from broad
    regressions, full SQL, browser/visual QA and signed-in I1–I6 acceptance.

No production dependency, migration, activation, source-data rewrite or auth
relaxation is requested. D1 records adoption, model budget, live provider/write
authority, gates, deployment and release remain separate prerequisites.
