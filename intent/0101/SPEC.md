# Specification

1. Retain the independent 4,036-ID catalog. Add 14 identifier IDs and 60 non-phone
   detector IDs. The ten existing phone detector IDs remain mapped once.
2. Execute exact declared text and expected class. Phone cases use the corrected
   phone implementation; non-phone cases use the unchanged detector component
   required by the corrected graph. Scope must explicitly exclude full corpus,
   later trust-era or newly corrected non-phone behavior claims.
3. Build the existing valid two-line invoice/usage/ledger set in reconciliation
   mode with complete per-line signed variance/successor records. Independent
   observation must include all 14 records, three observed-as-of time fields and
   the exact correction bytes. Verify 9,800,000 nano-USD, one rounded cent and two
   reconciled lines, with zero effects.
4. Show the legacy scalar graph accepts one pair bound to the first ledger line.
   For missing variance, missing successor and both missing, keep the graph bytes
   unchanged and provide a fresh complete observation of the supplied partial
   inventory. 0057 must return LINEAGE_MULTIPLICITY_INVALID and 0063 must deny.
   No totals may be emitted on these rejected paths.
5. Execute all 32 independent array orderings under the complete 0063 composition.
   Preserve exact record sets and aggregate values. Seal the 40 actual observations
   under the single R5-004 ID; neither baseline nor permutations add IDs.
6. Expose only a closed synthetic case builder with bounded permutation selection,
   not keys, generic signers or arbitrary mutation callbacks. No live credentials,
   provider records or gates are loaded or changed.
7. Save deterministic observations and supplemental source digests. Keep old
   reports intact and complete-coverage mode blocked while IDs remain unmapped.

Two-line execution does not establish all supported line bounds, every monetary
invariant, real billing, independent review or formal R5-004 closure. Pure detector
cases do not substitute for corpus observations. All five formal findings remain open.
