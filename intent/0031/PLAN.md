# Execution route

1. Define the async projection query/port in the existing registry and preserve
   synchronous identity query compatibility and generated transport contracts.
2. Implement the read-only tenant adapter with exact revision and byte checks.
3. Wire explicit services through browser/API composition and optional runtime
   read-model configuration, separate credentials and owned pool cleanup.
4. Exercise denials, revocation during I/O, corrupt/stale rows and pool lifecycle;
   connect synthetic Git ingestion to the real browser/database harness.
5. Run browser, database and root checks; document boundaries; push the verified
   candidate branch. Continue to bounded repository projection reconciliation.

No live provider access, governed write, spending, deployment or gate approval.
