# 0028 · Explicit local HTTPS runtime

Parent M2. Connect the identity runtime and native gateway to an owned loopback
HTTPS listener, with explicit profiles/secrets and coordinated shutdown.
The real browser authentication suite uses this production-source listener.

This is local development composition, not public ingress, real credential
loading, full workspace completion, an independent Exam or gate acceptance.

