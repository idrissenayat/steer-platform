# Evidence

Development verification completed. No gate, live-write or deployment claim.

With Node 24.20.0 and pnpm 11.19.0, `pnpm check` passed kit validation, scope audit,
all typechecks, 88 prototype tests, 438 root controls, 99 registry tests, 24 data,
53 web, 282 adapters, 91 API, 18 worker and 13 domain tests, plus all package builds.
Eligible Turbo tasks were cached. No protected intent/0001 or GitHub policy files
changed. No new package dependencies, runtime grants or database schema were added.

Six focused registry test groups exercise exact source/decision/Brief context,
all grants and strict input, independent curation, unreferenced or changed targets,
missing/stale decisions and evidence, corrupt/foreign/oversized source, and current
grant/identity/expiry denial after reading, including absent results. Three browser
reader groups verify exact nested inputs, response-context and source fingerprints,
rejection of authority flags, null versus denial and closed late responses.

The separate `pnpm test:brief:integration` regression passed all three groups again:
actual HTTP-to-native-Git creation and PostgreSQL projection, lost acknowledgment
without duplicate mutation, and wrong-confirmation/grant/authority denial. Identity
and full gate authority in that creation suite remain explicitly synthetic test
callbacks, not an approval or a live provider result. Owned resources were cleaned.

Both browser runs passed all 42 checks, including actual evidence reads through
native Git, PostgreSQL, Keycloak and the shared API. The final rerun includes an
explicit held-transport cancellation assertion and the final selection visibility guard.
Recorded source references do not admit unconfigured files; changed exact revisions
do not return replacement content. The raw evidence contains test HTML/script/link
text which remains inert. The suite checks focus, mobile/200% wrapping, automated
accessibility, source/decision switching, grant denial, recovery and close/reopen.
Final engine versions were Chromium 151.0.7922.34 and Keycloak 26.7.3. API typechecking
passed again after the final fixture changes; all owned browser, HTTPS, PostgreSQL
and Keycloak resources were cleaned up, leaving user data and services untouched.

Inspected final desktop/mobile screenshots:
`/tmp/steer-0164-browser.MzGQxK/decision-evidence-desktop.png` and
`/tmp/steer-0164-browser.MzGQxK/decision-evidence-mobile.png`. The source panel retains
pink/orange styling, clear unverified labels, visible keyboard focus and responsive
controls. This is scoped automated/visual QA, not qualified human accessibility
review or a live-user demo.

Limits: source bytes and recorded reference linkage are checked, not the truth of
the evidence, qualified review, authentic signatures, gate clearance or permission
to act. Individual exact reads are not a multi-source atomic snapshot or current
Git-head guarantee. The final pending-read cancellation case uses a held synthetic
browser route; ordinary source reads use the actual isolated stack. No real member,
provider credentials, foreground localhost:3000 demo, deployment or live save is
claimed. All five R5 findings, full governed authority, qualified/independent
protected review and human gates remain open.
