# Implementation plan

1. Implement exact portable preparation contracts and the current-human tool command.
2. Compose saved draft reading, full evidence planning, immutable admission and
   encrypted original preservation/readback under current authority.
3. Connect the existing actual repository-wide collector without live installation.
4. Verify contracts and real disposable HTTP/SQL/native-Git integration, including
   acknowledgement loss, concurrent requests, corrections and denied authority.
5. Run full regression, database integration, types/build, migration-history, kit
   and scope checks. Update evidence and current delivery documents; commit, push
   and verify only owned changes. Continue authorized workflow start/recovery and
   actual editor integration, retaining separate live authority and acceptance.

## Next integration boundary

- Build scope start/recovery around the retained review and draft revision; it
  must not admit source, select batches, renew expiry or reserve a model call.
- Verify the fixed Temporal namespace/queue, at least 24-hour history retention,
  exact initial review/preparation input and the existing 30-minute workflow bound.
  A lost scheduling acknowledgement is recovered, not replaced by another run.
- In the actual editor, keep empty, incomplete, prepared, running, uncertain,
  superseded and expired states distinct. Read findings from the authorized SQL
  scope reader; a prepared receipt or completed workflow is not semantic clearance.
- Keep live activation separate from these tests and from UI installation. D1,
  real source/lifecycle/model authority and the first-test budget still need their
  established prerequisites before any real execution or persistence.
