# Development evidence — 2026-09-09

Base: `0fad8f01c6dd38bcaecc0fabb5dd02b99c34d1cc`.

## Implemented

New distinct/linked candidate destination resolution using the actual GitHub reader
interface, immutable Git inventory and exact linked Brief bytes. Independent current
new-item policy evidence is required and rechecked; absent directories cannot grant
save authority. Existing-item/proposal requests fail closed. The compatible port is
exported at the API composition boundary but is not installed in live startup.

## Verification

Native temporary Git tests cover reproducible pure package-preview composition,
linked target fidelity, occupied roots, closed existing-item/amendment scope,
missing grants and policy service, malformed inventory, symlink/executable targets,
forged content, stale review/target commits, mismatched or expired proof, late source
denial, moving head, final identity/expiry and four-slot close/late-I/O behavior.
Provider transport, final reviews, lineage and policy responses are synthetic.
The first linked fixture hashed a non-canonical field order; it was corrected to
hash the parsed contract before the passing focused run. No production validation
was weakened for that correction.

The full regression run passed **1,266/1,266 tests**, including all **nine new
native-Git destination checks**. The prototype and all eight package type checks,
optimized Next 16.3.4 build, 95-artifact kit validation and workflow token-scope
audit pass. `git diff --check` passes and the protected Architecture, Exam and
accepted retention-policy hashes match the prior checkpoint.
All **281 local links across ten changed documents** resolve. User-owned roadmap
and outputs were not modified or staged.
No SQL schema/runtime change is introduced and no new SQL-suite run is claimed.
The focused SQL/recovery evidence remains in [0264](../0264/EVIDENCE.md).

## Not established

This is repository-byte verification plus a mandatory externally verified policy
interface, not implementation or activation of the real governed policy service.
The new resolver is tested with the pure preview planner, not newly claimed as
the encrypted SQL/SDK preview's live installed destination. Existing-item lifecycle,
prior candidate/proposal parents, real records/provider authority and signed-in
I1–I6 acceptance remain incomplete.

No model calls, paid usage, real GitHub runtime writes, deployment, provider grant,
credential changes, user-record deletion or gate signature occurred. The API-key
skill preserves the resolved credential decision and synthetic-only test boundary.
Tests remove only their owned temporary Git object databases. Protected signed
documents and user-owned roadmap/outputs remain untouched.
