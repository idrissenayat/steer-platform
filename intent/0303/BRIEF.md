# Application GitHub corpus batch primitive

Start the coherent application read-graph correction with the production adapter
seam established by [0299–0302](../0298/REQUEST-BUDGET-PLAN.md). The actual GitHub
reader must support bounded immutable multi-object queries, using its own scoped
installation token and exact inventory proofs. No synthetic token or test helper
may enter production imports. Do not switch the existing collectors piecemeal or
claim a full-action speedup; graph/policy/ownership integration remains required.
