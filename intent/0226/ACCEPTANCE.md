# Acceptance boundary

Development acceptance requires the actual production React component graph and
browser transport/controller to preserve exact content and distinguish metadata,
acknowledged draft revision, newer edits, uncertainty and GitHub saving. Synthetic
HTTP responses exercise behavior; they are not current account authority, live
SQL persistence, recorded model generation or end-to-end human UI acceptance.

No private HTML/remote image execution, automatic overwrite, duplicate request,
invented provenance or restored approval is permitted. Automated accessibility
checks exclude visual color/contrast; narrow-screen and real-browser review remain
necessary after authorized activation. The existing design tokens are retained.

The real D1 records amendment, runtime service/keys/grants/migrations and display
switch are not activated. Model usage and runtime GitHub saves remain closed.
Signed documents and the accepted records policy stay byte-identical. I1–I6 is
not marked complete by this increment, and no independent Test Agent exam is claimed.
