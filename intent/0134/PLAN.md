# Plan

1. Add a lazy managed-writer factory to the common save/status definition.
2. Bind HTTP/MCP factories per request and browser/Git-backed factories to fresh
   verified session context, retaining all existing transport checks.
3. Drain writer-enabled service requests before closing shared identity resources.
4. Test lazy allocation, separate instances, cleanup/denial/unknown outcomes,
   signed bearer/cookie context, Git-backed revocation and HTTP/MCP parity.
5. Run full checks and the existing isolated browser authentication regression;
   document exact evidence/limits and publish the candidate.

## Remaining route

The full source/historical human/qualified-hat/provider/gate verifier is still
unfinished. Complete it before selecting a real factory in the explicit runtime.
Existing provider-recorded approvals are not converted or replaced. Then finish
exact confirmation/save/status UI, authenticated board, revision-bound decision
review and the configured end-to-end journey.

Keep all five R5 findings, independent review/protected incorporation, 0124
history/protection, 0120 archival work, model conversation, manual accessibility
and signed release/pilot requirements. No spending or live write is inferred.
