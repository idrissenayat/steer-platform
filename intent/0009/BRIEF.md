# Brief

Build tenant-safe data foundations before attaching provider or UI state.
Prove PostgreSQL itself denies cross-organization reads/writes, including when
pooled connections are reused or transactions fail. Keep Git authoritative.
