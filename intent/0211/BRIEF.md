# Brief

Run an admitted candidate save as durable workflow orchestration without putting
private drafts in workflow history, retrying an uncertain effect or losing the
distinction between workflow completion and a verified saved bundle. Compose the
existing operation owner/writer, retaining required current-authority and original
payload services as explicit inactive dependencies.
