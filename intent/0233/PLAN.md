# Plan

1. Verify complete declared evidence and group whole target context deterministically.
2. Build bounded immutable batch/parent manifests with explicit coverage gaps.
3. Validate combined exact-profile results and citations against recomputed plans;
   keep structural completion distinct from semantic quality and authority.
4. Connect planning metadata to the existing review service, portable verifier and
   actual editor without enabling generation or changing stored original formats.
5. Test count/context limits, canonical/proposed grouping, missing work, stale
   evidence, fabricated citations, transport tampering and actual component/HTTP use.
6. Run regression, disposable PostgreSQL, type/build and repository checks; document
   results, preserve protected hashes, commit/push and verify the remote head.
7. Continue bounded semantic-role execution under existing durable ownership and
   reservation controls, plus real source/lifecycle authority binding. Keep live
   records/model/Git activation separate from synthetic development evidence.
