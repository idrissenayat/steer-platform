# Development acceptance

- Whole configured selection matches the exact scoped current manifest; no fallback.
- Invalid/colliding/oversized/mismatched/stale sources and observer drift deny.
- Existing bounds, source checks, single-flight and cleanup remain intact.
- Held runtime exposes fingerprints only and still cannot authorize or mutate.
- Verification and documentation complete; all five R5 findings remain open.
