# Evidence

## Implemented scope

The held browser journey obtains fresh service tokens from the actual disposable
Keycloak client through the existing pinned HTTPS transport. The normalized OIDC
adapter verifies the current agent, and a separate observer grant is resolved from
current native Git through the read-only App adapter on every authentication.
The direct-principal callback and synthetic identity JWKS port have zero calls in
the successful held collection. Both current actors now use actual local provider
verification; no successful full-authority callback is supplied.

The same browser-confirmed request reaches a matching whole-selection policy
assessment yet remains denied with zero write-token requests, Brief/marker creation
or Git mutation. Runtime reconstruction, not-found status and the submission lock
remain covered. Committed observer revocation, removal of gate.observe and an invalid
token each deny and retain no assessment. A fresh verified hat-free agent is restored
before independently testing committed human revocation.

## Verification — 2026-09-07

- Enhanced actual browser/Keycloak held journey passed, including all three observer
  negatives and the separate human denial. The final Keycloak 26.7.3 / Chromium
  151.0.7922.34 regression passed all 44 checks, including the separate disposable
  creation/Temporal replay/readback journey and current-access/shutdown checks.
- Kit validation, workflow-scope audit, workspace typechecks and 88 prototype tests
  passed. Workspace tests passed: 99 registry, 24 data, 61 web, 289 adapter, 96 API,
  23 worker and 13 domain tests. Eligible unchanged tasks used local Turbo cache.
- Repository controls: all 438 passed. Final prototype and workspace builds passed.
  The `pnpm check` components ran in separate phases to keep final Next.js builds
  outside the active browser journey. No tests were skipped or reclassified.
- Owned Chromium/HTTPS/Temporal services and synthetic PostgreSQL/Keycloak containers,
  data and generated test credentials were cleaned. The enhanced scenario passed
  on its first integration run; no production control was relaxed.

## Limits

Actual disposable authentication is not an approved live observer identity or
qualified historical gate evidence. Historical signers, trust roots, reviews and
selection remain synthetic/unverified real approval. No existing validity interval
was extended. No production limit, live provider grant, protected artifact, dependency,
schema or UI design changed.
All five R5 findings, governed live selection, review provenance, full action-time
authority and human gate signatures remain open. No live save, deployment, release,
spending or real-record deletion is authorized. This integration does not expose a
user-accessible live demo.
