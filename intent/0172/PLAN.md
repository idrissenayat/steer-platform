# Plan

1. Add a test-owned fresh service-token issuer closure to the existing Keycloak harness.
2. Compose the observer from existing OIDC and Git grant components in the held journey.
3. Verify committed revocation, missing grant, invalid token and independent human denial.
4. Run browser, repository/workspace and build regressions; update actual evidence.
5. Commit/push the candidate and verify remote equality and a clean tree.
