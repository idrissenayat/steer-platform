# Implementation plan

1. Verify the published 0051 checkpoint and inspect the existing curated reader.
2. Add the optional metadata capability and strict shared catalog tool with
   exact allowlist mapping, bounded SQL and pre/post current authorization.
3. Test native contract/SQL limits, actual PostgreSQL isolation and corruption,
   and HTTP/MCP/browser catalog discovery leading to exact Brief reads.
4. Run full repository and frozen-install checks, update guides and evidence,
   publish the candidate branch and verify remote commit equality.

Next: trusted workspace repository display binding and rendered Brief catalog/
detail integration. No manual source-path/fingerprint entry becomes an originator
requirement. Keep formal gates and provider/spending boundaries separate.
