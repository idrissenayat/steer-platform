# 0309 — Connect owned scope history to the actual journey

Replace repeated scope-review reconstruction inside the existing authenticated
application with one invocation-owned records/content/SDK read. Preserve current
and historical service contracts, independent authority and exact saved inputs.
Measure actual drafting and confirmation call paths, not a standalone prototype.

No model call, live profile/records activation, runtime GitHub write, signature or
spending authority is added. The fixed delivery tracker remains 17/25 until a full
acceptance checkpoint is verified. This is a scoped correction within C22, not an
architecture replacement or a claim that the intent-capture workflow is complete.
