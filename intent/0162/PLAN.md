# Plan

1. Audit the real first journey: typed UI/receipt readback and projection exist,
   but the positive browser operation is seeded; agent conversation and verified
   lifecycle decisions are still separate missing capabilities.
2. Reuse the native Git store fixture with actual request-bound writer/API preview
   and save composition. Isolate synthetic identity/authority from production paths.
3. Connect recovered actual-created receipts to the existing PostgreSQL runtime.
4. Verify duplicate/unknown/revoked/mismatched confirmation behavior and owned cleanup.
5. Run the explicit integration and full checks, document evidence limits, push.

This is verification of the already implemented authoring-to-save mechanics, not
another production wrapper or a live gate workaround. Model-backed work is not
selected here: the signed architecture requires the configured LiteLLM boundary,
and no provider choice, model credential or paid invocation is authorized by this
test. Five R5 findings, real configuration, complete gate authority, qualified/
independent protected review and human signatures remain outstanding.

## Next bounded capability

Advance the missing review step: implement a read-only, revision-bound decision
record view through the existing curated source/API/reader path, with its backend
contract and visible exact-brief/evidence linkage verified together. Treat recorded
decision text and signer claims as unverified source statements until complete
governed verification exists. Do not enable signing, infer lifecycle transitions,
expose unconfigured records or introduce another policy-only wrapper. Preserve the
signed P1-06 scope and keep any new source/grant configuration test-only by default.
