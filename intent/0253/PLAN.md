# Implementation plan

1. Add strict original-reference and inert outcome contracts.
2. Expose a read-only Git inspector and bind it to encrypted original recovery.
3. Compose the disabled records-backed API factory; register the shared query.
4. Connect canonical recovery links and same-operation recheck to the actual UI,
   with verified receipt-to-exact-bundle links and safe lifecycle clearing.
5. Test native Git/HTTP/MCP, lost acknowledgement with real disposable SQL,
   reconstructed services, denial/tampering and actual React interaction.
6. Verify regression, types, build, docs and protected files; commit/push only
   owned source, then continue preparation/confirmation/start integration.

No live provider access, write, migration or authority activation.
