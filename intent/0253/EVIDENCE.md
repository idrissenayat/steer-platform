# Development evidence — 2026-09-08

Base: `0e1c39fabdd8d427a851f5ffef5ec797396d5742`.

## Capability

`intent.candidate.save.status` restores the immutable original and confirmation,
checks the native Git receipt and yields a metadata-only exact reopen reference
only for a verified commit. The actual signed-in UI preserves unknown/not-found/
conflict distinctions, offers same-operation recheck and connects verified
receipts to the existing exact bundle viewer. No retry or write path is exposed.
See [the guide](../../docs/CANDIDATE-SAVE-RECOVERY.md).

## Completed verification

The final full regression run passes **1,148/1,148 tests**, including native
HTTP/MCP parity, original/owner/revision binding, post-provider records/key denial,
uncertain outcomes, canonical metadata links and the actual React status-to-reopen
flow. All-package typechecks, optimized Next.js 16.3.4 production build, kit checks
and the read-only workflow token-scope audit pass.

The final PostgreSQL 16.14 rerun passes **336/336 checks** with the post-provider
original recheck included. It covers lost native Git acknowledgement recovery
from encrypted originals, reader/API reconstruction, unchanged dispatch
bookkeeping and one original row/one synthetic Git write. The runner removed only
its own synthetic PostgreSQL container and tmpfs data.

All 182 local documentation links across the 12 increment-related documents
resolve. Protected Architecture, canonical Exam and retention-policy hashes are
unchanged. Automated React axe A/AA checks pass with contrast disabled; these do
not establish real-browser visual, semantic-model or signed-in human acceptance.

## Boundaries

Factories remain uninstalled. No live records activation, new grants, real
migration, key inspection, paid model call, runtime Git write, auth bypass, gate,
deployment or release. Synthetic provider/identity/lifecycle authorities are not
live human acceptance. Only test-owned native Git fixtures and disposable SQL/
Temporal state are created and removed; user-owned files remain untouched.

The credential skill preserved the resolved key choice; no repeated key question.
Installed Next.js client-boundary guidance was read under the web AGENTS directive.
Existing pink/orange controls and safe exact-bundle reader are reused. No new
dependency or database migration. Save preparation/confirmation/start, discovery,
quarantine resolution, historical agent recovery, current authority and actual
human acceptance remain open. The one-minute continuation loop stays active.
