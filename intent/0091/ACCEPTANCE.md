# Development acceptance · not a protected EXAM

- Both declared model version pairs run every phase, first/replay and supported
  interruption over actual before/after bytes with all 104 two-row cases.
- Old/new projection and mirrored writes execute for source-only, expanded and
  target-only shapes; null values, other rows and input bytes remain correct.
- Both competing-snapshot winners, stale loser, exact/key-drift replay and retry
  have the expected state/revision and no rejected-write mutation.
- Stale mirrors, logical contract loss and unlisted versions deny even when the
  lower-level signed migration graph passes.
- Every migration/human/action prerequisite, exact clock and source pin remains.
- Context/model/code injection, bad schema/columns/rows and unsupported mapping deny.
- Every row is exercised at 128 rows; UTF-8 and 256-command bounds are enforced.

No production client, live concurrency, durable CAS, deletion or gate acceptance.
