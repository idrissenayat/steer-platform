# Plan

1. Pin an explicit executable dual-column compatibility shim and version manifest.
2. Compose complete 0090 migration verification with both actual data states.
3. Execute every bounded read/write ordering and both competing snapshot outcomes.
4. Check logical preservation, independent rows, replay and exact finite bounds.
5. Run regressions/full checks, document, commit, push and verify remote.

## Next

Bind multiple complete migration steps through exact predecessor after-truth to
successor before-truth, approved ordered batch/checkpoint inventory and unique
action/transaction identities. Exercise interrupted/resumed and replayed steps
without using checkpoint labels as evidence. Keep this separate from live
database execution and continue the remaining source/class/trust-era/normative
inventory. Broader real application/concurrency evidence remains explicitly open.

No gate signature, protected edit, real migration/cleanup, provider mutation,
release, deployment or spending is authorized. The implementation loop continues.
