# 0091 · Executable migration compatibility model

Runs old/new client operations against the actual supplied before/after data of
a fully verified 0090 migration graph. It rejects stale mirrored values and
logical loss on contract that compatibility declarations alone cannot detect.

Read BRIEF, SPEC, PLAN, development ACCEPTANCE and EVIDENCE. This is a bounded
dual-column shim model, not actual application binaries or a live database test.
Every result denies execution; all five formal R5 findings remain open.
