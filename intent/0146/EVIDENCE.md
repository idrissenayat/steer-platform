# Evidence

Development verification on 2026-09-06, Node 24.20.0 / pnpm 11.19.0.

Compatibility tests read both original native Critic JSON files unchanged. Their
expected bindings are selected from those files for structural testing, not from
approved independent provenance. Native Git integration uses synthetic sources and
human signing fixtures and does not claim actual review or target ancestry.

The focused native-history, complete policy-collector and architectural boundary
suites pass 40 groups. The explicit omission counterexample passes the prior
within-record normalizer and is rejected by selected-link verification. A three-
report fixture proves new findings carry forward even after claimed resolution;
presentation reordering does not discard source facts.

An additional isolated sixteen-record boundary probe retains all six inherited
findings across fifteen follow-ups with every HOLD unchanged. The suite also
rejects seventeen records, duplicate selections and oversized individual sources.

The initial typecheck found a test fixture's gate-array inference was too broad
to access the selected native history. The fixture now preserves its tuple type.
The first full run then correctly rejected mutation of that readonly test selection;
negative cases now construct replacement configurations rather than mutating it.
No runtime schema or production check was weakened.

The subsequent full `pnpm check` exits zero: kit/scope audits, prototype typecheck,
88 prototype tests, 437 root assurance controls, all seven package typechecks,
all workspace tests (including 246 adapter tests) and all seven package builds
pass. The prototype production build also passes. Turbo reused unchanged package
results where shown. No browser, real runner or provider verification was performed.

All five R5 findings remain open. No protected records, provider configuration,
runtime keys, frontend, deployment or spending changes. Only owned temporary Git
fixtures are cleaned up. These development results do not close formal findings
or establish a complete, approved history outside the explicitly selected records.
