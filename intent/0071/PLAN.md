# Plan

1. Bind the source-policy ordering map in a pure exact-instant comparator.
2. Integrate pairwise order and canonical UUID replay identity in 0059.
3. Test all 200 ranked type/UUID pairings and signature/schema/replay failures.
4. Test complete lifecycle hold behavior at equal instants and retained limits.
5. Run full checks, update docs and verify the development-branch push.

Next remains complete retention/rotation/reference and raw-grant coverage,
auxiliary event-time semantics, other precision paths and migration/normative
evidence. Do not substitute another broad review loop for completing the package.
