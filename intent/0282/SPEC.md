# Intent 0282 specification

1. Add first-amendment to the existing joined signed-identity/21-service fixture.
   Preserve all 34 scope sources, two recorded scope reviews, separate Architect
   and Test Agent roles, human Brief correction, six model reservations, four
   fixed Temporal workflows, lost acknowledgements and runtime reconstruction.
2. Select exactly one canonical root Brief for the existing item at the reviewed
   commit and digest. Reject missing/ambiguous/versioned/noncanonical targets;
   never fall back to candidate revision, new-linked or new-distinct. Both drafting
   roles receive the exact human choice. Source status alone is not lifecycle
   authority; the existing independent governed proposal-only policy remains required.
3. Seed a canonical Exam and hidden unrelated file only in this disposable scenario,
   before identity/corpus snapshots. Never expose the canonical Exam to either
   drafting role. Keep the usual 34 Brief/Spec scope sources and their boundaries.
4. Reject the old scope assessment after the human correction; reassess the exact
   final content. Preview must produce amendment purpose, a reproducible first
   proposal ID, the same item's reviewed target commit, and no prior bundle,
   relationship or proposal parent. Current source denial and a wrong target digest
   block without another original, reservation or Git effect.
5. Confirmation/recovery retain one immutable original. One fixed save writes six
   create-only files: three candidate documents, their manifest, the new proposal
   pointer and exact operation receipt. No prior source path, root Brief/Spec/Exam
   or CANDIDATE.json may be written. Check every existing blob/mode/OID after save,
   plus the exact set of new item files and proposal pointer's parent/target binding.
6. Lost HTTP/scheduler/provider acknowledgements, replay and factory restart retain
   one candidate-save commit and six reservations. Reopen the exact saved proposal
   after a later branch edit without rebasing its reviewed target. Read-policy and
   Git-grant loss deny; no additional original, scheduling attempt or commit.
7. Add an explicit focused selector and include the case in the full SQL runner.
   Run choice/selector tests, first-amendment and default new-distinct joined
   regressions, broad tests, typechecks, build, links and protected hashes. Distinguish
   focused from full suites; do not present prior revision/linked runs as new runs.
8. No production authority/configuration/credentials, paid models, live runtime Git
   writes/grants, records/D1 adoption, migration, gate, deployment, release or user-data
   deletion. C09 earns four points only after verification. Actual UI/model quality,
   continuation, performance and governed live activation remain open.
