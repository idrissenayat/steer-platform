# Plan

1. Define a non-erasure qualified schema without rewriting signed source files.
2. Share the complete human proof body under an explicit trusted profile/policy.
3. Require exact one-record selectors, active-hold release and current freshness.
4. Test profile isolation, all proofs, binding changes, keys, clocks and expiry.
5. Run checks, document evidence, commit and remote-verify.

## Next

Compose the qualified profile with actual current hold/release events in the
mixed lifecycle history. Bind actor/hat, exact event payload, selector inventory,
prior hold, decision/reservation-before-event chronology and unique proof IDs.
Keep historical hold releases without full qualified archival evidence closed
instead of inventing retroactive authority. Preserve the separate later disposition
approval; a hold decision is never erase permission.

Then add exact reference inventory/revocation and retained verification bundles,
remaining class boundaries, auxiliary-time and migration/crash-cut coverage, and
the normative inventory before independent/protected review. No signature,
provider mutation, deletion, release, deployment or spending is authorized.

