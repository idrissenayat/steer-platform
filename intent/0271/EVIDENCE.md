# Development evidence — 2026-09-09

Base: `326e61b82739c31b8ccc93434c329bb82a3247bf`.

## Implemented

The production identity root accepts an explicit strict journey profile paired
with an owned factory and separate activation/use policy service. It validates
all 21 service entries plus internal publication recording, pins scope/methods,
preserves the existing HTTP/MCP authorization path and rejects unrecorded-agent
ambiguity. No real profile is configured or installed.

A shared managed-call boundary checks current policy, retains four admissions
through dependency drain and withholds late results. Identity shutdown now drains
newer browser-only intent services and pending managed work before disposing the
journey and closing shared identity/read-model pools. Failure stays failed/closed
while other owned resources still close. Publication remains an internal action.

## Verification

The first focused boundary run passed 26 tests. Type checking caught an explicit
test-inventory cast that did not preserve the mapped service keys; the fixture
cast was corrected, without relaxing production types or validation. Data/API
type checks then pass. The final HTTP/MCP and scope-hardening recheck passed all
29 related tests, including eight new manager tests and seven new runtime tests.

The initial, scope-hardened and final confirmation `--journey-runtime` runs each pass their
joined check plus idempotent migration verification. Actual signed synthetic
human JWTs and current native Git grants reach encrypted PostgreSQL draft create,
append and read through `createIdentityRuntime`. A lost append acknowledgement
keeps one encrypted revision; runtime reconstruction recovers the same mutation
and exact original/clarification/Brief/Spec/Exam bytes. Wrong products, current policy
denial, removed grants, inactive identity and a durable hold deny without changing
the encrypted revision. No Git mutation or publication-recording effect occurs.
The bearer path opens no browser-session database connection. Both runtime owners
close once. Each run removes only its owned synthetic SQL container/tmpfs and Git
fixture. This is focused integration, not the full SQL suite or a cookie-login UI.

The final broad regression command passes 1,320 tests with zero failures, skips or
cancellations (142,198 ms). Prototype and all eight package type checks pass, as
does the optimized Next production build. Kit validation passes all 95 required
artifacts and the workflow token-scope audit passes with read-only contents scope.
Protected Architecture, canonical Exam and accepted retention-candidate SHA-256
hashes remain respectively `9e1783a5f9870e8a8a2595d23226efa804902b4c472e309bf9f924d8cf61dc65`,
`84ad1d4c14d6614fe4b53509104e379e9ad6dff0462c96109901eefb1e02fd7f` and
`f8a9cb9acc90e2943181be428cb03bebcce64758a3ac19bf1243e3bbe3894e32`.
The final focused SQL confirmation again passes its one joined check plus
idempotent migration check; the full SQL suite was not run. The final related-test
confirmation passes all 29 tests (5,537 ms). All 334 local Markdown links in the
ten changed/new documentation files resolve, and `git diff --check` passes.

## Limits and next work

The factory boundary is implemented, not the complete construction of every
service. Only drafts use real SQL in the new authenticated integration; unexercised
inventory capabilities throw rather than simulate completion. Signed test JWTs,
Git grant fixtures and records-policy callbacks are synthetic. No actual identity
provider, model, saved-repository human acceptance or new UI result is claimed.

Next construct the remaining concrete owned services and extend the recorded
scope/drafting/preview/save/reopen journey through this identity root. Independently
adopted clock provenance, constrained late publication recovery, D1 records policy,
model spending, runtime provider/write authority and I1–I6 acceptance remain open.
Protected signed sources, user-owned roadmap/outputs, credentials and real runtime
configuration remain unchanged. No deployment, deletion, release or gate occurs.
