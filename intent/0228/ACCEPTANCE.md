# Acceptance boundary

Development acceptance requires actual shared HTTP, encrypted PostgreSQL and
Temporal composition to recover one exact workflow after a lost acknowledgement.
The worker must produce only the existing two reserved recorded role steps with
synthetic transport; foreign same-ID input must not be acknowledged or replaced.
Missing grants, current-authority denial, holds, stale revisions and late permission
loss must withhold dispatch or acknowledgements as appropriate.

The tests use disposable services and synthetic identities, records grants, budget
authority and provider responses. They are not real UI acceptance, provider/model
quality evidence, independent Test Agent acceptance, paid usage or a saved Git bundle.
Source admission is already prepared by the integration fixture; the editor does
not yet assemble/admit and submit it through this command.

The original signed sources and accepted records policy remain byte-identical. D1
stays unsigned/inactive; the real migration baseline and runtime remain held. Live
I1–I6 acceptance remains open. No deployment, release or deletion is authorized.
