# Plan

1. Introduce explicit staged schema/compatibility profiles without legacy changes.
2. Bind an approved ordered chain to full evidence and exact before/after bytes.
3. Enforce coverage, chronology and immutable identity ownership through replay.
4. Exercise complete, pending, interrupted/restored, retry and adversarial chains.
5. Run full checks; document, commit, push and verify remote publication.

## Next

Add durable checkpoint/current-head/consumption evidence around the verified
chain, including acknowledgment loss and restart readback, before claiming a
resumable runtime. Exhaustively identify remaining provider crash cuts rather
than treating a supplied terminal graph as transport proof. Preserve separate
real application/database integration and remaining normative/source/class/trust
coverage. No production adapter or executor imports these offline candidates.
Checkpoint-only observations must not be represented as a second backfill effect
over the same rows; distinguish durable observation from another migration step.

All five R5 findings remain open. No protected edit, signature, provider mutation,
real migration/cleanup, release, deployment or spending is authorized.
