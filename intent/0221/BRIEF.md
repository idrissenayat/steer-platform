# Brief

Connect exact role-request preparation, atomic cost reservation, acknowledged
dispatch and immutable result checkpoints. A recovered worker should reuse a
completed Architect or Test Agent result, never automatically repeat an uncertain
model call or overwrite newer human corrections.

This is an independent orchestration step toward the real intent journey, not
activation of model use, private records, Temporal activities or the editor.
