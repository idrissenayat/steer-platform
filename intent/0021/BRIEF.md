# Intent

Prevent oversized or stalled requests and excessive concurrent authentication
from consuming unbounded local API resources. Preserve all current identity,
Git authority and session checks. Keep deployment and real access closed.
