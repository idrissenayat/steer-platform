# Brief

Finish the declared public-signing boundary mappings without introducing another
signer, exporting fixture keys or implying provider access. The public record
signer must work normally but refuse each declared independent/private domain.

Preserve source fidelity and narrow claims. This is execution of a retained API
restriction, not proof that keys cannot be stolen or signatures cryptanalyzed.
Keep schema and streamed accessibility mappings separate until their actual
source/version and consumption evidence can be represented truthfully.
