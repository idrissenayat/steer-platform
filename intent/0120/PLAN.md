# Plan

1. Add a separate non-erasure retirement authority schema and closed human profile.
2. Compose complete human/event proof with exact corpus/parent/event/predecessor
   and signed policy-bound complete history head, enforcing pre-commit ordering.
3. Exercise mandatory proofs, authentic semantic faults, truthful holds, replay/CAS,
   time precision, schema/profile isolation and original/current key boundaries.
4. Preserve every prior mapped observation, refresh source-fingerprint reports,
   run focused/repository/separate full checks and publish the verified candidate.

## Next: preserve qualified retirement facts across trust eras

Add a distinct archived retirement verifier that pins exact trusted decision context,
original serialized decision evidence and original observation, including the full
nine-record bundle and event/history-head binding. Re-run the complete 0120 decision
at that original observation, preserve all original anchors/windows, check known
current revocations and require fresh independent current revalidation/retained-byte
witnesses. Never relabel an old ALLOW or a digest-only reference as current validity.
Bind archive records to the exact parent retirement event/config and predecessor
history used by 0119; retain bytes before the current manifest/history head.

Then compose qualified retirement archive, full qualified parent history and 0119
manifest closure into dedicated future readiness/full lifecycle profiles. Keep
current parent copies, separate actions/aggregate/tombstone and hold/reference
requirements intact. Preserve the dependency-cycle warning in 0119/PLAN.md; use
clean leaf/shared-core or external composition rather than weakening checks.
Only after complete composition may the four provenance source coordinates map.

All five formal R5 findings, twelve lifecycle coordinates, 3,614 migration and one
schema coordinate, normative/live integration and independent/protected review stay
open. No user-only blocker exists for the next local archive work. Provider access,
actual retirement/deletion, gate signatures, release, deployment and spending remain
separately gated. Signed sources and protected Exam artifacts are unchanged.
