# Development evidence — 2026-09-09

Base: `b0449a24f3a5b71e976d8e67c39ef580df6461b7`.

## Implemented

Explicit repository-backed existing-item destinations support exact pre-pull
candidate revisions and first canonical/in-flight amendment proposals under a
mandatory independently verified lifecycle service. A shared router composes new
and existing directions without fallback. API exports remain uninstalled.

The immutable-target/current-review mismatch for existing proposal correction is
documented with a native A-to-B commit fixture. That direction stays explicitly
unavailable, not silently rebased or declared complete.

## Verification

All **1,280 regression tests pass**, including **14 new native-Git destination
and routing checks**. Positive checks cover reproducible candidate parent/relationship
preservation and first amendment planning with no canonical replacement. Negative
checks cover strict direction/configuration, actual older proposal targets, corrupt
pointer/manifest/documents, changed root Brief mirror, invalid modes/topology,
forged bytes, missing grants/policy, binding changes, expiry, revoked sources/human,
branch movement and bounded close/late inventory. Pure preview composition is
exercised with synthetic final-review and generation-lineage bindings.

The prototype and all eight package type checks, optimized Next 16.3.4 production
build, 95-artifact kit validation and workflow token-scope audit pass.
`git diff --check` passes. All **301 local links across 12 changed documents**
resolve. Protected Architecture, Exam and accepted retention-policy SHA-256 values
match the previous checkpoint. User-owned roadmap and outputs are untouched.

Initial test setup incorrectly passed bundle planner write modes (`create`) as
native Git file modes; the fixture now maps only path/content. Focused testing
also found the existing resolver passed extra configuration fields into the strict
bundle reader; its adapter now supplies only that reader's declared scope. Neither
strict schemas nor production authority validation were weakened.

No SQL change is introduced and no new SQL suite run is claimed.

## Not established

Native repository bytes and pure package planning use synthetic provider transport,
policy, final-review and lineage fixtures. This does not establish governed policy
service implementation, encrypted SQL/SDK destination installation, real records
adoption, existing-proposal correction or signed-in I1–I6 acceptance.

No model calls/spending, runtime GitHub writes, provider grants, deployment, release,
credential changes, real-record deletion or gate signature are authorized by this
increment. The API-key skill preserves the resolved credential decision and local
synthetic-only test boundary. Tests clean up only owned temporary Git fixtures.
