# Execution route

1. Introduce the bounded, canonical-origin production gateway without new dependencies.
2. Replace the test-only root/static proxy with the shared factory.
3. Test credential isolation, routing, security headers, rejection and resource limits.
4. Exercise real renderer transport failures and the complete native browser flow.
5. Run root checks, record evidence and commit/push this development increment.

Next: trusted local listener/profile composition and authenticated workspace/session
UI. Keep actual membership, secret handling, ingress, remaining stack services
and formal gate evidence separately visible.

