# 0027 · Native identity gateway

Parent M2. Promote the native UI/API routing policy from the browser fixture into
production application source. Use the same gateway in the actual Next.js,
Keycloak, Git-membership and encrypted-session browser checks.

This is a bounded development increment, not a public listener, deployment,
complete workspace, independent Exam or gate approval.

