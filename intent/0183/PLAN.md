# Plan

1. Export/reuse the portable closed recovery plan schema, add an exact optional
   runtime profile/factory, and integrate ownership and service registration.
2. Include recovery-only work in request draining, retaining all-settled cleanup.
3. Test pairing, malformed/foreign factory bindings, current signed identity and
   native Git grants, pre/post-shutdown behavior, failed allocation and close errors.
4. Extend actual local Temporal/Git/PostgreSQL integration through authenticated
   runtime dispatch and reconstruction, using distinct synthetic recovery/projector
   identities and explicitly limited provenance.
5. Run full checks/builds, record exact evidence, update current docs, push and
   verify remote equality with a clean tree. Next bind an actual disposable Keycloak
   recovery service identity into this same fixed-failed-run journey; live configuration
   and complete governed acceptance remain independently gated.
