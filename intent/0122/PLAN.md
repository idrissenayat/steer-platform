# Plan

1. Reuse portable preview contracts and validate scope/content in a bounded client.
2. Connect a one-question interview, rendered preview and correction transcript to
   the authenticated production page; preserve tokens and no-provider boundaries.
3. Exercise client identity/digest/shape/closure and answer-conversion negatives.
4. Verify actual Next.js/Chromium/Keycloak/encrypted-session/local-Git composition,
   corrections, revocation, expiry, no browser storage, keyboard and mobile/axe.
5. Inspect screenshots, run repository checks, record limits and publish candidate.

Next: implement the exact-content confirmation contract and disabled-by-default
save/idempotency/CAS orchestration, before any write adapter activation. A preview
hash or browser click cannot be treated as a signature or gate approval. Continue
tracking model-backed/scoped-agent conversation and trusted system context as
unfinished authoring requirements. Use `docs/FIRST-USABLE-DELIVERY.md` for the
bounded journey sequence and separate gate/provider dependencies. All R5 findings
and the archival/provenance backlog remain open.
