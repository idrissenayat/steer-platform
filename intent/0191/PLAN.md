# Plan

1. Expose the schema-only contract and test its browser import boundary.
2. Build the pinned read controller and source-coverage cards using existing style
   tokens and current authorization; mount in both existing review entry points.
3. Add unit negatives for scope/authority injection, stale data, cancellation,
   invalid clocks, expiry and no automatic calls.
4. Extend only disposable integration fixtures; test the actual authenticated UI,
   source fingerprints, revocation and keyboard/layout/accessibility behavior.
5. Run full repository checks and final build after browser shutdown. Update guides,
   finite delivery ledger and numbered verification without changing signed scope.
6. Commit owned files, push the candidate branch and verify remote equality.

Preserve unrelated `docs/REAL-USER-ROADMAP.md` and `outputs/`. Do not alter protected
canon, human signatures, live configuration or spending/deployment boundaries.
