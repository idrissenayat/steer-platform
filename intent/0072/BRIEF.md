# Brief: Three-key raw lifecycle coverage

The frozen review amendment's raw evidence scenario requires at least two copies
and three raw keys. The bounded composed fixture previously exercised two keys.
Extend it to three independently identified key/object tuples across two provider
bindings and verify every actual copy/action/receipt path, without changing
frozen fixtures, production storage or the disposition verifier's semantics.

The purpose is coverage of an existing requirement, not more live data copies or
a claim that full raw-policy acceptance is complete. The policy's pre-terminal
grant requirement remains explicit and separate.
