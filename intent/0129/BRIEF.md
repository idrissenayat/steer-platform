# Exact gate chronology

Before connecting gate policy to the pending trusted writer, fix a reproduced
precision defect in its production normalized-fact evaluator. JavaScript
millisecond parsing collapses distinct sub-millisecond signature, authentication,
prerequisite and Critic timestamps, causing both false acceptance and rejection.

Use exact UTC ordering without changing signed artifacts, inventing provider
proof, enabling a writer or claiming closure of the existing R5 timing finding.
