# Intent 0281 execution evidence

Status: verified synthetic integration and regression; live acceptance remains open.
Base: `ff01c9c0784fd6f600a1f78806a1d53ba5e7171d` (0280).

The joined test path now supports explicit new-linked work targeting an
existing versioned candidate Brief while writing a separate new item. Production
source and authority remain unchanged. Exact choice/target checks, source-denial
negatives, create-only plans, full linked-item preservation and pinned-relationship
reopen assertions are added.

All 15 focused selector/choice/diagnostic/native-destination tests pass, including
the new explicit missing-linked-target allowlist rejection. Prototype/all eight
package typechecks and the optimized Next.js 16.3.4 build pass. Source inspection
confirmed the native linked destination
requires the target in its governed item allowlist; the synthetic profile now
explicitly includes both the new item and that target. No production authority
check was removed, expanded or replaced.

The `--journey-runtime-linked` selection passes its one joined authenticated check
plus idempotent migrations. The same 34-source journey carries the explicit
new-linked choice through both scope workflows, separate recorded drafting roles,
human Brief correction, reassessment, exact confirmation and fixed save/reopen.
Current source denial and wrong digest reject. Lost HTTP/scheduler/provider replies,
replay and reconstructed identity/factory retain one original, six synthetic model
calls/reservations and one native candidate-save commit.

The complete linked-item tree inventory is unchanged after saving the new item.
The plan uses create-only files under the new root or its exact operation receipt,
with no root canonical Spec/Exam. Exact older-commit reopen preserves all documents
and the original reviewed relationship after a later branch edit. Current policy
and Git-grant revocation deny. Only this run's disposable PostgreSQL container and
tmpfs were removed. The focused result is not the full SQL suite or live UI evidence.

Synthetic preview makes 30,292 requests in 19,359 ms. First confirmation with a
discarded reply makes 61,016 requests in 40,446 ms; reconstruction/repetition make
60,954 / 40,208 ms and 60,952 / 39,624 ms respectively. This is not an improvement
or acceptable live-provider performance claim.

The default `--journey-runtime` regression passes all three joined checks plus
idempotent migrations after the shared test-helper changes. It retains six synthetic
model reservations and one candidate-save commit/exact reopen, with unchanged
0279/0280 request totals. Its disposable container/tmpfs were cleaned up separately.
The `--journey-runtime-revision` selection was not rerun in this increment; its
prior joined result is recorded in 0280. Neither selection here is the full SQL
suite. Final broad regression passes **1,371/1,371**, with zero failures,
cancellations or skips (148,761 ms; concurrency four, run separately from heavy
SQL work). The 15 focused tests overlap that count; they are not additional tests.
Final prototype/eight-package typechecks, optimized production build, 95-artifact
kit, token-scope audit and whitespace checks pass.
All 272 local Markdown links in the seven changed/new documents resolve. The fixed
25-row checklist independently recounts to 15 verified and ten pending (60%).

Completed C08 advances the fixed tracker from 56% (14/25) to **60% (15/25;
+4 percentage points)**. No test-count, documentation or time-based points are
added. Next: C09, authenticated first-amendment save/reopen with canonical-source
preservation. The remaining ten checkpoints include continuation, performance,
governed activation, real model quality and actual signed-in human acceptance.

## Boundaries

No production source, dependency or migration changes. The credential skill kept
the resolved key decision unchanged; no key was inspected, recreated or exposed.
No paid model call, real runtime GitHub write/grant, records/D1 adoption, live
migration, auth bypass, gate signature, deployment, release or user-data deletion.
The protected Architecture, canonical Exam and accepted retention-policy SHA-256
values remain unchanged. User-owned `docs/REAL-USER-ROADMAP.md` and `outputs/` are
untouched and excluded. The existing one-minute loop remains active.
