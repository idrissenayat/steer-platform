# Intent 0281 specification

1. Extend the existing joined signed-identity/21-service fixture with an explicit
   new-linked direction. Keep all 34 native sources, both recorded scope reviews,
   separate Architect/Test Agent roles, corrected Brief, six model reservations,
   four fixed Temporal workflows, lost acknowledgements and restarts.
2. Select exactly one reviewed versioned pre-pull candidate Brief. Bind its path,
   commit and digest without substituting its root mirror or silently falling back
   to new-distinct. The new item root differs from the target. Both drafting roles
   receive the exact explicit linked choice. The synthetic governed profile must
   explicitly allow both the new destination and linked source; source discovery
   does not confer that permission.
3. Reject the stale pre-correction assessment and reassess the exact final content.
   Preview must resolve new-candidate with the correct source relationship and no
   prior bundle/amendment. Source denial and a wrong target digest must block with
   no extra original, model reservation or Git effect.
4. Exact confirmation survives a discarded reply and reconstruction as one immutable
   encrypted original. Denied start, lost scheduler/provider replies, replay and
   restart retain one fixed save, six model reservations and one native Git commit.
5. Every planned write is create-only under the new root or the exact operation
   receipt path. Never create root canonical Spec/Exam. Compare the linked item's
   complete tree inventory before and after the save: all old files and prior
   bundles remain identical. The saved manifest's relationship remains pinned to
   the reviewed pre-save source commit, not the new save or a later head.
6. Reopen the exact saved bundle after a later branch edit and verify documents,
   manifest and relationship. Current read-policy and Git-grant revocation deny
   without another original, reservation, scheduling attempt or commit.
7. Add an explicit focused selector and include the linked case in the full SQL
   runner. Run selector/choice tests, the linked journey and default new-distinct
   regression, broad tests, typechecks and build. Report only suites actually run;
   previous revision verification must not be presented as a new run.
8. Preserve protected and user-owned documents, credentials, auth and live config.
   No paid models, real runtime GitHub writes/grants, records/D1 adoption, migration,
   gate, deployment, release or user-data deletion. C08 earns four progress points
   only after verification; real-model quality and UI acceptance remain pending.
