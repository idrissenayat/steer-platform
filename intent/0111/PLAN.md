# Plan

1. Reconcile five existing current profiles with the source's exact historical
   trigger, boundary and observation instants.
2. Add a closed adapter and ten complete-control hooks, preserving original keys
   and fresh qualified archive/state/current-action evidence.
3. Verify exact source times, key-domain lifetimes, retained bytes, witness freshness,
   current receipts, reference/tombstone requirements and safe missing-evidence outcomes.
4. Save fresh quick/full reports, run focused/repository/separate full integration,
   update docs, commit/push the candidate and verify remote revision and clean state.

## Next

Forty-six lifecycle coordinates remain: eighteen at/after pending-state coordinates
for the nine mapped classes, plus twenty-eight coordinates across seven other classes.
The unmapped classes are authoritative artifacts, release/migration records,
rebuildable projections, deletion evidence, raw working corpus, corpus provenance
and sanitized corpus. Raw is a 60-second completion deadline, not a minimum wait.
Provenance/sanitized history and release/deletion event semantics must stay source-faithful.

Implement explicit pending-disposition evidence semantics without confusing an invalid
or absent action graph with a valid pending state, and without granting execution.
Preserve original profiles and avoid accepting a synthetic end-state receipt as a
real provider effect. Current runtime admission for unsupported classes needs actual
implementation and full controls, not aliasing an already supported class.

A bounded next implementation is a separate read-only eligibility result after
verified event/history, inventory and current state: waiting versus eligible but
pending complete disposition evidence. It must always report zero effects and
executionAuthorized=false, never claim actual quarantine/deletion, and never
weaken the complete action/human/provider/tombstone path. Missing or invalid head
evidence must still deny. A future trigger supplied before its actual occurrence
must not count as a valid observed event; reconcile such source cases explicitly.

Start that readiness path with only the nine classes already mapped in 0110/0111,
then exercise their exact source at/+1-second observations. Use fresh, available
head evidence rather than embedding future receipts. Keep head-only input and
readiness output distinct from a complete disposition graph: the same missing
effect evidence must not pass the complete verifier. Include positive/replay full
controls and altered history, state, inventory, target and clock rejections.

3,614 migration cases and one obsolete schema case remain unmapped. GAP-01–06 and
M1–M8 are separate remaining work; all five formal findings stay open. No protected
edit, real provider/deletion action, deployment, release, spending or signature is authorized.
