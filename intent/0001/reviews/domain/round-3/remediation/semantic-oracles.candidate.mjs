import fs from 'node:fs';
import path from 'node:path';
import { createHash } from 'node:crypto';
import { fileURLToPath } from 'node:url';
import { compile202012, parseStrict } from '../../round-2/remediation/schema-validator.candidate.mjs';
import { compileOffline } from './offline-schema-registry.candidate.mjs';
import {
  TARGET_REVISION,TARGET_EXAM_SHA,H40,H64,sha256,clone,zeroEffects,deny,blocked,jcs,
  exactKeys,nonempty,hex,strictTime,parseCanonical,TRUST_REGISTRY,TRUST_REGISTRY_DIGEST,
  TRUST_ANCHORS,sealRecord,verifyRecord,parseSigned,checkedAdd,checkedMultiply,MAX_NANOUSD,
  AUTHORIZATION_POLICY_PATH,AUTHORIZATION_POLICY_BYTES,AUTHORIZATION_POLICY_SHA,
  RETENTION_POLICY_PATH,RETENTION_POLICY_BYTES,RETENTION_POLICY_SHA
} from './strict-evidence.candidate.mjs';

export {TARGET_REVISION,TARGET_EXAM_SHA,H40,H64,sha256,clone,zeroEffects,jcs,TRUST_ANCHORS,sealRecord,verifyRecord,checkedAdd,checkedMultiply,MAX_NANOUSD};
const dir=path.dirname(fileURLToPath(import.meta.url));
const readExact=name=>fs.readFileSync(path.join(dir,name),'utf8').trimEnd();
const exactArtifact=(name,bytes)=>{const expected=readExact(name);if(bytes!==expected)throw new Error(`${name}_BYTES_MISMATCH`);return parseStrict(bytes);};
const compiledSchema=name=>compile202012(parseStrict(fs.readFileSync(path.join(dir,'schemas',name),'utf8')));
const privacyEvidenceSchema=compiledSchema('PRIVACY-EVIDENCE.schema.json');
const accessibilityManifestSchema=compiledSchema('ACCESSIBILITY-MANIFEST.schema.json');
const accessibilityRowSchema=compiledSchema('ACCESSIBILITY-ROW.schema.json');
const accessibilitySummarySchema=compiledSchema('ACCESSIBILITY-SUMMARY.schema.json');

const bundleFields=['manifestBytes','trustRegistryBytes','authorizationPolicyBytes','requestBytes','upstreamCredentialBytes','downstreamCredentialBytes','delegationBytes','assignmentBytes','authorityBytes','providerResourcesBytes','replayLedgerBytes','casHeadBytes','reservationBytes'];
const requestFields=['requestId','action','organization','tenant','repositoryId','installationId','item','path','targetExamRevision','targetExamSha256','authorizationPolicyPath','authorizationPolicyDigest','actorSubject','actorRole','principal','provider','upstreamPrincipal','upstreamCredentialId','upstreamCredentialDigest','delegationDigest','assignmentDigest','authorityDigest','immutableRequestDigest','idempotencyKey','casHead','reservationDigest','requestedAt'];
const credentialFields=['credentialId','principal','subject','provider','action','issuedAt','expiresAt','lastUsedAt','oneUse','selectorsDigest','targetExamRevision','targetExamSha256','authorizationPolicyDigest'];
export function authorizationDecision(input){
  try{
    const bundle=typeof input==='string'?parseCanonical(input):input;
    if(!exactKeys(bundle,bundleFields)||bundleFields.some(key=>typeof bundle[key]!=='string'))return deny('PROTECTED_ACTION_BUNDLE_SCHEMA_INVALID');
    const manifest=exactArtifact('PERMISSIONS-MANIFEST.candidate.json',bundle.manifestBytes);
    if(manifest.version!=='steer-permissions-manifest/v12'||manifest.denyByDefault!==true||manifest.trustRegistryDigest!==TRUST_REGISTRY_DIGEST||manifest.authorizationPolicyPath!==AUTHORIZATION_POLICY_PATH||manifest.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||manifest.targetExamRevision!==TARGET_REVISION)return deny('PERMISSION_MANIFEST_INVALID');
    if(bundle.authorizationPolicyBytes!==AUTHORIZATION_POLICY_BYTES||sha256(bundle.authorizationPolicyBytes)!==AUTHORIZATION_POLICY_SHA)return deny('AUTHORIZATION_POLICY_BYTES_MISMATCH');
    if(jcs(parseCanonical(bundle.trustRegistryBytes))!==jcs(TRUST_REGISTRY)||sha256(bundle.trustRegistryBytes)!==manifest.trustRegistryDigest)return deny('TRUST_REGISTRY_MISMATCH');
    const request=parseSigned(bundle.requestBytes),actions=manifest.actions.filter(action=>action.action===request.action);
    if(actions.length!==1)return deny(actions.length?'ACTION_DUPLICATED':'ACTION_UNLISTED');const action=actions[0];
    if(!exactKeys(request,[...requestFields,'recordDigest','signature']))return deny('REQUEST_SCHEMA_INVALID');
    const expected={principal:action.executingPrincipal,upstreamPrincipal:action.upstreamPrincipal,actorRole:action.requiredActorRole,provider:manifest.provider,path:action.exactPath,targetExamRevision:TARGET_REVISION,targetExamSha256:manifest.targetExamSha256,authorizationPolicyPath:AUTHORIZATION_POLICY_PATH,authorizationPolicyDigest:AUTHORIZATION_POLICY_SHA};
    for(const [key,value] of Object.entries(expected))if(request[key]!==value)return deny(`${key.toUpperCase()}_MISMATCH`);
    for(const selector of manifest.requiredSelectors)if(!Object.hasOwn(request,selector)||request[selector]===null||request[selector]==='')return deny(`SELECTOR_MISSING_${selector}`);
    const upstream=parseSigned(bundle.upstreamCredentialBytes,'upstream'),downstream=parseSigned(bundle.downstreamCredentialBytes,'downstream');
    for(const [credential,label] of [[upstream,'UPSTREAM'],[downstream,'DOWNSTREAM']]){
      if(!exactKeys(credential,[...credentialFields,'recordDigest','signature']))return deny(`${label}_CREDENTIAL_SCHEMA_INVALID`);
      const issued=strictTime(credential.issuedAt),expires=strictTime(credential.expiresAt),used=strictTime(credential.lastUsedAt),requested=strictTime(request.requestedAt);
      if([issued,expires,used,requested].includes(null)||!(issued<=used&&used<=requested&&requested<expires)||expires-issued>manifest.maxCredentialLifetimeSeconds*1000)return deny(`${label}_CREDENTIAL_TIME_INVALID`);
      if(credential.oneUse!==true||credential.targetExamRevision!==TARGET_REVISION||credential.targetExamSha256!==TARGET_EXAM_SHA||credential.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA)return deny(`${label}_CREDENTIAL_POLICY_TARGET_MISMATCH`);
      const selectorDigest=sha256(jcs(Object.fromEntries(action.selectors.map(key=>[key,request[key]]))));
      if(credential.selectorsDigest!==selectorDigest)return deny(`${label}_CREDENTIAL_SELECTOR_MISMATCH`);
    }
    if(upstream.credentialId!==request.upstreamCredentialId||upstream.recordDigest!==request.upstreamCredentialDigest||upstream.principal!==action.upstreamPrincipal||upstream.action!==action.requiredUpstreamAction)return deny('UPSTREAM_CREDENTIAL_MISMATCH');
    if(downstream.principal!==action.executingPrincipal||downstream.subject!==request.actorSubject||downstream.action!==request.action||downstream.provider!==request.provider)return deny('DOWNSTREAM_CREDENTIAL_MISMATCH');
    const delegation=parseSigned(bundle.delegationBytes,'delegation'),assignment=parseSigned(bundle.assignmentBytes,'assignment'),authority=parseSigned(bundle.authorityBytes,'authority'),resources=parseSigned(bundle.providerResourcesBytes,'provider'),replay=parseSigned(bundle.replayLedgerBytes,'replay-authority'),head=parseSigned(bundle.casHeadBytes,'cas-authority'),reservation=parseSigned(bundle.reservationBytes,'cas-authority');
    const requestedAt=strictTime(request.requestedAt),delegatedAt=strictTime(delegation.issuedAt),delegationExpires=strictTime(delegation.expiresAt);
    if(!exactKeys(delegation,['delegationId','issuerPrincipal','issuerSubject','recipientPrincipal','recipientSubject','upstreamCredentialDigest','downstreamCredentialDigest','requestDigest','organization','tenant','item','targetExamRevision','targetExamSha256','authorizationPolicyDigest','issuedAt','expiresAt','recordDigest','signature'])||delegation.recordDigest!==request.delegationDigest||delegation.issuerPrincipal!==upstream.principal||delegation.issuerSubject!==upstream.subject||delegation.recipientPrincipal!==downstream.principal||delegation.recipientSubject!==downstream.subject||delegation.upstreamCredentialDigest!==upstream.recordDigest||delegation.downstreamCredentialDigest!==downstream.recordDigest||delegation.requestDigest!==request.immutableRequestDigest||delegation.organization!==request.organization||delegation.tenant!==request.tenant||delegation.item!==request.item||delegation.targetExamRevision!==TARGET_REVISION||delegation.targetExamSha256!==TARGET_EXAM_SHA||delegation.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||[delegatedAt,delegationExpires,requestedAt].includes(null)||delegatedAt<strictTime(upstream.issuedAt)||delegatedAt<strictTime(downstream.issuedAt)||delegationExpires>strictTime(upstream.expiresAt)||delegationExpires>strictTime(downstream.expiresAt)||!(delegatedAt<=requestedAt&&requestedAt<delegationExpires))return deny('DELEGATION_BINDING_MISMATCH');
    if(!exactKeys(assignment,['assignmentId','organization','tenant','actorSubject','actorRole','item','targetExamRevision','targetExamSha256','authorizationPolicyDigest','status','validFrom','expiresAt','recordDigest','signature'])||assignment.recordDigest!==request.assignmentDigest||assignment.organization!==request.organization||assignment.tenant!==request.tenant||assignment.actorSubject!==request.actorSubject||assignment.actorRole!==request.actorRole||assignment.item!==request.item||assignment.targetExamRevision!==TARGET_REVISION||assignment.targetExamSha256!==request.targetExamSha256||assignment.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||assignment.status!=='current'||strictTime(assignment.validFrom)>requestedAt||requestedAt>=strictTime(assignment.expiresAt))return deny('ASSIGNMENT_BINDING_MISMATCH');
    if(!exactKeys(authority,['authorityId','organization','tenant','item','action','actorSubject','actorRole','path','targetExamRevision','targetExamSha256','authorizationPolicyPath','authorizationPolicyDigest','decidedAt','validThrough','recordDigest','signature'])||authority.recordDigest!==request.authorityDigest||authority.organization!==request.organization||authority.tenant!==request.tenant||authority.item!==request.item||authority.action!==request.action||authority.actorSubject!==request.actorSubject||authority.actorRole!==request.actorRole||authority.path!==request.path||authority.targetExamRevision!==TARGET_REVISION||authority.targetExamSha256!==request.targetExamSha256||authority.authorizationPolicyPath!==AUTHORIZATION_POLICY_PATH||authority.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(authority.decidedAt)>requestedAt||requestedAt>=strictTime(authority.validThrough))return deny('AUTHORITY_BINDING_MISMATCH');
    if(!exactKeys(resources,['snapshotId','registryDigest','provider','repositoryId','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordedAt','resources','recordDigest','signature'])||resources.registryDigest!==manifest.trustRegistryDigest||resources.provider!==request.provider||resources.repositoryId!==request.repositoryId||resources.targetExamRevision!==TARGET_REVISION||resources.targetExamSha256!==TARGET_EXAM_SHA||resources.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(resources.recordedAt)>requestedAt||!Array.isArray(resources.resources)||resources.resources.length!==action.resourceIds.length)return deny('RESOURCE_SNAPSHOT_BINDING_MISMATCH');
    const declaredIds=action.resourceIds.slice().sort(),snapshotIds=resources.resources.map(resource=>resource.id).sort();if(jcs(snapshotIds)!==jcs(declaredIds))return deny('RESOURCE_SET_NOT_EXACT');
    for(const resourceId of action.resourceIds){const declaration=manifest.providerResources.find(resource=>resource.id===resourceId);if(!declaration||declaration.type!=='exact'||declaration.value.includes('*')||request[declaration.selector]!==declaration.value)return deny('RESOURCE_POLICY_INVALID');const matches=resources.resources.filter(resource=>exactKeys(resource,['id','selector','type','value'])&&resource.id===resourceId&&resource.selector===declaration.selector&&resource.type===declaration.type&&resource.value===declaration.value&&resource.value===request[declaration.selector]&&!resource.value.includes('*'));if(matches.length!==1)return deny(matches.length?'RESOURCE_MULTI_MATCH':'RESOURCE_ZERO_MATCH');}
    const replayFields=['ledgerId','source','status','idempotencyKey','requestDigest','resultDigest','headId','snapshotAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];
    const headFields=['headId','source','head','previousHead','sequence','snapshotAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];
    const reservationFields=['reservationId','source','headId','expectedHead','idempotencyKey','requestDigest','winner','status','recordedAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];
    if(!exactKeys(replay,replayFields)||!nonempty(replay.ledgerId)||!nonempty(replay.headId)||replay.source!=='authoritative-replay-store'||!['unused','committed'].includes(replay.status)||replay.targetExamRevision!==TARGET_REVISION||replay.targetExamSha256!==TARGET_EXAM_SHA||replay.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(replay.snapshotAt)>requestedAt||requestedAt>=strictTime(replay.validThrough))return deny('AUTHORITATIVE_REPLAY_INVALID');
    if(!exactKeys(head,headFields)||!nonempty(head.headId)||head.source!=='authoritative-cas-store'||head.headId!==replay.headId||head.head!==request.casHead||!Number.isSafeInteger(head.sequence)||head.sequence<1||head.targetExamRevision!==TARGET_REVISION||head.targetExamSha256!==TARGET_EXAM_SHA||head.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(head.snapshotAt)>requestedAt||requestedAt>=strictTime(head.validThrough))return deny('AUTHORITATIVE_CAS_HEAD_INVALID');
    if(!exactKeys(reservation,reservationFields)||!nonempty(reservation.reservationId)||reservation.source!=='authoritative-cas-store'||reservation.headId!==head.headId||reservation.expectedHead!==head.head||reservation.idempotencyKey!==request.idempotencyKey||reservation.requestDigest!==request.immutableRequestDigest||reservation.recordDigest!==request.reservationDigest||reservation.targetExamRevision!==TARGET_REVISION||reservation.targetExamSha256!==TARGET_EXAM_SHA||reservation.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(reservation.recordedAt)<strictTime(head.snapshotAt)||strictTime(reservation.recordedAt)>requestedAt||requestedAt>=strictTime(reservation.validThrough))return deny('CAS_RESERVATION_BINDING_MISMATCH');
    if(replay.idempotencyKey===request.idempotencyKey){if(replay.requestDigest!==request.immutableRequestDigest)return deny('IDEMPOTENCY_DIGEST_MISMATCH');if(replay.status==='committed'){if(!hex(replay.resultDigest,64))return deny('REPLAY_RESULT_MISSING');return {decision:'REPLAY_NOOP',firstError:null,effects:zeroEffects(),consumedRecordIds:[request.requestId,upstream.credentialId,downstream.credentialId,delegation.delegationId,assignment.assignmentId,authority.authorityId,resources.snapshotId,replay.ledgerId,head.headId,reservation.reservationId]};}}
    if(reservation.winner!==true||reservation.status!=='reserved')return deny('CAS_RESERVATION_LOSER');
    const immutableFields=['requestId','action','organization','tenant','repositoryId','installationId','item','path','targetExamRevision','targetExamSha256','authorizationPolicyPath','authorizationPolicyDigest','actorSubject','actorRole','principal','provider','upstreamPrincipal','upstreamCredentialId','upstreamCredentialDigest','idempotencyKey','casHead','requestedAt'];
    const digest=sha256(jcs(Object.fromEntries(immutableFields.map(key=>[key,request[key]]))));if(request.immutableRequestDigest!==digest)return deny('IMMUTABLE_REQUEST_DIGEST_MISMATCH');
    return {decision:'ALLOW',firstError:null,effects:{...zeroEffects(),credentialAccess:1,installationToken:1,providerRequest:1,gitWrite:1},consumedRecordIds:[request.requestId,upstream.credentialId,downstream.credentialId,delegation.delegationId,assignment.assignmentId,authority.authorityId,resources.snapshotId,replay.ledgerId,head.headId,reservation.reservationId]};
  }catch(error){return deny(error.message==='SERIALIZED_BYTES_REQUIRED'?'PROTECTED_ACTION_BUNDLE_SCHEMA_INVALID':error.message);}
}

const detectorPatterns={
  'personal-name':/\b(?:(?:my name is|approved by|reviewed by|signed by)\s+[a-z][a-z.'-]+(?:\s+[a-z][a-z.'-]+){1,3}|[a-z][a-z.'-]+(?:\s+[a-z][a-z.'-]+){1,3}\s+approved this record)\b/u,
  'username-handle':/(?:username|handle)\s*[:=]\s*@?[a-z0-9_]{3,}/u,email:/\b[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}\b/u,phone:/(?<![\p{L}\p{N}])(?:(?:(?:\+|00)\d(?:[ .()\-]*\d){6,14})|(?:(?:\+?1[ .-]?)?\(?\d{3}\)?[ .-]?\d{3}[ .-]?\d{4}))(?:\s*(?:x|ext\.?)[ ]*\d{1,6})?(?![\p{L}\p{N}])/u,
  'postal-address':/\b\d{1,6}\s+[a-z0-9 .'-]+\s(?:lane|ln|street|st|avenue|ave|road|rd|boulevard|blvd)\b/u,'precise-location':/\b(?:lat(?:itude)?|gps)\s*[:=]?\s*-?\d{1,3}\.\d{4,}\s*[, ]+\s*(?:lon(?:gitude)?\s*[:=]?\s*)?-?\d{1,3}\.\d{4,}\b/u,
  'government-identifier':/\b(?:(?:ssn)\s*(?:[:#=]|is)\s*[a-z0-9-]{5,}|(?:passport|driver(?:'s)? license)(?: id| number)\s*(?:[:#=]|is)?\s*[a-z0-9-]{5,})\b/u,'account-identifier':/\baccount(?: id| identifier| number)\s*(?:[:#=]|is)?\s*[a-z0-9-]{4,}\b/u,'customer-identifier':/\bcustomer(?: id| identifier| number)\s*(?:[:#=]|is)?\s*[a-z0-9-]{4,}\b/u,'employee-identifier':/\bemployee(?: id| identifier| number)\s*(?:[:#=]|is)?\s*[a-z0-9-]{4,}\b/u,'device-identifier':/\b(?:device id|device identifier|imei|serial number)\s*(?:[:#=]|is)?\s*[a-z0-9-]{5,}\b/u,
  'ip-address':/(?:\b(?:\d{1,3}\.){3}\d{1,3}\b)|(?:\b[0-9a-f]{1,4}:[0-9a-f:]+[0-9a-f]\b)/u,'mac-address':/\b(?:[0-9a-f]{2}[:-]){5}[0-9a-f]{2}\b/u,'identity-bearing-url':/https?:\/\/[^\s]+[?&](?:user|name|email|account|id)=[^&\s]+/u,biometric:/\b(?:fingerprint|face template|iris scan|voiceprint)\s*[:#=]?\s*[a-z0-9-]{4,}/u,signature:/\b(?:signed by|signature|approved by)\s*[:=]?\s*[a-z][a-z .'-]{3,}/u,secret:/\b(?:api[_ -]?key|client[_ -]?secret|private[_ -]?key)\s*[:=]\s*[a-z0-9_\/-]{8,}/u,token:/\b(?:bearer|access[_ -]?token|refresh[_ -]?token)\s*[:=]?\s*[a-z0-9._-]{8,}/u,credential:/\b(?:password|passcode|credential)\s*[:=]\s*\S{6,}/u,'free-text-self-identification':/\bi am [a-z][a-z.'-]+\s+[a-z][a-z.'-]+(?:,| and)?\s+(?:i work|employed by|working at|my employer|i live)\b/u
};
export function normalizePrivacyText(input){return input.normalize('NFKC').toLowerCase().replace(/\p{Cf}/gu,'').replace(/\s*(?:\[|\()(?:at)(?:\]|\))\s*/gu,'@').replace(/\s+(?:at)\s+/gu,'@').replace(/\s*(?:\[|\()(?:dot)(?:\]|\))\s*/gu,'.').replace(/\s+(?:dot)\s+/gu,'.').replace(/[‐‑‒–—]/gu,'-').replace(/\s+/gu,' ').trim();}
export function detectIdentifiers(input){if(typeof input!=='string')return ['invalid-input'];const candidates=[input];try{const decoded=decodeURIComponent(input);if(decoded!==input)candidates.push(decoded);}catch{}for(const token of input.split(/\s+/u)){if(/^[A-Za-z0-9+/]{16,}={0,2}$/.test(token)){try{const decoded=Buffer.from(token,'base64').toString('utf8');if(/[\x20-\x7e]{8}/.test(decoded))candidates.push(decoded);}catch{}}}const hits=new Set();for(const candidate of candidates){const text=normalizePrivacyText(candidate);for(const [kind,pattern] of Object.entries(detectorPatterns))if(pattern.test(text))hits.add(kind);}return [...hits];}
export const DETECTOR_SAMPLES={};

const privacyFields=['version','organization','tenant','item','targetExamRevision','targetExamDigest','retentionPolicyPath','retentionPolicyBytes','policyDigest','detectorRegistryBytes','detectorRegistryDigest','normalizationRevision','decisionAt','sourcePoolBytesBase64','sourcePoolDigest','sourceAuthorityBytes','useAuthorizationBytes','sanitizerRunBytes','inspectionBytes','encryptedCorpus','prompts','derivatives','rawCopies','rawAuthorities','rawReceipts','destinationPolicy','linkageTrials','linkageClaims'];
export function privacyGraphDecision(input){
  try{
    const graph=typeof input==='string'?parseCanonical(input):input;if(!graph||!exactKeys(graph,privacyFields)||!Array.isArray(graph.prompts)||graph.prompts.length===0||!Array.isArray(graph.rawReceipts)||graph.rawReceipts.length===0)return {decision:'REJECT',firstError:'PRIVACY_GRAPH_EMPTY',effects:zeroEffects()};const graphSchemaErrors=privacyEvidenceSchema(graph);if(graphSchemaErrors.length)return {decision:'REJECT',firstError:'PRIVACY_GRAPH_SCHEMA_INVALID',effects:zeroEffects(),schemaErrors:graphSchemaErrors};
    const detectorBytes=readExact('PRIVACY-DETECTOR-REGISTRY.candidate.json'),registry=parseStrict(detectorBytes);if(graph.detectorRegistryBytes!==detectorBytes||graph.detectorRegistryDigest!==sha256(detectorBytes)||registry.policyClasses.some(kind=>!detectorPatterns[kind])||graph.normalizationRevision!==registry.normalization.join('|'))return {decision:'REJECT',firstError:'DETECTOR_BINDING_MISMATCH',effects:zeroEffects()};
    if(graph.organization!=='steer-platform'||graph.tenant!=='steer-platform'||graph.item!=='0001-flight-deck-foundation'||graph.targetExamRevision!==TARGET_REVISION||graph.targetExamDigest!==TARGET_EXAM_SHA||graph.retentionPolicyPath!==RETENTION_POLICY_PATH||graph.retentionPolicyBytes!==RETENTION_POLICY_BYTES||graph.policyDigest!==RETENTION_POLICY_SHA||sha256(graph.retentionPolicyBytes)!==graph.policyDigest)return {decision:'REJECT',firstError:'PRIVACY_SCOPE_OR_POLICY_MISMATCH',effects:zeroEffects()};
    const now=strictTime(graph.decisionAt),sourceBytes=Buffer.from(graph.sourcePoolBytesBase64,'base64');if(now===null||!sourceBytes.length||sha256(sourceBytes)!==graph.sourcePoolDigest)return {decision:'REJECT',firstError:'SOURCE_POOL_DIGEST_MISMATCH',effects:zeroEffects()};const sourceAuthority=parseSigned(graph.sourceAuthorityBytes,'authority'),useAuthorization=parseSigned(graph.useAuthorizationBytes,'authority'),sanitizer=parseSigned(graph.sanitizerRunBytes,'record'),inspection=parseSigned(graph.inspectionBytes,'record');
    if(sourceAuthority.organization!==graph.organization||sourceAuthority.tenant!==graph.tenant||sourceAuthority.item!==graph.item||sourceAuthority.sourcePoolDigest!==graph.sourcePoolDigest||sourceAuthority.targetExamRevision!==TARGET_REVISION||sourceAuthority.targetExamDigest!==TARGET_EXAM_SHA||sourceAuthority.retentionPolicyDigest!==RETENTION_POLICY_SHA||sourceAuthority.purpose!=='sanitized-real-fixture'||sourceAuthority.status!=='current'||now>=strictTime(sourceAuthority.validThrough))return {decision:'REJECT',firstError:'SOURCE_AUTHORITY_BINDING_MISMATCH',effects:zeroEffects()};
    const requiredDerivatives=['baseline','failure-shrink','provider-copy','model-copy','cache-copy'];if(useAuthorization.authorityId!==sourceAuthority.authorityId||useAuthorization.sourcePoolDigest!==graph.sourcePoolDigest||useAuthorization.organization!==graph.organization||useAuthorization.tenant!==graph.tenant||useAuthorization.item!==graph.item||useAuthorization.targetExamRevision!==TARGET_REVISION||useAuthorization.targetExamDigest!==TARGET_EXAM_SHA||useAuthorization.retentionPolicyDigest!==RETENTION_POLICY_SHA||useAuthorization.destination!==graph.destinationPolicy.fixtureDestination||now<strictTime(useAuthorization.validFrom)||now>=strictTime(useAuthorization.validThrough)||jcs(useAuthorization.allowedDerivatives.slice().sort())!==jcs(requiredDerivatives.slice().sort()))return {decision:'REJECT',firstError:'USE_AUTHORIZATION_BINDING_MISMATCH',effects:zeroEffects()};
    const sourcePool=parseCanonical(sourceBytes.toString('utf8'));if(!Array.isArray(sourcePool)||sourcePool.length<20||new Set(sourcePool.map(row=>row.sourceId)).size!==sourcePool.length||new Set(sourcePool.map(row=>row.text)).size!==sourcePool.length)return {decision:'REJECT',firstError:'SOURCE_POOL_INVALID',effects:zeroEffects()};const sourceById=new Map(sourcePool.map(row=>[row.sourceId,row])),sourceFourCounts=new Map(),quasiKey=text=>(normalizePrivacyText(text).match(/[\p{L}\p{N}]+/gu)??[]).slice(0,3).join('|'),equivalenceCounts=new Map();for(const row of sourcePool){const key=quasiKey(row.text);equivalenceCounts.set(key,(equivalenceCounts.get(key)??0)+1);const tokens=normalizePrivacyText(row.text).match(/[\p{L}\p{N}]+/gu)??[];for(let index=0;index+3<tokens.length;index++){const phrase=tokens.slice(index,index+4).join(' ');sourceFourCounts.set(phrase,(sourceFourCounts.get(phrase)??0)+1);}}
    if(graph.prompts.length!==20)return {decision:'REJECT',firstError:'PROMPT_COUNT_INVALID',effects:zeroEffects()};const ids=new Set(),digests=new Set(),derivedClassSizes=[];for(const prompt of graph.prompts){if(!exactKeys(prompt,['promptId','bytesBase64','sha256','sourceId','destination','detectorHits','namedEntities','uniqueFourTokens']))return {decision:'REJECT',firstError:'PROMPT_SCHEMA_INVALID',effects:zeroEffects()};const bytes=Buffer.from(prompt.bytesBase64,'base64');if(!bytes.length||sha256(bytes)!==prompt.sha256||!sourceById.has(prompt.sourceId))return {decision:'REJECT',firstError:'PROMPT_DIGEST_OR_SOURCE_MISMATCH',effects:zeroEffects()};if(ids.has(prompt.promptId)||digests.has(prompt.sha256))return {decision:'REJECT',firstError:'PROMPT_INVENTORY_DUPLICATE',effects:zeroEffects()};ids.add(prompt.promptId);digests.add(prompt.sha256);const promptText=bytes.toString('utf8'),tokens=normalizePrivacyText(promptText).match(/[\p{L}\p{N}]+/gu)??[],unique=[];for(let index=0;index+3<tokens.length;index++){const phrase=tokens.slice(index,index+4).join(' ');if(sourceFourCounts.get(phrase)===1)unique.push(phrase);}const named=[...promptText.matchAll(/\b[A-Z][a-z]+\s+[A-Z][a-z]+\b/g)].map(match=>match[0]),equivalenceClassSize=equivalenceCounts.get(quasiKey(promptText))??0;derivedClassSizes.push(equivalenceClassSize);if(jcs(detectIdentifiers(promptText).sort())!==jcs(prompt.detectorHits.slice().sort())||prompt.detectorHits.length||jcs(named)!==jcs(prompt.namedEntities)||jcs(unique)!==jcs(prompt.uniqueFourTokens)||equivalenceClassSize<5)return {decision:'REJECT',firstError:'SANITIZATION_RECOMPUTE_FAILED',effects:zeroEffects()};if(prompt.destination!==graph.destinationPolicy.fixtureDestination)return {decision:'REJECT',firstError:'DESTINATION_CLASS_MISMATCH',effects:zeroEffects()};}
    const corpusDigest=sha256(jcs(graph.prompts));if(!exactKeys(graph.encryptedCorpus,['corpusId','version','encryptedObjectDigest','manifestDigest','keyId','promptInventoryDigest','destination'])||graph.encryptedCorpus.promptInventoryDigest!==corpusDigest||graph.encryptedCorpus.destination!==graph.destinationPolicy.fixtureDestination||!nonempty(graph.encryptedCorpus.keyId))return {decision:'REJECT',firstError:'ENCRYPTED_CORPUS_BINDING_MISMATCH',effects:zeroEffects()};
    if(graph.derivatives.length!==requiredDerivatives.length||new Set(graph.derivatives.map(value=>value.kind)).size!==requiredDerivatives.length||requiredDerivatives.some(kind=>!graph.derivatives.some(value=>exactKeys(value,['derivativeId','kind','corpusDigest','destination','credentialClass'])&&value.kind===kind&&value.corpusDigest===corpusDigest&&value.destination===graph.destinationPolicy.fixtureDestination&&value.credentialClass===graph.destinationPolicy.fixtureCredential)))return {decision:'REJECT',firstError:'DERIVATIVE_INVENTORY_INCOMPLETE',effects:zeroEffects()};
    if(graph.rawCopies.length===0||graph.rawCopies.length!==graph.rawReceipts.length||graph.rawCopies.length!==graph.rawAuthorities.length)return {decision:'REJECT',firstError:'RAW_INVENTORY_INCOMPLETE',effects:zeroEffects()};for(const copy of graph.rawCopies){const signedCopy=parseSigned(copy.recordBytes),authorities=graph.rawAuthorities.filter(row=>row.copyId===copy.copyId),matches=graph.rawReceipts.filter(receipt=>receipt.copyId===copy.copyId);if(signedCopy.copyId!==copy.copyId||signedCopy.provider!==copy.provider||signedCopy.sourcePoolDigest!==graph.sourcePoolDigest||signedCopy.item!==graph.item||authorities.length!==1||matches.length!==1)return {decision:'REJECT',firstError:'RAW_INVENTORY_BINDING_MISMATCH',effects:zeroEffects()};const authority=parseSigned(authorities[0].recordBytes,'authority'),receipt=parseSigned(matches[0].recordBytes,'provider');if(authority.copyId!==copy.copyId||authority.provider!==copy.provider||authority.objectDigest!==signedCopy.objectDigest||authority.sourcePoolDigest!==graph.sourcePoolDigest||authority.validThrough!==useAuthorization.validThrough||receipt.copyId!==copy.copyId||matches[0].provider!==copy.provider||matches[0].objectDigest!==signedCopy.objectDigest||receipt.provider!==copy.provider||receipt.objectDigest!==signedCopy.objectDigest||receipt.disposition!=='deleted'||receipt.terminal!==true||receipt.authorityDigest!==authority.recordDigest)return {decision:'REJECT',firstError:'RAW_RECEIPT_BINDING_MISMATCH',effects:zeroEffects()};}
    if(sanitizer.sourcePoolDigest!==graph.sourcePoolDigest||sanitizer.detectorRegistryDigest!==graph.detectorRegistryDigest||sanitizer.promptInventoryDigest!==corpusDigest||sanitizer.targetExamRevision!==TARGET_REVISION||sanitizer.targetExamDigest!==TARGET_EXAM_SHA||sanitizer.retentionPolicyDigest!==RETENTION_POLICY_SHA||sanitizer.identity!=='sanitizer-service'||sanitizer.revision!=='sanitizer-v1'||sanitizer.result!=='pass'||inspection.runDigest!==sanitizer.recordDigest||inspection.promptInventoryDigest!==corpusDigest||inspection.targetExamRevision!==TARGET_REVISION||inspection.targetExamDigest!==TARGET_EXAM_SHA||inspection.retentionPolicyDigest!==RETENTION_POLICY_SHA||inspection.identity!=='independent-inspector'||inspection.revision!=='inspector-v1'||inspection.result!=='pass')return {decision:'REJECT',firstError:'SANITIZER_INSPECTION_BINDING_MISMATCH',effects:zeroEffects()};
    const trials=graph.prompts.map(prompt=>{const promptTokens=new Set(normalizePrivacyText(Buffer.from(prompt.bytesBase64,'base64').toString('utf8')).match(/[\p{L}\p{N}]+/gu)??[]);let best=null,bestScore=0;for(const source of sourcePool){const sourceTokens=new Set(normalizePrivacyText(source.text).match(/[\p{L}\p{N}]+/gu)??[]),score=[...promptTokens].filter(token=>sourceTokens.has(token)).length;if(score>bestScore){bestScore=score;best=source.sourceId;}}return {promptId:prompt.promptId,predictedSourceId:best,actualSourceId:prompt.sourceId,linked:best!==null&&best===prompt.sourceId,score:bestScore};});if(jcs(trials)!==jcs(graph.linkageTrials))return {decision:'REJECT',firstError:'LINKAGE_TRIALS_MISMATCH',effects:zeroEffects()};const n=trials.length,matches=trials.filter(row=>row.linked).length,top1=matches/n,z=1.96,center=(top1+z*z/(2*n))/(1+z*z/n),margin=z*Math.sqrt((top1*(1-top1)+z*z/(4*n))/n)/(1+z*z/n),wilson=center+margin;if(Math.round(top1*1_000_000)!==graph.linkageClaims.top1Millionths||Math.round(wilson*1_000_000)!==graph.linkageClaims.wilson95UpperMillionths||top1>registry.linkage.top1Maximum||wilson>registry.linkage.wilson95UpperMaximum)return {decision:'REJECT',firstError:'LINKAGE_RECOMPUTE_MISMATCH',effects:zeroEffects()};
    return {decision:'ACCEPT',firstError:null,effects:zeroEffects(),promptCount:20,equivalenceClassSizes:derivedClassSizes};
  }catch(error){return {decision:'REJECT',firstError:error.message,effects:zeroEffects()};}
}
export function accessibilityMatrixProof(input){
  try{
    const bundleKeys=['manifestBytes','rows','summaryBytes','identityBytes','qualificationBytes','assignmentBytes','batchProofBytes','providerProofBytes','evaluationTime'];
    if(!input||typeof input!=='object'||!exactKeys(input,bundleKeys)||typeof input.manifestBytes!=='string'||!input.rows||bundleKeys.slice(2).some(key=>typeof input[key]!=='string'))return {valid:false,error:'ACCESSIBILITY_BUNDLE_SCHEMA_INVALID',rowCount:0,digest:null};
    const manifest=exactArtifact('ACCESSIBILITY-MATRIX-MANIFEST.candidate.json',input.manifestBytes),summary=parseSigned(input.summaryBytes,'summary'),identity=parseSigned(input.identityBytes,'provider'),qualification=parseSigned(input.qualificationBytes,'provider'),assignment=parseSigned(input.assignmentBytes,'assignment'),batch=parseSigned(input.batchProofBytes,'summary'),provider=parseSigned(input.providerProofBytes,'human-provider'),now=strictTime(input.evaluationTime);if(accessibilityManifestSchema(manifest).length||accessibilitySummarySchema(summary).length)return {valid:false,error:'ACCESSIBILITY_MANIFEST_OR_SUMMARY_SCHEMA_INVALID',rowCount:0,digest:null};if(manifest.version!=='steer-accessibility-matrix/v2'||manifest.checkpointModel.first!=='A01'||manifest.checkpointModel.last!=='A81'||manifest.checkpointModel.count!==81||manifest.requiredRowFields.length!==27)return {valid:false,error:'ACCESSIBILITY_MANIFEST_INVALID',rowCount:0,digest:null};
    if(now===null||!exactKeys(identity,['evidenceId','organization','humanSubject','issuer','status','verifiedAt','recordDigest','signature'])||identity.organization!=='steer-platform'||identity.humanSubject!==summary.reviewerSubject||identity.issuer!=='steer-identity'||identity.status!=='verified-active'||strictTime(identity.verifiedAt)>now||identity.recordDigest!==summary.identityDigest)return {valid:false,error:'ACCESSIBILITY_IDENTITY_INVALID',rowCount:0,digest:null};
    if(!exactKeys(qualification,['evidenceId','organization','humanSubject','credential','issuer','status','validFrom','validThrough','recordDigest','signature'])||qualification.organization!=='steer-platform'||qualification.humanSubject!==summary.reviewerSubject||qualification.credential!=='DHS Trusted Tester'||qualification.issuer!=='DHS'||qualification.status!=='current'||strictTime(qualification.validFrom)>now||now>=strictTime(qualification.validThrough)||qualification.recordDigest!==summary.qualificationDigest)return {valid:false,error:'ACCESSIBILITY_QUALIFICATION_INVALID',rowCount:0,digest:null};
    if(!exactKeys(assignment,['assignmentId','organization','humanSubject','activeHat','pod','item','targetExamRevision','targetExamDigest','implementationRevision','status','validFrom','validThrough','recordDigest','signature'])||assignment.organization!=='steer-platform'||assignment.humanSubject!==summary.reviewerSubject||assignment.activeHat!=='accessibility-specialist'||assignment.pod!=='platform'||assignment.item!=='0001-flight-deck-foundation'||assignment.targetExamRevision!==TARGET_REVISION||assignment.targetExamDigest!==TARGET_EXAM_SHA||assignment.implementationRevision!==summary.implementationRevision||assignment.status!=='current'||strictTime(assignment.validFrom)>now||now>=strictTime(assignment.validThrough)||assignment.recordDigest!==summary.assignmentDigest)return {valid:false,error:'ACCESSIBILITY_ASSIGNMENT_INVALID',rowCount:0,digest:null};
    const surfaces=[...manifest.routes.flatMap(route=>route.states.map(state=>['route',route.id,state])),...manifest.stories.flatMap(story=>manifest.storyStates.map(state=>['story',story,state]))],expectedCount=surfaces.length*manifest.scenarios.length*manifest.environments.reduce((count,environment)=>count+manifest.viewports.filter(viewport=>viewport.class===environment.viewportClass).length,0)*81;
    let count=0,notApplicable=0,failures=0;const seen=new Set(),hash=createHash('sha256');
    for(const bytes of input.rows){const row=parseCanonical(bytes);if(accessibilityRowSchema(row).length||!exactKeys(row,manifest.requiredRowFields))return {valid:false,error:'ROW_SCHEMA_INVALID',rowCount:count,digest:null};const environment=manifest.environments.find(value=>value.id===row.environmentId),viewport=manifest.viewports.find(value=>value.id===row.viewportId);if(!environment||!viewport||viewport.class!==environment.viewportClass||['os','osVersion','browser','browserVersion','assistiveTechnology','assistiveTechnologyVersion','inputMode'].some(key=>row[key]!==environment[key])||row.viewportWidth!==viewport.width||row.viewportHeight!==viewport.height)return {valid:false,error:'ROW_ENVIRONMENT_BINDING_MISMATCH',rowCount:count,digest:null};const surfaceOkay=row.surfaceKind==='route'?manifest.routes.some(route=>route.id===row.surfaceId&&route.states.includes(row.state)):row.surfaceKind==='story'&&manifest.stories.includes(row.surfaceId)&&manifest.storyStates.includes(row.state);const evidence=Buffer.from(row.evidenceBytesBase64,'base64');if(!surfaceOkay||!manifest.scenarios.includes(row.scenario)||!Array.isArray(row.checkpoints)||row.checkpoints.length!==81||row.operatorSubject!==summary.reviewerSubject||strictTime(row.startedAt)===null||strictTime(row.endedAt)<=strictTime(row.startedAt)||!nonempty(row.notes)||row.evidenceReferences.length!==1||!evidence.length||sha256(evidence)!==row.evidenceSha256||row.evidenceReferences[0]!==`sha256:${row.evidenceSha256}`||row.examRevision!==TARGET_REVISION||row.implementationRevision!==summary.implementationRevision||row.checkpointModelDigest!==summary.checkpointModelDigest)return {valid:false,error:'ROW_EVIDENCE_INVALID',rowCount:count,digest:null};for(const checkpoint of row.checkpoints){if(!exactKeys(checkpoint,['checkpointId','outcome','applicabilityRationale'])||!/^A(?:0[1-9]|[1-7][0-9]|8[01])$/.test(checkpoint.checkpointId))return {valid:false,error:'ROW_KEY_MISMATCH',rowCount:count,digest:null};if(checkpoint.outcome==='not-applicable'){notApplicable++;if(!checkpoint.applicabilityRationale.startsWith(`${checkpoint.checkpointId}:`))return {valid:false,error:'NA_RATIONALE_REQUIRED',rowCount:count,digest:null};}else if(checkpoint.outcome!=='pass')failures++;const key=[row.surfaceKind,row.surfaceId,row.state,row.scenario,row.viewportId,row.environmentId,checkpoint.checkpointId].join('|');if(seen.has(key))return {valid:false,error:'DUPLICATE_ROW_KEY',rowCount:count,digest:null};seen.add(key);count++;}hash.update(bytes+'\n');}
    const digest=hash.digest('hex');if(count!==expectedCount)return {valid:false,error:count<expectedCount?'EXPECTED_KEY_MISSING':'UNDECLARED_KEY_EXTRA',rowCount:count,digest};if(notApplicable===count)return {valid:false,error:'ALL_NOT_APPLICABLE_DENIED',rowCount:count,digest};if(summary.manifestDigest!==sha256(input.manifestBytes)||summary.rowCount!==count||summary.notApplicableCount!==notApplicable||summary.failureCount!==failures||summary.rawRowsDigest!==digest||summary.decision!==(failures?'fail':'pass')||summary.targetExamRevision!==TARGET_REVISION||summary.targetExamDigest!==TARGET_EXAM_SHA||summary.activeHat!=='accessibility-specialist'||summary.pod!=='platform')return {valid:false,error:'SUMMARY_BINDING_MISMATCH',rowCount:count,digest};
    if(!exactKeys(batch,['batchId','manifestDigest','rawRowsDigest','rowCount','reviewerSubject','assignmentDigest','sealedAt','recordDigest','signature'])||batch.manifestDigest!==summary.manifestDigest||batch.rawRowsDigest!==digest||batch.rowCount!==count||batch.reviewerSubject!==summary.reviewerSubject||batch.assignmentDigest!==assignment.recordDigest||strictTime(batch.sealedAt)>now)return {valid:false,error:'ACCESSIBILITY_BATCH_PROOF_INVALID',rowCount:count,digest};
    if(!exactKeys(provider,['providerRecordId','summaryDigest','batchProofDigest','reviewerSubject','credential','assignmentDigest','decision','recordedAt','recordDigest','signature'])||provider.summaryDigest!==summary.recordDigest||provider.batchProofDigest!==batch.recordDigest||provider.reviewerSubject!==summary.reviewerSubject||provider.credential!==qualification.credential||provider.assignmentDigest!==assignment.recordDigest||provider.decision!=='verified-pass'||strictTime(provider.recordedAt)>now)return {valid:false,error:'ACCESSIBILITY_PROVIDER_PROOF_INVALID',rowCount:count,digest};return {valid:failures===0,error:failures?'ACCESSIBILITY_FAILURE':null,rowCount:count,digest};
  }catch(error){return {valid:false,error:error.message,rowCount:0,digest:null};}
}
const lifecycleSchema=compile202012(parseStrict(fs.readFileSync(path.join(dir,'schemas/LIFECYCLE-EVENT.schema.json'),'utf8')));
export function lifecycleEventDecision(input,historyBytes='[]'){
  try{
    const event=parseCanonical(input),schemaErrors=lifecycleSchema(event);if(schemaErrors.length)return {state:'blocked-policy-conflict',firstError:'EVENT_SCHEMA_INVALID',effects:zeroEffects(),schemaErrors};if(verifyRecord(event))return {state:'blocked-policy-conflict',firstError:'EVENT_SIGNATURE_INVALID',effects:zeroEffects()};
    const proof=parseSigned(event.providerProofBytes,'provider'),eventPreimage=clone(event);delete eventPreimage.providerProofDigest;delete eventPreimage.providerProofBytes;delete eventPreimage.recordDigest;delete eventPreimage.signature;const eventBindingDigest=sha256(jcs(eventPreimage));if(proof.eventId!==event.eventId||proof.eventBindingDigest!==eventBindingDigest||proof.recordedAt!==event.occurredAt||proof.recordDigest!==event.providerProofDigest)return {state:'blocked-policy-conflict',firstError:'EVENT_PROVIDER_PROOF_BINDING_MISMATCH',effects:zeroEffects()};
    const history=parseCanonical(historyBytes);if(!Array.isArray(history))return {state:'blocked-policy-conflict',firstError:'EVENT_HISTORY_INVALID',effects:zeroEffects()};let prior=0;const ids=new Set(),digests=new Set();for(const bytes of history){const priorEvent=parseCanonical(bytes),errors=lifecycleSchema(priorEvent),time=strictTime(priorEvent.occurredAt);if(errors.length||verifyRecord(priorEvent)||time===null)return {state:'blocked-policy-conflict',firstError:'EVENT_HISTORY_INVALID',effects:zeroEffects()};if(time<=prior)return {state:'blocked-policy-conflict',firstError:'EVENT_ORDER_INVALID',effects:zeroEffects()};if(ids.has(priorEvent.eventId)||digests.has(priorEvent.recordDigest))return {state:'blocked-policy-conflict',firstError:'EVENT_REPLAY',effects:zeroEffects()};prior=time;ids.add(priorEvent.eventId);digests.add(priorEvent.recordDigest);}if(ids.has(event.eventId)||digests.has(event.recordDigest))return {state:'blocked-policy-conflict',firstError:'EVENT_REPLAY',effects:zeroEffects()};if(prior&&strictTime(event.occurredAt)<=prior)return {state:'blocked-policy-conflict',firstError:'EVENT_ORDER_INVALID',effects:zeroEffects()};return {state:'validated-trigger',firstError:null,effects:zeroEffects(),eventId:event.eventId};
  }catch(error){return {state:'blocked-policy-conflict',firstError:error.message,effects:zeroEffects()};}
}
const addLifecycleDuration=(trigger,duration)=>{if(duration==='indefinite')return null;if(duration==='immediate')return trigger;const match=/^P(?:(\d+)Y|(\d+)D)|^PT(\d+)S$/.exec(duration);if(!match)throw new Error('LIFECYCLE_DURATION_INVALID');const date=new Date(trigger);if(match[1]){const month=date.getUTCMonth(),day=date.getUTCDate();date.setUTCDate(1);date.setUTCFullYear(date.getUTCFullYear()+Number(match[1]));date.setUTCMonth(month);date.setUTCDate(Math.min(day,new Date(Date.UTC(date.getUTCFullYear(),month+1,0)).getUTCDate()));return date.getTime();}return trigger+Number(match[2]??0)*86_400_000+Number(match[3]??0)*1000;};
const lifecycleIso=time=>new Date(time).toISOString().replace('.000Z','Z');
function lifecycleGraphDecisionLegacy(input){
  try{
    const graph=parseCanonical(input),keys=['policyTableBytes','recordClass','objectId','tenant','evaluationAt','triggerBytes','parentExpiryAt','holdState','referenceRevocationBytes','rawGrantBytes','inventoryBytes','requestBytes','providerReceiptBytes','aggregateReceiptBytes','tombstoneBytes'];if(!exactKeys(graph,keys))return blocked('LIFECYCLE_GRAPH_SCHEMA_INVALID');
    const policy=exactArtifact('LIFECYCLE-POLICY-TABLE.candidate.json',graph.policyTableBytes),rows=policy.classes.filter(row=>row.classId===graph.recordClass);if(rows.length!==1||policy.classes.length!==16||new Set(policy.classes.map(row=>row.classId)).size!==16)return blocked('LIFECYCLE_POLICY_CLASS_INVALID');const row=rows[0],tableDigest=sha256(graph.policyTableBytes),trigger=parseSigned(graph.triggerBytes),inventory=parseSigned(graph.inventoryBytes),request=parseSigned(graph.requestBytes),receipt=parseSigned(graph.providerReceiptBytes,'provider'),aggregate=parseSigned(graph.aggregateReceiptBytes,'provider'),tombstone=parseSigned(graph.tombstoneBytes),now=strictTime(graph.evaluationAt),triggerAt=strictTime(trigger.occurredAt),parentAt=graph.parentExpiryAt===null?null:strictTime(graph.parentExpiryAt);if(now===null||triggerAt===null||trigger.recordClass!==graph.recordClass||trigger.objectId!==graph.objectId||trigger.tenant!==graph.tenant||trigger.eventType!==row.trigger||trigger.policyTableDigest!==tableDigest||trigger.policySha256!==policy.policySha256)return blocked('LIFECYCLE_TRIGGER_BINDING_MISMATCH');
    let boundary=addLifecycleDuration(triggerAt,row.duration);if(row.parentCap){if(parentAt===null)return blocked('LIFECYCLE_PARENT_EXPIRY_MISSING');boundary=Math.min(boundary,parentAt);}if(boundary===null)return {state:'retained-immutable',firstError:null,effects:zeroEffects(),boundaryAt:null};if(now<boundary)return {state:'scheduled',firstError:null,effects:zeroEffects(),boundaryAt:lifecycleIso(boundary)};
    if(graph.holdState==='active')return {state:'retained-on-hold',firstError:null,effects:zeroEffects(),boundaryAt:lifecycleIso(boundary)};if(!['none','released'].includes(graph.holdState))return blocked('LIFECYCLE_HOLD_CONFLICT');
    if(row.disposition.startsWith('reference-')){if(!graph.referenceRevocationBytes)return {state:'retained-pending-safe-disposition',firstError:'REFERENCE_REVOCATION_MISSING',effects:zeroEffects(),boundaryAt:new Date(boundary).toISOString()};const reference=parseSigned(graph.referenceRevocationBytes,'authority');if(reference.objectId!==graph.objectId||reference.tenant!==graph.tenant||reference.inventoryDigest!==inventory.recordDigest||reference.decision!=='authorized')return {state:'retained-pending-safe-disposition',firstError:'REFERENCE_REVOCATION_INVALID',effects:zeroEffects(),boundaryAt:new Date(boundary).toISOString()};}
    if(graph.recordClass==='RC-CORPUS-RAW-WORKING'){if(!graph.rawGrantBytes)return blocked('BLOCKED_MISSING_RAW_POLICY_GRANT');const grant=parseSigned(graph.rawGrantBytes,'authority');if(grant.recordClass!==graph.recordClass||grant.objectId!==graph.objectId||grant.tenant!==graph.tenant||grant.policySha256!==policy.policySha256||grant.targetExamDigest!==TARGET_EXAM_SHA||strictTime(grant.validFrom)>triggerAt||boundary>strictTime(grant.expiresAt)||jcs(grant.safeguards)!==jcs(['network-denied','encrypted','inventory','receipt']))return blocked('RAW_POLICY_GRANT_INVALID');}
    if(!exactKeys(inventory,['inventoryId','tenant','objectId','recordClass','policyTableDigest','capturedAt','copies','recordDigest','signature'])||inventory.tenant!==graph.tenant||inventory.objectId!==graph.objectId||inventory.recordClass!==graph.recordClass||inventory.policyTableDigest!==tableDigest||!Array.isArray(inventory.copies)||inventory.copies.length===0||new Set(inventory.copies.map(copy=>copy.copyId)).size!==inventory.copies.length||now-strictTime(inventory.capturedAt)>300_000)return blocked('LIFECYCLE_INVENTORY_INVALID');
    if(!exactKeys(request,['requestId','tenant','objectId','recordClass','boundaryAt','inventoryDigest','idempotencyKey','requestedAt','recordDigest','signature'])||request.tenant!==graph.tenant||request.objectId!==graph.objectId||request.recordClass!==graph.recordClass||request.boundaryAt!==lifecycleIso(boundary)||request.inventoryDigest!==inventory.recordDigest||strictTime(request.requestedAt)<boundary)return {state:'quarantined-deletion-pending',firstError:'LIFECYCLE_REQUEST_INVALID',effects:zeroEffects(),boundaryAt:lifecycleIso(boundary)};
    const copy=inventory.copies[0];if(inventory.copies.length!==1||!exactKeys(copy,['copyId','provider','account','objectDigest'])||!exactKeys(receipt,['receiptId','requestId','transactionId','tenant','objectId','copyId','provider','account','objectDigest','effect','status','recordedAt','recordDigest','signature'])||receipt.requestId!==request.requestId||receipt.tenant!==graph.tenant||receipt.objectId!==graph.objectId||receipt.copyId!==copy.copyId||receipt.provider!==copy.provider||receipt.account!==copy.account||receipt.objectDigest!==copy.objectDigest||receipt.effect!==(graph.recordClass==='RC-CORPUS-RAW-WORKING'?'crypto-erased':'deleted')||receipt.status!=='terminal-success'||strictTime(receipt.recordedAt)<=strictTime(request.requestedAt)||strictTime(receipt.recordedAt)>now)return {state:'quarantined-deletion-pending',firstError:'PROVIDER_RECEIPT_INCOMPLETE',effects:zeroEffects(),boundaryAt:new Date(boundary).toISOString()};
    if(!exactKeys(aggregate,['aggregateId','requestId','receiptDigests','allCopiesGone','recordedAt','recordDigest','signature'])||aggregate.requestId!==request.requestId||jcs(aggregate.receiptDigests)!==jcs([receipt.recordDigest])||aggregate.allCopiesGone!==true||strictTime(aggregate.recordedAt)<=strictTime(receipt.recordedAt)||strictTime(aggregate.recordedAt)>now)return {state:'quarantined-deletion-pending',firstError:'AGGREGATE_RECEIPT_INVALID',effects:zeroEffects(),boundaryAt:new Date(boundary).toISOString()};
    if(!exactKeys(tombstone,['tombstoneId','tenant','objectId','recordClass','requestDigest','aggregateDigest','committedAt','recordDigest','signature'])||tombstone.tenant!==graph.tenant||tombstone.objectId!==graph.objectId||tombstone.recordClass!==graph.recordClass||tombstone.requestDigest!==request.recordDigest||tombstone.aggregateDigest!==aggregate.recordDigest||strictTime(tombstone.committedAt)<=strictTime(aggregate.recordedAt)||strictTime(tombstone.committedAt)>now)return {state:'quarantined-deletion-pending',firstError:'TOMBSTONE_INVALID',effects:zeroEffects(),boundaryAt:new Date(boundary).toISOString()};return {state:'deleted-tombstoned',firstError:null,effects:{...zeroEffects(),lifecycle:1},boundaryAt:new Date(boundary).toISOString()};
  }catch(error){return blocked(error.message);}
}
export function lifecycleGraphDecision(input){
  const pending=(firstError,boundaryAt)=>({state:'quarantined-deletion-pending',firstError,effects:zeroEffects(),boundaryAt});
  try{
    const graph=parseCanonical(input),keys=['policyTableBytes','retentionPolicyBytes','authorizationPolicyBytes','providerKeyRegistryBytes','recordClass','objectId','tenant','evaluationAt','triggerBytes','parentExpiryAt','holdState','referenceRevocationBytes','rawGrantBytes','inventoryBytes','humanAuthorityBundles','protectedRequestsBytes','replayLedgersBytes','casHeadsBytes','reservationsBytes','providerResourceSnapshotsBytes','providerReceiptsBytes','aggregateReceiptBytes','tombstoneBytes'];
    if(!exactKeys(graph,keys))return blocked('LIFECYCLE_GRAPH_SCHEMA_INVALID');
    if(graph.retentionPolicyBytes!==RETENTION_POLICY_BYTES||sha256(graph.retentionPolicyBytes)!==RETENTION_POLICY_SHA||graph.authorizationPolicyBytes!==AUTHORIZATION_POLICY_BYTES||sha256(graph.authorizationPolicyBytes)!==AUTHORIZATION_POLICY_SHA)return blocked('LIFECYCLE_POLICY_BYTES_MISMATCH');
    const policy=exactArtifact('LIFECYCLE-POLICY-TABLE.candidate.json',graph.policyTableBytes),providerRegistry=exactArtifact('PROVIDER-KEY-REGISTRY.candidate.json',graph.providerKeyRegistryBytes),providerRegistryDigest=sha256(graph.providerKeyRegistryBytes),rows=policy.classes.filter(row=>row.classId===graph.recordClass);
    if(rows.length!==1||policy.classes.length!==16||new Set(policy.classes.map(row=>row.classId)).size!==16)return blocked('LIFECYCLE_POLICY_CLASS_INVALID');
    const row=rows[0],tableDigest=sha256(graph.policyTableBytes),trigger=parseSigned(graph.triggerBytes),now=strictTime(graph.evaluationAt),triggerAt=strictTime(trigger.occurredAt),parentAt=graph.parentExpiryAt===null?null:strictTime(graph.parentExpiryAt);
    if(now===null||triggerAt===null||trigger.recordClass!==graph.recordClass||trigger.objectId!==graph.objectId||trigger.tenant!==graph.tenant||trigger.eventType!==row.trigger||trigger.policyTableDigest!==tableDigest||trigger.policySha256!==RETENTION_POLICY_SHA||trigger.targetExamRevision!==TARGET_REVISION||trigger.targetExamDigest!==TARGET_EXAM_SHA||trigger.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||trigger.providerKeyRegistryDigest!==providerRegistryDigest)return blocked('LIFECYCLE_TRIGGER_BINDING_MISMATCH');
    let boundary=addLifecycleDuration(triggerAt,row.duration);if(row.parentCap){if(parentAt===null)return blocked('LIFECYCLE_PARENT_EXPIRY_MISSING');boundary=Math.min(boundary,parentAt);}if(boundary===null)return {state:'retained-immutable',firstError:null,effects:zeroEffects(),boundaryAt:null};const boundaryAt=lifecycleIso(boundary);if(now<boundary)return {state:'scheduled',firstError:null,effects:zeroEffects(),boundaryAt};
    if(graph.holdState==='active')return {state:'retained-on-hold',firstError:null,effects:zeroEffects(),boundaryAt};if(!['none','released'].includes(graph.holdState))return blocked('LIFECYCLE_HOLD_CONFLICT');
    const inventory=parseSigned(graph.inventoryBytes);if(!exactKeys(inventory,['inventoryId','tenant','objectId','recordClass','policyTableDigest','providerKeyRegistryDigest','targetExamRevision','targetExamDigest','retentionPolicyDigest','authorizationPolicyDigest','capturedAt','tupleDigest','copies','recordDigest','signature'])||inventory.tenant!==graph.tenant||inventory.objectId!==graph.objectId||inventory.recordClass!==graph.recordClass||inventory.policyTableDigest!==tableDigest||inventory.providerKeyRegistryDigest!==providerRegistryDigest||inventory.targetExamRevision!==TARGET_REVISION||inventory.targetExamDigest!==TARGET_EXAM_SHA||inventory.retentionPolicyDigest!==RETENTION_POLICY_SHA||inventory.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||!Array.isArray(inventory.copies)||inventory.copies.length<2||new Set(inventory.copies.map(copy=>copy.authorityId)).size!==inventory.copies.length||new Set(inventory.copies.map(copy=>jcs(copy))).size!==inventory.copies.length||inventory.tupleDigest!==sha256(jcs(inventory.copies))||now-strictTime(inventory.capturedAt)>300_000)return blocked('LIFECYCLE_INVENTORY_INVALID');
    if(row.disposition.startsWith('reference-')){if(!graph.referenceRevocationBytes)return {state:'retained-pending-safe-disposition',firstError:'REFERENCE_REVOCATION_MISSING',effects:zeroEffects(),boundaryAt};const reference=parseSigned(graph.referenceRevocationBytes,'authority');if(reference.objectId!==graph.objectId||reference.tenant!==graph.tenant||reference.inventoryDigest!==inventory.recordDigest||reference.decision!=='authorized')return {state:'retained-pending-safe-disposition',firstError:'REFERENCE_REVOCATION_INVALID',effects:zeroEffects(),boundaryAt};}
    if(!Array.isArray(graph.rawGrantBytes)||(graph.recordClass==='RC-CORPUS-RAW-WORKING'?graph.rawGrantBytes.length!==inventory.copies.length:graph.rawGrantBytes.length!==0))return blocked('BLOCKED_MISSING_RAW_POLICY_GRANT');
    const arrays=['humanAuthorityBundles','protectedRequestsBytes','replayLedgersBytes','casHeadsBytes','reservationsBytes','providerResourceSnapshotsBytes','providerReceiptsBytes'];if(arrays.some(key=>!Array.isArray(graph[key])||graph[key].length!==inventory.copies.length))return pending('LIFECYCLE_TUPLE_CARDINALITY_INVALID',boundaryAt);
    const orderedRows=[],receiptDigests=[],seenRequests=new Set(),seenKeys=new Set(),seenTransactions=new Set();let allReplay=true;
    for(const [index,tuple] of inventory.copies.entries()){
      if(!exactKeys(tuple,['authorityId','copyId','copyKind','provider','providerBindingId','account','objectKey','versionId','keyId','targetExamRevision','targetExamDigest','retentionPolicyDigest','authorizationPolicyDigest'])||tuple.targetExamRevision!==TARGET_REVISION||tuple.targetExamDigest!==TARGET_EXAM_SHA||tuple.retentionPolicyDigest!==RETENTION_POLICY_SHA||tuple.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA)return pending('LIFECYCLE_TUPLE_INVALID',boundaryAt);
      const bindings=providerRegistry.bindings.filter(value=>value.providerBindingId===tuple.providerBindingId&&value.provider===tuple.provider&&value.account===tuple.account&&value.tenant===graph.tenant);if(bindings.length!==1)return pending('PROVIDER_BINDING_SELECTION_INVALID',boundaryAt);const binding=bindings[0],trustBindings=TRUST_REGISTRY.bindings.filter(value=>value.domain===binding.domain&&value.keyId===binding.keyId);if(trustBindings.length!==1||['algorithm','publicKeyHex','notBefore','notAfter','revokedAt'].some(field=>trustBindings[0][field]!==binding[field]))return pending('PROVIDER_BINDING_TRUST_ANCHOR_MISMATCH',boundaryAt);const validFrom=strictTime(binding.notBefore),validUntil=strictTime(binding.notAfter),revokedAt=binding.revokedAt===null?null:strictTime(binding.revokedAt);if([validFrom,validUntil].includes(null)||now<validFrom||now>=validUntil||(revokedAt!==null&&now>=revokedAt))return pending('PROVIDER_BINDING_NOT_CURRENT',boundaryAt);
      const humanBundle=graph.humanAuthorityBundles[index],humanResult=humanAuthorityDecision(humanBundle);if(humanResult.decision!=='ALLOW')return pending(`HUMAN_DISPOSITION_${humanResult.firstError}`,boundaryAt);const authority=parseCanonical(humanBundle.authorityBytes),authorityInventory=parseCanonical(humanBundle.inventoryBytes),tupleHash=sha256(jcs(tuple));if(authority.authorityId!==tuple.authorityId||authority.copyInventoryDigest!==authorityInventory.recordDigest||authorityInventory.lifecycleInventoryDigest!==inventory.recordDigest||authorityInventory.tupleDigest!==inventory.tupleDigest||authorityInventory.items.length!==1||authorityInventory.items[0].copyId!==tuple.copyId||authorityInventory.items[0].provider!==tuple.provider||authorityInventory.items[0].objectDigest!==tupleHash||!authority.conditions.includes(`lifecycle-inventory:${inventory.recordDigest}`)||!authority.conditions.includes(`tuple:${tupleHash}`)||jcs(authority.allowedCopyProviders)!==jcs([tuple.provider])||authority.terminalEventId!==trigger.triggerId)return pending('HUMAN_DISPOSITION_SCOPE_INVALID',boundaryAt);
      if(graph.recordClass==='RC-CORPUS-RAW-WORKING'){const rawGrant=parseCanonical(graph.rawGrantBytes[index]),rawErrors=rawGrantSchema(rawGrant);if(rawErrors.length||jcs(rawGrant.authority)!==jcs(authority)||authority.authorityType!=='raw-policy-grant'||authority.eraseMethod!=='cryptographic-erase'||authority.deadlineSeconds!==60)return pending('RAW_POLICY_GRANT_INVALID',boundaryAt);}else if(authority.authorityType!=='disposition-authorization'||authority.eraseMethod!=='provider-delete')return pending('HUMAN_DISPOSITION_AUTHORITY_INVALID',boundaryAt);
      const request=parseSigned(graph.protectedRequestsBytes[index]),requestPreimage=clone(request);delete requestPreimage.recordDigest;delete requestPreimage.signature;delete requestPreimage.immutableRequestDigest;const immutableRequestDigest=sha256(jcs(requestPreimage));if(request.immutableRequestDigest!==immutableRequestDigest||request.tenant!==graph.tenant||request.objectId!==graph.objectId||request.recordClass!==graph.recordClass||request.inventoryDigest!==inventory.recordDigest||request.tupleDigest!==inventory.tupleDigest||request.authorityId!==authority.authorityId||request.authorityDigest!==authority.recordDigest||request.providerBindingId!==tuple.providerBindingId||request.copyId!==tuple.copyId||request.copyKind!==tuple.copyKind||request.provider!==tuple.provider||request.account!==tuple.account||request.objectKey!==tuple.objectKey||request.versionId!==tuple.versionId||request.keyId!==tuple.keyId||request.targetExamRevision!==TARGET_REVISION||request.targetExamDigest!==TARGET_EXAM_SHA||request.retentionPolicyDigest!==RETENTION_POLICY_SHA||request.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||request.providerKeyRegistryDigest!==providerRegistryDigest||strictTime(request.requestedAt)<boundary)return pending('PROTECTED_LIFECYCLE_REQUEST_INVALID',boundaryAt);if(seenRequests.has(immutableRequestDigest)||seenKeys.has(request.idempotencyKey))return pending('LIFECYCLE_REQUEST_REUSED',boundaryAt);seenRequests.add(immutableRequestDigest);seenKeys.add(request.idempotencyKey);
      const replay=parseSigned(graph.replayLedgersBytes[index],'replay-authority'),head=parseSigned(graph.casHeadsBytes[index],'cas-authority'),reservation=parseSigned(graph.reservationsBytes[index],'cas-authority'),resource=parseCanonical(graph.providerResourceSnapshotsBytes[index]),receipt=parseCanonical(graph.providerReceiptsBytes[index]);if(verifyRecord(resource,binding.domain,resource.capturedAt)||verifyRecord(resource,binding.domain,graph.evaluationAt))return pending('LIFECYCLE_RESOURCE_PROVIDER_KEY_INVALID',boundaryAt);if(verifyRecord(receipt,binding.domain,receipt.providerTime)||verifyRecord(receipt,binding.domain,graph.evaluationAt))return pending('LIFECYCLE_RECEIPT_PROVIDER_KEY_INVALID',boundaryAt);
      if(replay.source!=='authoritative-replay-store'||replay.headId!==head.headId||replay.targetExamRevision!==TARGET_REVISION||replay.targetExamSha256!==TARGET_EXAM_SHA||replay.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(replay.snapshotAt)>strictTime(request.requestedAt)||strictTime(request.requestedAt)>=strictTime(replay.validThrough))return pending('LIFECYCLE_REPLAY_INVALID',boundaryAt);
      const replayMatch=replay.idempotencyKey===request.idempotencyKey;if(replayMatch&&(replay.requestDigest!==immutableRequestDigest||replay.status!=='committed'||!hex(replay.resultDigest,64)))return pending('LIFECYCLE_REPLAY_DRIFT',boundaryAt);if(!replayMatch)allReplay=false;
      if(head.source!=='authoritative-cas-store'||head.requestDigest!==immutableRequestDigest||head.tupleDigest!==inventory.tupleDigest||head.targetExamRevision!==TARGET_REVISION||head.targetExamSha256!==TARGET_EXAM_SHA||head.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||reservation.source!=='authoritative-cas-store'||reservation.headId!==head.headId||reservation.expectedHead!==head.head||reservation.idempotencyKey!==request.idempotencyKey||reservation.requestDigest!==immutableRequestDigest||reservation.tupleDigest!==inventory.tupleDigest||reservation.winner!==true||reservation.status!=='reserved')return pending('LIFECYCLE_CAS_INVALID',boundaryAt);
      if(resource.providerBindingId!==tuple.providerBindingId||resource.tenant!==graph.tenant||resource.provider!==tuple.provider||resource.account!==tuple.account||resource.copyId!==tuple.copyId||resource.objectKey!==tuple.objectKey||resource.versionId!==tuple.versionId||resource.keyId!==tuple.keyId||resource.tupleDigest!==inventory.tupleDigest||resource.inventoryDigest!==inventory.recordDigest||resource.requestDigest!==immutableRequestDigest||resource.providerKeyRegistryDigest!==providerRegistryDigest)return pending('LIFECYCLE_RESOURCE_SNAPSHOT_INVALID',boundaryAt);
      const receiptExpected={proofIssuer:binding.proofIssuer,proofType:binding.proofType,providerBindingId:tuple.providerBindingId,tenant:graph.tenant,recordClass:graph.recordClass,provider:tuple.provider,account:tuple.account,copyId:tuple.copyId,copyKind:tuple.copyKind,objectKey:tuple.objectKey,versionId:tuple.versionId,keyId:tuple.keyId,tupleDigest:inventory.tupleDigest,inventoryDigest:inventory.recordDigest,authorityId:authority.authorityId,authorityDigest:authority.recordDigest,immutableRequestDigest,idempotencyKey:request.idempotencyKey,effect:graph.recordClass==='RC-CORPUS-RAW-WORKING'?'crypto-erased':'deleted',status:'terminal-success',requestTime:request.requestedAt,targetExamRevision:TARGET_REVISION,targetExamDigest:TARGET_EXAM_SHA,retentionPolicyDigest:RETENTION_POLICY_SHA,authorizationPolicyDigest:AUTHORIZATION_POLICY_SHA,providerKeyRegistryDigest:providerRegistryDigest};
      for(const [field,value] of Object.entries(receiptExpected))if(receipt[field]!==value)return pending(`PROVIDER_RECEIPT_${field.toUpperCase()}_INVALID`,boundaryAt);
      if(strictTime(receipt.providerTime)<=strictTime(receipt.requestTime))return pending('PROVIDER_RECEIPT_TIME_INVALID',boundaryAt);if(seenTransactions.has(receipt.providerTransactionId))return pending('PROVIDER_TRANSACTION_DUPLICATE',boundaryAt);seenTransactions.add(receipt.providerTransactionId);
      if(strictTime(receipt.providerTime)>now)return pending('PROVIDER_RECEIPT_NOT_YET_CURRENT',boundaryAt);receiptDigests.push(receipt.recordDigest);orderedRows.push({tuple,authorityId:authority.authorityId,authorityDigest:authority.recordDigest,immutableRequestDigest,idempotencyKey:request.idempotencyKey,providerReceiptDigest:receipt.recordDigest});
    }
    const aggregate=parseSigned(graph.aggregateReceiptBytes,'provider'),tombstone=parseSigned(graph.tombstoneBytes);if(aggregate.tenant!==graph.tenant||aggregate.objectId!==graph.objectId||aggregate.recordClass!==graph.recordClass||aggregate.inventoryDigest!==inventory.recordDigest||aggregate.tupleDigest!==inventory.tupleDigest||jcs(aggregate.orderedRows)!==jcs(orderedRows)||jcs(aggregate.receiptDigests)!==jcs(receiptDigests)||aggregate.allCopiesGone!==true||aggregate.providerKeyRegistryDigest!==providerRegistryDigest||aggregate.targetExamRevision!==TARGET_REVISION||aggregate.targetExamDigest!==TARGET_EXAM_SHA||aggregate.retentionPolicyDigest!==RETENTION_POLICY_SHA||aggregate.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(aggregate.recordedAt)<=Math.max(...graph.providerReceiptsBytes.map(bytes=>strictTime(parseCanonical(bytes).providerTime)))||strictTime(aggregate.recordedAt)>now)return pending('AGGREGATE_RECEIPT_INVALID',boundaryAt);
    if(tombstone.tenant!==graph.tenant||tombstone.objectId!==graph.objectId||tombstone.recordClass!==graph.recordClass||tombstone.inventoryDigest!==inventory.recordDigest||tombstone.tupleDigest!==inventory.tupleDigest||jcs(tombstone.orderedRows)!==jcs(orderedRows)||tombstone.aggregateDigest!==aggregate.recordDigest||tombstone.providerKeyRegistryDigest!==providerRegistryDigest||tombstone.targetExamRevision!==TARGET_REVISION||tombstone.targetExamDigest!==TARGET_EXAM_SHA||tombstone.retentionPolicyDigest!==RETENTION_POLICY_SHA||tombstone.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(tombstone.committedAt)<=strictTime(aggregate.recordedAt)||strictTime(tombstone.committedAt)>now)return pending('TOMBSTONE_INVALID',boundaryAt);
    return {state:'deleted-tombstoned',firstError:null,effects:allReplay?zeroEffects():{...zeroEffects(),lifecycle:1},boundaryAt,copyCount:inventory.copies.length,orderedReceiptDigest:sha256(jcs(receiptDigests))};
  }catch(error){return blocked(error.message);}
}

const humanSchema=compile202012(parseStrict(fs.readFileSync(path.join(dir,'schemas/HUMAN-AUTHORITY.schema.json'),'utf8')));
const rawGrantSchema=compileOffline('RAW-POLICY-GRANT.schema.json');
export {RETENTION_POLICY_PATH,RETENTION_POLICY_SHA,AUTHORIZATION_POLICY_PATH,AUTHORIZATION_POLICY_SHA};
const humanBindingFields=['authorityId','authorityType','organization','tenant','item','humanSubject','activeHat','targetExamRevision','targetExamDigest','retentionPolicyDigest','copyInventoryDigest','conditions','safeguards','allowedCopyProviders','sourceOriginalExcluded','deadlineSeconds','eraseMethod','decidedAt'];
export function humanAuthorityDecision(input){
  try{
    if(!input||typeof input!=='object'||!exactKeys(input,['authorityBytes','providerProofBytes','identityEvidenceBytes','qualificationEvidenceBytes','assignmentEvidenceBytes','inventoryBytes','authorizationPolicyBytes','retentionPolicyBytes','replayLedgerBytes','casHeadBytes','casReservationBytes','evaluationTime']))return deny('HUMAN_AUTHORITY_BUNDLE_SCHEMA_INVALID');
    if(input.authorizationPolicyBytes!==AUTHORIZATION_POLICY_BYTES||sha256(input.authorizationPolicyBytes)!==AUTHORIZATION_POLICY_SHA||input.retentionPolicyBytes!==RETENTION_POLICY_BYTES||sha256(input.retentionPolicyBytes)!==RETENTION_POLICY_SHA)return deny('HUMAN_AUTHORITY_POLICY_BYTES_MISMATCH');
    const authority=parseCanonical(input.authorityBytes),schemaErrors=humanSchema(authority);if(schemaErrors.length)return deny('HUMAN_AUTHORITY_SCHEMA_INVALID',{schemaErrors});if(verifyRecord(authority,'authority'))return deny('HUMAN_AUTHORITY_SIGNATURE_INVALID');
    const identity=parseSigned(input.identityEvidenceBytes,'provider'),qualification=parseSigned(input.qualificationEvidenceBytes,'provider'),assignment=parseSigned(input.assignmentEvidenceBytes,'assignment'),inventory=parseSigned(input.inventoryBytes),provider=parseSigned(input.providerProofBytes,'human-provider'),replay=parseSigned(input.replayLedgerBytes,'replay-authority'),head=parseSigned(input.casHeadBytes,'cas-authority'),reservation=parseSigned(input.casReservationBytes,'cas-authority');
    const now=strictTime(input.evaluationTime),times=['authenticatedAt','decidedAt','validFrom','expiresAt','qualificationValidThrough','assignmentValidThrough'].map(key=>strictTime(authority[key]));if(now===null||times.includes(null)||!(times[0]<=times[1]&&times[1]<=now&&times[2]<=now&&now<times[3]&&now<times[4]&&now<times[5]))return deny('HUMAN_AUTHORITY_TIME_INVALID');
    if(authority.version!=='steer-human-authority/v3'||authority.organization!=='steer-platform'||authority.tenant!=='steer-platform'||authority.item!=='0001-flight-deck-foundation'||authority.targetExamRevision!==TARGET_REVISION||authority.targetExamDigest!==TARGET_EXAM_SHA||authority.retentionPolicyPath!==RETENTION_POLICY_PATH||authority.retentionPolicyDigest!==RETENTION_POLICY_SHA||authority.authorizationPolicyPath!==AUTHORIZATION_POLICY_PATH||authority.authorizationPolicyRevision!==TARGET_REVISION||authority.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA)return deny('HUMAN_AUTHORITY_IMMUTABLE_TARGET_MISMATCH');
    if(identity.recordDigest!==authority.identityEvidenceDigest||identity.organization!==authority.organization||identity.tenant!==authority.tenant||identity.humanSubject!==authority.humanSubject||identity.issuer!==authority.identityIssuer||identity.status!=='verified-active'||strictTime(identity.verifiedAt)>now)return deny('IDENTITY_EVIDENCE_BINDING_MISMATCH');
    if(qualification.recordDigest!==authority.qualificationEvidenceDigest||qualification.organization!==authority.organization||qualification.tenant!==authority.tenant||qualification.humanSubject!==authority.humanSubject||qualification.qualification!=='qualified-current'||qualification.issuer!=='records-board'||strictTime(qualification.validThrough)!==strictTime(authority.qualificationValidThrough))return deny('QUALIFICATION_EVIDENCE_BINDING_MISMATCH');
    if(assignment.recordDigest!==authority.assignmentEvidenceDigest||assignment.organization!==authority.organization||assignment.tenant!==authority.tenant||assignment.humanSubject!==authority.humanSubject||assignment.activeHat!==authority.activeHat||assignment.item!==authority.item||assignment.targetExamRevision!==authority.targetExamRevision||assignment.targetExamDigest!==authority.targetExamDigest||assignment.status!=='current'||strictTime(assignment.validThrough)!==strictTime(authority.assignmentValidThrough))return deny('ASSIGNMENT_EVIDENCE_BINDING_MISMATCH');
    if(inventory.recordDigest!==authority.copyInventoryDigest||inventory.organization!==authority.organization||inventory.tenant!==authority.tenant||inventory.item!==authority.item||!Array.isArray(inventory.items)||inventory.items.length===0||new Set(inventory.items.map(row=>row.copyId)).size!==inventory.items.length)return deny('INVENTORY_BINDING_MISMATCH');
    const authorityBindingDigest=sha256(jcs(Object.fromEntries(humanBindingFields.map(key=>[key,authority[key]]))));if(provider.authorityBindingDigest!==authorityBindingDigest||provider.providerRecordId!==authority.providerRecordId||provider.humanSubject!==authority.humanSubject||provider.decision!=='authorized'||provider.recordDigest!==authority.providerProofDigest)return deny('PROVIDER_PROOF_BINDING_MISMATCH');
    if(!exactKeys(replay,['ledgerId','source','status','idempotencyKey','requestDigest','resultDigest','headId','snapshotAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'])||replay.source!=='authoritative-replay-store'||!['unused','committed'].includes(replay.status)||replay.targetExamRevision!==TARGET_REVISION||replay.targetExamSha256!==TARGET_EXAM_SHA||replay.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(replay.snapshotAt)>now||now>=strictTime(replay.validThrough))return deny('AUTHORITY_REPLAY_INVALID');
    if(replay.idempotencyKey===authority.idempotencyKey)return deny(replay.requestDigest===authority.recordDigest?'AUTHORITY_REPLAY':'AUTHORITY_REPLAY_DRIFT');
    if(!exactKeys(head,['headId','source','head','previousHead','sequence','snapshotAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'])||head.source!=='authoritative-cas-store'||head.headId!==replay.headId||head.head!==authority.casHead||head.targetExamRevision!==TARGET_REVISION||head.targetExamSha256!==TARGET_EXAM_SHA||head.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(head.snapshotAt)>now||now>=strictTime(head.validThrough))return deny('AUTHORITY_CAS_HEAD_INVALID');
    if(!exactKeys(reservation,['reservationId','source','headId','expectedHead','idempotencyKey','requestDigest','authorityDigest','winner','status','recordedAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'])||reservation.source!=='authoritative-cas-store'||reservation.headId!==head.headId||reservation.expectedHead!==authority.casHead||reservation.idempotencyKey!==authority.idempotencyKey||reservation.requestDigest!==authority.recordDigest||reservation.authorityDigest!==authority.recordDigest||reservation.winner!==true||reservation.status!=='reserved'||reservation.targetExamRevision!==TARGET_REVISION||reservation.targetExamSha256!==TARGET_EXAM_SHA||reservation.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(reservation.recordedAt)<strictTime(head.snapshotAt)||strictTime(reservation.recordedAt)>now||now>=strictTime(reservation.validThrough))return deny('AUTHORITY_CAS_LOSER');
    return {decision:'ALLOW',firstError:null,effects:zeroEffects(),consumedRecordIds:[authority.authorityId,provider.providerRecordId,identity.evidenceId,qualification.evidenceId,assignment.assignmentId,inventory.inventoryId,replay.ledgerId,head.headId,reservation.reservationId]};
  }catch(error){return deny(error.message);}
}
const migrationSchema=compile202012(parseStrict(fs.readFileSync(path.join(dir,'schemas/MIGRATION-EVIDENCE.schema.json'),'utf8')));
const expectedMigrationTruth=(graph,position,effectCount)=>{const matrix=graph.matrix,phase=matrix.phase==='contract'?'contract':matrix.phase==='expand'?'expand':'backfill',batch=matrix.phase.includes('batch-2')?'batch-2':'batch-1',checkpoint=matrix.phase.includes('checkpoint-2')?'checkpoint-2':'checkpoint-1',readers={'old-read':`${graph.oldAppVersion}:old-shape-readable`,'old-write':'no-read','new-read':`${graph.newAppVersion}:new-shape-readable`,'new-write':'no-read','old-new-concurrent':`${graph.oldAppVersion}+${graph.newAppVersion}:same-row-readable`},writers={'old-read':'no-write','old-write':`${graph.oldAppVersion}:dual-write`,'new-read':'no-write','new-write':`${graph.newAppVersion}:new-write`,'old-new-concurrent':`${graph.oldAppVersion}+${graph.newAppVersion}:serialized-write`},semantic={schemaState:position==='pre'||effectCount===0?graph.schemaFrom:matrix.rollback==='none'?`${graph.schemaTo}:${phase}`:`${graph.schemaFrom}:restored-${matrix.rollback}`,readerObservation:readers[matrix.interleaving],writerObservation:writers[matrix.interleaving],rollbackState:matrix.rollback==='none'?'not-requested':`${matrix.rollback}:${position==='post'&&effectCount?'restored':'armed'}`,checkpointState:`${phase}:${batch}:${checkpoint}:${position}:effect-${effectCount}`,recoverySource:matrix.rollback==='none'?'not-required':'verified-provider-backup'};return {itemDigest:H64('1'),signatureDigest:H64('2'),attemptDigest:H64('3'),auditDigest:H64('4'),releaseDigest:H64('5'),evidenceDigest:H64('6'),dataDigest:position==='pre'||effectCount===0?H64('7'):sha256(jcs({matrix,semantic})),...semantic,transitionDigest:sha256(jcs(semantic))};};
function migrationDecisionCore(input){
  try{
    const graph=parseCanonical(input),schemaErrors=migrationSchema(graph);if(schemaErrors.length)return blocked('MIGRATION_SCHEMA_INVALID',{schemaErrors,journalEffects:0});
    for(const [record,domain] of [[graph,'money'],[graph.plan,'record'],[graph.providerRequest,'record'],[graph.journal,'record'],[graph.providerResult,'provider'],[graph.backup,'provider'],[graph.restoration,'provider']])if(verifyRecord(record,domain))return blocked('MIGRATION_SIGNATURE_INVALID',{journalEffects:0});
    const tuple=`${graph.schemaFrom}:${graph.oldAppVersion}:${graph.newAppVersion}`,matrixDigest=sha256(jcs(graph.matrix)),phase=graph.matrix.phase==='contract'?'contract':graph.matrix.phase==='expand'?'expand':'backfill',batch=graph.matrix.phase.includes('batch-2')?'batch-2':'batch-1',checkpoint=graph.matrix.phase.includes('checkpoint-2')?'checkpoint-2':'checkpoint-1';
    if(!['schema-v1:app-v1:app-v2','schema-v2:app-v2:app-v3'].includes(tuple)||graph.matrix.version!==tuple||graph.matrixDigest!==matrixDigest||graph.executionId!==`migration-${matrixDigest.slice(0,16)}`||graph.phase!==phase)return blocked('MIGRATION_VERSION_OR_MATRIX_MISMATCH',{journalEffects:0});
    const plan=graph.plan,request=graph.providerRequest,journal=graph.journal,result=graph.providerResult;
    if(plan.executionId!==graph.executionId||plan.matrixDigest!==matrixDigest||plan.organization!==graph.organization||plan.tenant!==graph.tenant||plan.database!==graph.database||plan.schema!==graph.schema||plan.schemaFrom!==graph.schemaFrom||plan.schemaTo!==graph.schemaTo||plan.oldAppVersion!==graph.oldAppVersion||plan.newAppVersion!==graph.newAppVersion||plan.phase!==phase||plan.batch!==batch||plan.checkpoint!==checkpoint||!plan.supportedReaders.includes(graph.oldAppVersion)||!plan.supportedReaders.includes(graph.newAppVersion)||!plan.supportedWriters.includes(graph.oldAppVersion)||!plan.supportedWriters.includes(graph.newAppVersion))return blocked('MIGRATION_PLAN_BINDING_MISMATCH',{journalEffects:0});
    if(request.executionId!==graph.executionId||request.matrixDigest!==matrixDigest||request.planDigest!==plan.recordDigest||request.organization!==graph.organization||request.tenant!==graph.tenant||request.schemaFrom!==graph.schemaFrom||request.schemaTo!==graph.schemaTo||request.oldAppVersion!==graph.oldAppVersion||request.newAppVersion!==graph.newAppVersion||request.phase!==phase||request.batch!==batch||request.checkpoint!==checkpoint||request.interleaving!==graph.matrix.interleaving||request.interruption!==graph.matrix.interruption||request.rollback!==graph.matrix.rollback||request.idempotency!==graph.matrix.idempotency||request.idempotencyKey!==graph.idempotencyKey||request.preTruthDigest!==sha256(jcs(graph.preTruth)))return blocked('MIGRATION_REQUEST_BINDING_MISMATCH',{journalEffects:0});
    const expectedPre=expectedMigrationTruth(graph,'pre',0),expectedPost=expectedMigrationTruth(graph,'post',journal.effectCount);if(jcs(graph.preTruth)!==jcs(expectedPre)||jcs(graph.postTruth)!==jcs(expectedPost))return blocked('MIGRATION_OBSERVED_STATE_INVALID',{journalEffects:0});
    if(journal.executionId!==graph.executionId||journal.matrixDigest!==matrixDigest||journal.planDigest!==plan.recordDigest||journal.requestDigest!==request.recordDigest||journal.journalId!==`journal-${matrixDigest.slice(0,12)}`||journal.batch!==batch||journal.checkpoint!==checkpoint||journal.state!==graph.matrix.phase||journal.preStateDigest!==graph.preTruth.transitionDigest||journal.postStateDigest!==graph.postTruth.transitionDigest)return blocked('MIGRATION_JOURNAL_BINDING_MISMATCH',{journalEffects:0});
    if(result.requestId!==request.requestId||result.executionId!==graph.executionId||result.requestDigest!==request.recordDigest||result.journalDigest!==journal.recordDigest||result.postTruthDigest!==sha256(jcs(graph.postTruth))||result.effectCount!==journal.effectCount)return blocked('MIGRATION_PROVIDER_RESULT_BINDING_MISMATCH',{journalEffects:0});
    if(graph.matrix.phase==='contract'){if(!graph.cleanupAuthorization)return blocked('MIGRATION_CLEANUP_SCOPE_MISMATCH',{journalEffects:0});const humanResult=humanAuthorityDecision(graph.cleanupAuthorization);if(humanResult.decision!=='ALLOW')return blocked(`MIGRATION_CLEANUP_${humanResult.firstError}`,{journalEffects:0});const cleanup=parseCanonical(graph.cleanupAuthorization.authorityBytes),cleanupInventory=parseCanonical(graph.cleanupAuthorization.inventoryBytes),requiredConditions=[`migration-execution:${graph.executionId}`,`plan:${plan.recordDigest}`,`backup:${graph.backup.digest}`,`columns:${sha256(jcs(plan.columns))}`,`operations:${sha256(jcs(plan.dataOperations))}`,`tenants:${sha256(jcs(plan.affectedTenants))}`];if(cleanup.authorityType!=='disposition-authorization'||cleanup.eraseMethod!=='provider-delete'||cleanup.terminalEventId!==plan.planId||jcs(cleanup.allowedCopyProviders)!==jcs(['migration-provider'])||requiredConditions.some(value=>!cleanup.conditions.includes(value))||cleanupInventory.executionId!==graph.executionId||cleanupInventory.planDigest!==plan.recordDigest||cleanupInventory.backupDigest!==graph.backup.digest)return blocked('MIGRATION_CLEANUP_SCOPE_MISMATCH',{journalEffects:0});}
    if(['partial','unknown'].includes(result.status))return blocked('MIGRATION_PROVIDER_NOT_TERMINAL',{journalEffects:0});if(graph.backup.executionId!==graph.executionId||graph.backup.verified!==true||graph.restoration.executionId!==graph.executionId||graph.restoration.backupId!==graph.backup.backupId||graph.restoration.restoredDigest!==graph.backup.digest||graph.restoration.result!=='identical')return blocked('MIGRATION_RESTORATION_INVALID',{journalEffects:0});
    if(graph.matrix.idempotency==='different-byte-replay'||graph.matrix.idempotency==='two-runner-loser'||graph.matrix.interruption==='before-effect')return {state:'safe-non-result',firstError:graph.matrix.idempotency==='different-byte-replay'?'MIGRATION_REPLAY_DRIFT':graph.matrix.idempotency==='two-runner-loser'?'MIGRATION_CAS_LOER':'MIGRATION_INTERRUPTED_BEFORE_EFFECT',effects:zeroEffects(),journalEffects:0,tupleKey:jcs(graph.matrix)};if(graph.matrix.idempotency==='same-byte-replay')return {state:'replay-noop',firstError:null,effects:zeroEffects(),journalEffects:1,tupleKey:jcs(graph.matrix)};if(journal.effectCount!==1||result.status!=='committed'||graph.casWinner!==true)return blocked('MIGRATION_EXECUTION_INVALID',{journalEffects:0});return {state:'journaled',firstError:null,effects:{...zeroEffects(),migration:1},journalEffects:1,tupleKey:jcs(graph.matrix)};
  }catch(error){return blocked(error.message,{journalEffects:0});}
}
export function migrationDecision(input){
  try{
    const graph=parseCanonical(input);
    const result=migrationDecisionCore(input);
    return {...result,inputDigest:sha256(input),resultDigest:sha256(jcs({state:result.state,firstError:result.firstError,effects:result.effects,journalEffects:result.journalEffects,tupleKey:result.tupleKey??null})),transition:result.state==='journaled'?`${graph.matrix.interruption}:${graph.matrix.rollback}->${graph.restoration.rehearsalId}`:result.state};
  }catch{return migrationDecisionCore(input);}
}
const recoverySchema=compile202012(parseStrict(fs.readFileSync(path.join(dir,'schemas/PROVIDER-RECOVERY.schema.json'),'utf8')));
const decodeSigned=(encoded,domain)=>parseSigned(Buffer.from(encoded,'base64').toString('utf8'),domain);
export const RECOVERY_CUTS=['before-provider-effect','after-provider-effect-before-acknowledgement','after-acknowledgement-before-webhook','after-webhook-before-fetch','after-fetch-before-spool-fsync','after-spool-fsync-before-WORM-acknowledgement','after-WORM-before-projection','after-projection'];
export const RECOVERY_CORRUPTIONS=['missing','stale','partial','corrupt','unavailable','wrong-tenant','wrong-key','duplicated','reordered','provider-timestamp-regressed','cursor-gapped','ID-colliding','mapped-many-to-one','identity-proof-missing','artifact-mismatched','policy-mismatched','loss-during-mapping','loss-during-independent-verification','retry-request-drift','arbitrary-policy-digest','opaque-identity','generic-decision','cut-scope-mismatch','class-scope-mismatch','revision-scope-mismatch'];
export function recoveryDecision(input){
  const fail=(firstError,extra={})=>({outcome:'RECOVERY_INCOMPLETE',firstError,effects:zeroEffects(),...extra});
  try{
    const recovery=parseCanonical(input),schemaErrors=recoverySchema(recovery);if(schemaErrors.length)return fail('RECOVERY_SCHEMA_INVALID',{schemaErrors});if(verifyRecord(recovery))return fail('RECOVERY_SIGNATURE_INVALID');
    const recoveryAnchor=TRUST_REGISTRY.bindings.find(binding=>binding.domain==='recovery-provider'),scopeFields=['cut','organization','tenant','repositoryId','item','recordClass','providerId','providerAccountId','targetExamRevision','targetExamDigest','implementationRevision','retentionPolicyPath','policyDigest','providerProofType'],scope=Object.fromEntries(scopeFields.map(key=>[key,recovery[key]]));if(recovery.targetExamRevision!==TARGET_REVISION||recovery.targetExamDigest!==TARGET_EXAM_SHA||recovery.implementationRevision!==H40('e')||recovery.retentionPolicyPath!==RETENTION_POLICY_PATH||recovery.policyDigest!==RETENTION_POLICY_SHA||recovery.providerProofType!=='provider-native-worm-journal'||recovery.providerTrustAnchorDigest!==sha256(recoveryAnchor.publicKeyHex))return fail('RECOVERY_AUTHORITY_SCOPE_INVALID');
    const identity=decodeSigned(recovery.identityEvidenceBytes,'provider');if(identity.organization!==recovery.organization||identity.tenant!==recovery.tenant||identity.providerId!==recovery.providerId||identity.providerAccountId!==recovery.providerAccountId||identity.subject!=='service:provider-recovery'||identity.status!=='verified-active'||strictTime(identity.verifiedAt)===null)return fail('RECOVERY_IDENTITY_INVALID');
    const stage=RECOVERY_CUTS.indexOf(recovery.cut);if(stage<0)return fail('RECOVERY_CUT_UNKNOWN');if(recovery.providerEffect!==(stage>=1)||recovery.providerAcknowledged!==(stage>=2))return fail('RECOVERY_CUT_STATE_INVALID');const expectedSources={providerJournal:stage>=2,webhook:stage>=3,fetch:stage>=4,spool:stage>=5,worm:stage>=6,projection:stage>=7};if(jcs(recovery.availableSources)!==jcs(expectedSources))return fail('RECOVERY_CUT_SOURCE_UNREACHABLE');if(stage<2)return {outcome:'UNKNOWN_RECONCILE_PROVIDER',firstError:null,effects:zeroEffects(),cut:recovery.cut};
    const journal=decodeSigned(recovery.providerJournalBytes,'recovery-provider'),exported=decodeSigned(recovery.exportedRecordBytes,'recovery-provider'),restored=decodeSigned(recovery.restoredRecordBytes,'record'),verifier=decodeSigned(recovery.independentVerifierBytes,'verifier');for(const value of [journal,exported,restored,verifier])for(const field of scopeFields)if(value[field]!==scope[field])return fail('RECOVERY_AUTHORITY_SCOPE_INVALID');if(journal.keyId!=='recovery-provider-key-v1'||journal.providerTrustAnchorDigest!==recovery.providerTrustAnchorDigest||journal.identityDigest!==identity.recordDigest||exported.identityDigest!==identity.recordDigest||restored.identityDigest!==identity.recordDigest||verifier.identityDigest!==identity.recordDigest||!Array.isArray(journal.records)||journal.records.length===0)return fail('RECOVERY_RECORD_BINDING_INVALID');
    for(const [index,record] of journal.records.entries()){for(const field of scopeFields)if(record[field]!==scope[field])return fail('RECOVERY_RECORD_SCOPE_INVALID');const decisionBytes=Buffer.from(record.decisionBytesBase64,'base64'),gitObjectBytes=Buffer.from(record.gitObjectBytesBase64,'base64');let decision;try{decision=parseCanonical(decisionBytes.toString('utf8'));}catch{return fail('RECOVERY_DECISION_BYTES_INVALID');}if(record.cursor!==index+1||record.identityDigest!==identity.recordDigest||record.decisionDigest!==sha256(decisionBytes)||record.artifactDigest!==sha256(gitObjectBytes)||decision.cursor!==record.cursor||decision.decisionId!==`decision-${record.cursor}`||decision.outcome!=='approved'||decision.identityDigest!==identity.recordDigest||scopeFields.some(field=>decision[field]!==scope[field])||strictTime(record.serverTimestamp)===null)return fail(record.cursor!==index+1?'RECOVERY_CURSOR_GAP':'RECOVERY_RECORD_BINDING_INVALID');if(index&&strictTime(record.serverTimestamp)<=strictTime(journal.records[index-1].serverTimestamp))return fail('RECOVERY_ORDER_OR_TIMESTAMP_INVALID');}
    if(new Set(journal.records.map(record=>record.providerRecordId)).size!==journal.records.length)return fail('RECOVERY_CANONICAL_BYTES_MISMATCH');const inventory=journal.records.map(record=>({cursor:record.cursor,providerRecordId:record.providerRecordId,decisionDigest:record.decisionDigest,artifactDigest:record.artifactDigest}));if(jcs(recovery.inventory)!==jcs(inventory)||jcs(recovery.preInventory)!==jcs(inventory))return fail('RECOVERY_INVENTORY_MISMATCH');const recordsDigest=sha256(jcs(journal.records));if(exported.journalDigest!==journal.recordDigest||exported.recordsDigest!==recordsDigest||jcs(exported.records)!==jcs(journal.records))return fail('RECOVERY_CANONICAL_BYTES_MISMATCH');
    if(!Array.isArray(restored.mappings)||restored.mappings.length!==journal.records.length||new Set(restored.mappings.map(row=>row.restoredId)).size!==restored.mappings.length||new Set(restored.mappings.map(row=>row.providerRecordId)).size!==restored.mappings.length)return fail('RECOVERY_MAPPING_INVALID');const post=[];for(const source of journal.records){const matches=restored.mappings.filter(row=>row.providerRecordId===source.providerRecordId);if(matches.length!==1||matches[0].decisionBytesBase64!==source.decisionBytesBase64||matches[0].gitObjectBytesBase64!==source.gitObjectBytesBase64||matches[0].gitObjectDigest!==source.artifactDigest)return fail('RECOVERY_RESTORED_BYTES_MISMATCH');post.push({cursor:source.cursor,providerRecordId:source.providerRecordId,decisionDigest:source.decisionDigest,artifactDigest:source.artifactDigest});}if(jcs(recovery.postInventory)!==jcs(post)||jcs(post)!==jcs(inventory))return fail('RECOVERY_INVENTORY_MISMATCH');
    if(!Number.isSafeInteger(recovery.startedAtMs)||!Number.isSafeInteger(recovery.verifiedAtMs)||!Number.isSafeInteger(recovery.rtoLimitMs)||recovery.verifiedAtMs<recovery.startedAtMs||recovery.verifiedAtMs-recovery.startedAtMs>recovery.rtoLimitMs)return fail('RECOVERY_RTO_EXCEEDED');if(recovery.retryIdentity.attempts!==3||recovery.retryIdentity.idempotencyKeys.some(value=>value!==recovery.retryIdentity.idempotencyKeys[0])||recovery.retryIdentity.requestDigests.some(value=>value!==recovery.retryIdentity.requestDigests[0]))return fail('RECOVERY_RETRY_IDENTITY_INVALID');const preimage=clone(recovery);delete preimage.independentVerifierBytes;delete preimage.recordDigest;delete preimage.signature;const recoveryBindingDigest=sha256(jcs(preimage)),evidenceDigest=sha256(jcs([journal.recordDigest,exported.recordDigest,restored.recordDigest]));if(verifier.recoveryBindingDigest!==recoveryBindingDigest||verifier.retrievedEvidenceDigest!==evidenceDigest||verifier.decision!=='verified')return fail('RECOVERY_INDEPENDENT_VERIFICATION_MISSING');return {outcome:'RECOVERY_VERIFIED',firstError:null,effects:zeroEffects(),cut:recovery.cut,rtoMs:recovery.verifiedAtMs-recovery.startedAtMs,inventoryDigest:sha256(jcs(inventory))};
  }catch(error){return fail(error.message);}
}
const moneyRecord=bytes=>parseSigned(bytes,'money'),moneyScopes=['organization','tenant','product','pod','execution'],moneyClasses=['infrastructure','model'];
const spendCeilings={organization:{infrastructure:1_000_000_000_000n,model:200_000_000_000n},tenant:{infrastructure:1_000_000_000_000n,model:200_000_000_000n},product:{infrastructure:800_000_000_000n,model:150_000_000_000n},pod:{infrastructure:500_000_000_000n,model:100_000_000_000n},execution:{infrastructure:5_000_000_000n,model:2_000_000_000n}};
function validateAuthorization(record,decisionAt){if(record.version!=='steer-spend-authorization/v2'||record.organization!=='steer-platform'||record.tenant!=='steer-platform'||record.currency!=='USD'||record.periodKind!=='calendar-month-utc'||record.status!=='active'||!Array.isArray(record.allowedProviders)||!record.allowedProviders.length||!Array.isArray(record.allowedSkus)||!record.allowedSkus.length||jcs(record.allowedCostClasses.slice().sort())!==jcs(moneyClasses))throw new Error('SPEND_RECORD_INVALID');const times=[record.periodStart,record.periodEnd,record.effectiveAt,record.expiresAt,record.sealedAt,record.providerProof.recordedAt,decisionAt].map(strictTime);if(times.includes(null)||!(times[0]<=times[2]&&times[2]<=times[4]&&times[4]<=times[5]&&times[5]<=times[6]&&times[6]<times[3]&&times[3]===times[1]))throw new Error('SPEND_TIME_INVALID');const start=new Date(times[0]),end=new Date(times[1]),expectedEnd=Date.UTC(start.getUTCFullYear(),start.getUTCMonth()+1,1);if(start.getUTCDate()!==1||start.getUTCHours()!==0||start.getUTCMinutes()!==0||start.getUTCSeconds()!==0||times[1]!==expectedEnd)throw new Error('SPEND_PERIOD_INVALID');if(verifyRecord(record.providerProof,'provider'))throw new Error('SPEND_PROVIDER_PROOF_INVALID');const payload=clone(record);delete payload.recordDigest;delete payload.signature;delete payload.providerProof;if(record.providerProof.authorizationPayloadDigest!==sha256(jcs(payload))||record.providerProof.authorizationId!==record.authorizationId)throw new Error('SPEND_PROVIDER_PROOF_INVALID');for(const scope of moneyScopes)for(const kind of moneyClasses){const delta=BigInt(record.delta[scope][kind]),cumulative=BigInt(record.cumulative[scope][kind]);if(delta<0n||cumulative<0n||cumulative>spendCeilings[scope][kind])throw new Error('SPEND_AMOUNT_INVALID');}for(const kind of moneyClasses)for(let index=1;index<moneyScopes.length;index++)if(BigInt(record.cumulative[moneyScopes[index]][kind])>BigInt(record.cumulative[moneyScopes[index-1]][kind]))throw new Error('SPEND_SCOPE_HIERARCHY_INVALID');return record;}
export function spendDecision(input){
  try{
    const graph=parseCanonical(input);if(!exactKeys(graph,['authorizationChainBytes','consumerBytes','replayLedgerBytes','casHeadBytes','casReservationBytes','decisionAt'])||!Array.isArray(graph.authorizationChainBytes)||!graph.authorizationChainBytes.length)return deny('SPEND_GRAPH_SCHEMA_INVALID');
    const chain=graph.authorizationChainBytes.map(bytes=>validateAuthorization(moneyRecord(bytes),graph.decisionAt)),consumer=moneyRecord(graph.consumerBytes),replay=parseSigned(graph.replayLedgerBytes,'replay-authority'),head=parseSigned(graph.casHeadBytes,'cas-authority'),reservation=parseSigned(graph.casReservationBytes,'cas-authority'),now=strictTime(graph.decisionAt);if(now===null)return deny('SPEND_TIME_INVALID');
    if(new Set(chain.map(row=>row.authorizationId)).size!==chain.length||new Set(chain.map(row=>row.recordDigest)).size!==chain.length)return deny('SPEND_LINEAGE_FORK');const root=chain[0];if(root.predecessorId!==null||root.predecessorDigest!==null)return deny('SPEND_ROOT_MISSING');for(const scope of moneyScopes)for(const kind of moneyClasses)if(BigInt(root.cumulative[scope][kind])!==BigInt(root.delta[scope][kind]))return deny('SPEND_ROOT_CUMULATIVE_INVALID');for(let index=1;index<chain.length;index++){const previous=chain[index-1],current=chain[index];if(current.predecessorId!==previous.authorizationId||current.predecessorDigest!==previous.recordDigest)return deny('SPEND_LINEAGE_GAP');for(const scope of moneyScopes)for(const kind of moneyClasses)if(BigInt(current.cumulative[scope][kind])!==checkedAdd(previous.cumulative[scope][kind],current.delta[scope][kind]))return deny('SPEND_CUMULATIVE_INVALID');}
    const current=chain.at(-1),consumerFields=['consumerId','authorizationId','authorizationDigest','organization','tenant','product','pod','execution','periodStart','periodEnd','provider','sku','costClass','idempotencyKey','casHead','targetExamRevision','targetExamDigest','authorizationPolicyDigest','requestedAt','requestDigest','recordDigest','signature'];if(!exactKeys(consumer,consumerFields)||consumer.authorizationId!==current.authorizationId||consumer.authorizationDigest!==current.recordDigest||moneyScopes.some(scope=>consumer[scope]!==current[scope])||consumer.periodStart!==current.periodStart||consumer.periodEnd!==current.periodEnd||!current.allowedProviders.includes(consumer.provider)||!current.allowedSkus.includes(consumer.sku)||!current.allowedCostClasses.includes(consumer.costClass)||consumer.targetExamRevision!==TARGET_REVISION||consumer.targetExamDigest!==TARGET_EXAM_SHA||consumer.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||consumer.requestedAt!==graph.decisionAt)return deny('SPEND_CONSUMER_SCOPE_DENIED');
    const consumerPreimage=clone(consumer);delete consumerPreimage.recordDigest;delete consumerPreimage.signature;delete consumerPreimage.requestDigest;if(consumer.requestDigest!==sha256(jcs(consumerPreimage)))return deny('SPEND_REQUEST_DIGEST_INVALID');
    const replayFields=['ledgerId','source','status','idempotencyKey','requestDigest','resultDigest','headId','snapshotAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];if(!exactKeys(replay,replayFields)||replay.source!=='authoritative-replay-store'||!['unused','committed'].includes(replay.status)||replay.targetExamRevision!==TARGET_REVISION||replay.targetExamSha256!==TARGET_EXAM_SHA||replay.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(replay.snapshotAt)>now||now>=strictTime(replay.validThrough))return deny('SPEND_REPLAY_INVALID');
    const headFields=['headId','source','head','sequence','snapshotAt','validThrough','requestDigest','tupleDigest','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];if(!exactKeys(head,headFields)||head.source!=='authoritative-cas-store'||head.headId!==replay.headId||head.head!==consumer.casHead||head.requestDigest!==consumer.requestDigest||head.tupleDigest!==current.recordDigest||head.targetExamRevision!==TARGET_REVISION||head.targetExamSha256!==TARGET_EXAM_SHA||head.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(head.snapshotAt)>now||now>=strictTime(head.validThrough))return deny('SPEND_CAS_HEAD_INVALID');
    if(replay.idempotencyKey===consumer.idempotencyKey){if(replay.requestDigest!==consumer.requestDigest)return deny('SPEND_REPLAY_DRIFT');if(replay.status==='committed'&&hex(replay.resultDigest,64))return {decision:'REPLAY_NOOP',firstError:null,effects:zeroEffects()};}
    const reservationFields=['reservationId','source','headId','expectedHead','idempotencyKey','requestDigest','authorizationDigest','consumerDigest','winner','status','recordedAt','validThrough','targetExamRevision','targetExamSha256','authorizationPolicyDigest','recordDigest','signature'];if(!exactKeys(reservation,reservationFields)||reservation.source!=='authoritative-cas-store'||reservation.headId!==head.headId||reservation.expectedHead!==head.head||reservation.idempotencyKey!==consumer.idempotencyKey||reservation.authorizationDigest!==current.recordDigest||reservation.consumerDigest!==consumer.recordDigest||reservation.requestDigest!==consumer.requestDigest||reservation.winner!==true||reservation.status!=='reserved'||reservation.targetExamRevision!==TARGET_REVISION||reservation.targetExamSha256!==TARGET_EXAM_SHA||reservation.authorizationPolicyDigest!==AUTHORIZATION_POLICY_SHA||strictTime(reservation.recordedAt)<strictTime(head.snapshotAt)||strictTime(reservation.recordedAt)>now||now>=strictTime(reservation.validThrough))return deny('SPEND_CAS_LOSER');
    return {decision:'ALLOW',firstError:null,effects:zeroEffects(),authorizationId:current.authorizationId,headId:head.headId};
  }catch(error){return deny(error.message);}
}
function costDecisionLegacy(input){
  try{const graph=parseCanonical(input);if(!exactKeys(graph,['kind','authorizationBytes','priceBytes','recordsBytes','forecastScenarios','claimedDecision','decisionAt'])||!['forecast','invoice','reconciliation','aggregate'].includes(graph.kind))return deny('COST_GRAPH_SCHEMA_INVALID');const authorization=validateAuthorization(moneyRecord(graph.authorizationBytes),graph.decisionAt),price=moneyRecord(graph.priceBytes),records=graph.recordsBytes.map(moneyRecord);if(!records.length)return deny('COST_RECORDS_MISSING');if(!exactKeys(price,['priceRevision','provider','sku','currency','nanoUsdPerUnit','effectiveAt','expiresAt','recordDigest','signature'])||price.currency!=='USD'||!authorization.allowedProviders.includes(price.provider)||!authorization.allowedSkus.includes(price.sku)||price.priceRevision!==authorization.priceRevision||strictTime(price.effectiveAt)>strictTime(graph.decisionAt)||strictTime(graph.decisionAt)>=strictTime(price.expiresAt)||BigInt(price.nanoUsdPerUnit)<0n||BigInt(price.nanoUsdPerUnit)>MAX_NANOUSD)return deny('PRICE_RECORD_INVALID');let total=0n;for(const record of records){if(!exactKeys(record,[...['recordId','authorizationId','authorizationDigest','priceRevision','provider','sku','currency','periodStart','periodEnd','scopes','costClass','units','nanoUsdPerUnit','totalNanoUsd'],...(graph.kind==='invoice'?['providerInvoiceId','providerUsageId','billedAt','acknowledgedAt']:graph.kind==='reconciliation'?['providerInvoiceId','providerUsageId','invoiceAcknowledgedAt','reconciledAt']:[]),'recordDigest','signature'])||record.authorizationId!==authorization.authorizationId||record.authorizationDigest!==authorization.recordDigest||record.priceRevision!==price.priceRevision||record.provider!==price.provider||record.sku!==price.sku||record.currency!==price.currency||record.nanoUsdPerUnit!==price.nanoUsdPerUnit||record.periodStart!==authorization.periodStart||record.periodEnd!==authorization.periodEnd||!moneyScopes.every(scope=>record.scopes[scope]===authorization[scope])||!authorization.allowedCostClasses.includes(record.costClass))return deny('COST_BINDING_MISMATCH');const line=checkedMultiply(record.units,price.nanoUsdPerUnit);if(BigInt(record.totalNanoUsd)!==line)return deny('COST_TOTAL_MISMATCH');total=checkedAdd(total,line);}let derived='DENY';const close=strictTime(authorization.periodEnd),record=records[0];if(graph.kind==='forecast'){if(!Array.isArray(graph.forecastScenarios)||graph.forecastScenarios.length!==records.length||graph.forecastScenarios.some((row,index)=>!exactKeys(row,['recordId','lowUnits','expectedUnits','highUnits'])||row.recordId!==records[index].recordId||BigInt(row.lowUnits)<0n||BigInt(row.lowUnits)>BigInt(row.expectedUnits)||BigInt(row.expectedUnits)>BigInt(row.highUnits)||BigInt(row.highUnits)!==BigInt(records[index].units)))return deny('FORECAST_SCENARIO_INVALID');derived=moneyScopes.every(scope=>total<=BigInt(authorization.cumulative[scope][record.costClass]))?'ALLOW':'DENY';}else if(graph.forecastScenarios.length)return deny('FORECAST_SCENARIO_UNEXPECTED');if(graph.kind==='invoice'){const billed=strictTime(record.billedAt),acknowledged=strictTime(record.acknowledgedAt);if(nonempty(record.providerInvoiceId)&&nonempty(record.providerUsageId)&&billed!==null&&acknowledged!==null&&billed>=close&&acknowledged>=billed)derived='ALLOW';}if(graph.kind==='reconciliation'){const invoiceAt=strictTime(record.invoiceAcknowledgedAt),reconciled=strictTime(record.reconciledAt);if(nonempty(record.providerInvoiceId)&&nonempty(record.providerUsageId)&&invoiceAt!==null&&reconciled!==null&&invoiceAt>=close&&reconciled>=invoiceAt&&reconciled-invoiceAt<=86_400_000)derived='ALLOW';}if(graph.kind==='aggregate')derived='ALLOW';if(graph.claimedDecision!==derived)return deny('COST_DECISION_CONTRADICTION');if(derived==='DENY')return deny(graph.kind==='invoice'?'INVOICE_BEFORE_PERIOD_CLOSE':graph.kind==='reconciliation'?'RECONCILIATION_TIME_INVALID':'FORECAST_OVER_AUTHORIZATION');const quotient=total/10_000_000n,remainder=total%10_000_000n,rounded=quotient+(remainder>5_000_000n||remainder===5_000_000n&&quotient%2n===1n?1n:0n);return {decision:'ALLOW',firstError:null,effects:zeroEffects(),aggregateNanoUsd:total.toString(),roundedCents:rounded.toString()};}catch(error){return deny(error.message);}
}
const costEvidenceSchema=compiledSchema('COST-EVIDENCE.schema.json');
const providerCostCommon=(record,authorization,price)=>record.organization===authorization.organization&&record.tenant===authorization.tenant&&record.product===authorization.product&&record.pod===authorization.pod&&record.execution===authorization.execution&&record.authorizationId===authorization.authorizationId&&record.authorizationDigest===authorization.recordDigest&&record.provider===price.provider&&record.sku===price.sku&&record.periodStart===authorization.periodStart&&record.periodEnd===authorization.periodEnd&&record.targetExamRevision===TARGET_REVISION&&record.targetExamDigest===TARGET_EXAM_SHA&&record.implementationRevision===H40('e');
export function costDecision(input){
  try{
    const graph=parseCanonical(input);
    if(!exactKeys(graph,['kind','authorizationBytes','priceBytes','priceProviderProofBytes','recordsBytes','providerUsageRecordsBytes','providerInvoiceRecordsBytes','varianceBytes','reconciliationBytes','forecastScenarios','claimedDecision','decisionAt'])||!['forecast','invoice','reconciliation','aggregate'].includes(graph.kind))return deny('COST_GRAPH_SCHEMA_INVALID');
    const authorizationRecord=moneyRecord(graph.authorizationBytes),authorization=validateAuthorization(authorizationRecord,graph.kind==='forecast'||graph.kind==='aggregate'?graph.decisionAt:lifecycleIso(strictTime(authorizationRecord.periodEnd)-1000)),price=moneyRecord(graph.priceBytes),priceProof=parseSigned(graph.priceProviderProofBytes,'provider-usage'),records=graph.recordsBytes.map(moneyRecord),now=strictTime(graph.decisionAt);
    if(!records.length||!Array.isArray(graph.providerUsageRecordsBytes)||!Array.isArray(graph.providerInvoiceRecordsBytes)||new Set(records.map(record=>record.recordId)).size!==records.length||new Set(records.map(record=>record.recordDigest)).size!==records.length||now===null||!exactKeys(price,['priceRevision','provider','sku','currency','nanoUsdPerUnit','effectiveAt','expiresAt','targetExamRevision','targetExamDigest','implementationRevision','recordDigest','signature'])||price.currency!=='USD'||price.targetExamRevision!==TARGET_REVISION||price.targetExamDigest!==TARGET_EXAM_SHA||price.implementationRevision!==H40('e')||!authorization.allowedProviders.includes(price.provider)||!authorization.allowedSkus.includes(price.sku)||price.priceRevision!==authorization.priceRevision||strictTime(price.effectiveAt)>strictTime(authorization.periodStart)||strictTime(price.expiresAt)<strictTime(authorization.periodEnd)||BigInt(price.nanoUsdPerUnit)<0n||BigInt(price.nanoUsdPerUnit)>MAX_NANOUSD)return deny('PRICE_RECORD_INVALID');
    if(!exactKeys(priceProof,['proofId','priceDigest','provider','sku','priceRevision','recordedAt','targetExamRevision','targetExamDigest','recordDigest','signature'])||priceProof.priceDigest!==price.recordDigest||priceProof.provider!==price.provider||priceProof.sku!==price.sku||priceProof.priceRevision!==price.priceRevision||priceProof.targetExamRevision!==TARGET_REVISION||priceProof.targetExamDigest!==TARGET_EXAM_SHA||strictTime(priceProof.recordedAt)>now)return deny('PRICE_PROVIDER_PROOF_INVALID');
    let total=0n;
    for(const record of records){
      const schemaErrors=costEvidenceSchema(record);if(schemaErrors.length)return deny('COST_EVIDENCE_SCHEMA_INVALID',{schemaErrors});
      if(!providerCostCommon(record,authorization,price)||record.kind!==graph.kind||record.priceRevision!==price.priceRevision||record.currency!==price.currency||record.nanoUsdPerUnit!==price.nanoUsdPerUnit||!authorization.allowedCostClasses.includes(record.costClass))return deny('COST_BINDING_MISMATCH');
      const line=checkedMultiply(record.units,price.nanoUsdPerUnit);if(BigInt(record.totalNanoUsd)!==line)return deny('COST_TOTAL_MISMATCH');total=checkedAdd(total,line);
    }
    let derived='DENY';
    if(graph.kind==='forecast'){
      if(graph.providerUsageRecordsBytes.length||graph.providerInvoiceRecordsBytes.length||graph.varianceBytes||graph.reconciliationBytes||!Array.isArray(graph.forecastScenarios)||graph.forecastScenarios.length!==records.length||graph.forecastScenarios.some((row,index)=>!exactKeys(row,['recordId','source','unit','lowUnits','expectedUnits','highUnits','priceDigest','authorizationDigest','periodStart','periodEnd'])||row.recordId!==records[index].recordId||row.source!=='provider-price-and-frozen-assumption'||row.unit!=='billable-unit'||row.priceDigest!==price.recordDigest||row.authorizationDigest!==authorization.recordDigest||row.periodStart!==authorization.periodStart||row.periodEnd!==authorization.periodEnd||BigInt(row.lowUnits)<0n||BigInt(row.lowUnits)>BigInt(row.expectedUnits)||BigInt(row.expectedUnits)>BigInt(row.highUnits)||BigInt(row.highUnits)!==BigInt(records[index].units)))return deny('FORECAST_SCENARIO_INVALID');
      derived=moneyScopes.every(scope=>total<=BigInt(authorization.cumulative[scope][records[0].costClass]))?'ALLOW':'DENY';
    }else if(graph.kind==='aggregate'){
      if(graph.providerUsageRecordsBytes.length||graph.providerInvoiceRecordsBytes.length||graph.varianceBytes||graph.reconciliationBytes||graph.forecastScenarios.length)return deny('COST_EVIDENCE_UNEXPECTED');derived='ALLOW';
    }else{
      if(graph.forecastScenarios.length||graph.providerUsageRecordsBytes.length!==records.length||graph.providerInvoiceRecordsBytes.length!==records.length)return deny('PROVIDER_COST_EVIDENCE_MULTIPLICITY_INVALID');
      const usages=graph.providerUsageRecordsBytes.map(bytes=>parseSigned(bytes,'provider-usage')),invoices=graph.providerInvoiceRecordsBytes.map(bytes=>parseSigned(bytes,'provider-invoice'));if(new Set(usages.map(value=>value.usageId)).size!==usages.length||new Set(usages.map(value=>value.recordDigest)).size!==usages.length||new Set(invoices.map(value=>value.invoiceId)).size!==invoices.length||new Set(invoices.map(value=>value.recordDigest)).size!==invoices.length)return deny('PROVIDER_COST_EVIDENCE_DUPLICATE');
      const lineages=[];for(const record of records){const usageMatches=usages.filter(value=>value.recordDigest===record.providerUsageDigest),invoiceMatches=invoices.filter(value=>value.recordDigest===record.providerInvoiceDigest);if(usageMatches.length!==1||invoiceMatches.length!==1)return deny('COST_PROVIDER_LINEAGE_MISMATCH');const usage=usageMatches[0],invoice=invoiceMatches[0],invoiceBytes=Buffer.from(invoice.invoiceBytesBase64,'base64');if(!exactKeys(usage,['usageId','organization','tenant','product','pod','execution','authorizationId','authorizationDigest','provider','sku','periodStart','periodEnd','usageStartedAt','usageEndedAt','units','priceRevision','targetExamRevision','targetExamDigest','implementationRevision','recordedAt','recordDigest','signature'])||!providerCostCommon(usage,authorization,price)||usage.priceRevision!==price.priceRevision||usage.units!==record.units||usage.usageStartedAt!==authorization.periodStart||usage.usageEndedAt!==authorization.periodEnd||strictTime(usage.recordedAt)<strictTime(usage.usageEndedAt)||strictTime(usage.recordedAt)>now)return deny('PROVIDER_USAGE_INVALID');if(!exactKeys(invoice,['invoiceId','invoiceBytesBase64','invoiceDigest','usageId','usageDigest','organization','tenant','product','pod','execution','authorizationId','authorizationDigest','provider','sku','periodStart','periodEnd','units','totalNanoUsd','currency','targetExamRevision','targetExamDigest','implementationRevision','issuedAt','acknowledgedAt','recordDigest','signature'])||!providerCostCommon(invoice,authorization,price)||invoice.usageId!==usage.usageId||invoice.usageDigest!==usage.recordDigest||invoice.units!==usage.units||invoice.totalNanoUsd!==record.totalNanoUsd||invoice.currency!=='USD'||!invoiceBytes.length||sha256(invoiceBytes)!==invoice.invoiceDigest||record.providerInvoiceBytesDigest!==invoice.invoiceDigest||strictTime(invoice.issuedAt)<strictTime(authorization.periodEnd)||strictTime(invoice.acknowledgedAt)<strictTime(invoice.issuedAt)||strictTime(invoice.acknowledgedAt)>now)return deny('PROVIDER_INVOICE_INVALID');lineages.push({record,usage,invoice});}
      const providerUsageSet=new Set(lineages.map(value=>value.usage.recordDigest)),providerInvoiceSet=new Set(lineages.map(value=>value.invoice.recordDigest));if(providerUsageSet.size!==usages.length||providerInvoiceSet.size!==invoices.length)return deny('PROVIDER_COST_EVIDENCE_ORPHAN');const {record,usage,invoice}=lineages[0];
      if(graph.kind==='invoice'){
        if(graph.varianceBytes||graph.reconciliationBytes)return deny('COST_EVIDENCE_UNEXPECTED');derived='ALLOW';
      }else{
        if(!graph.varianceBytes||!graph.reconciliationBytes)return deny('RECONCILIATION_EVIDENCE_MISSING');
        const variance=moneyRecord(graph.varianceBytes),reconciliation=moneyRecord(graph.reconciliationBytes);
        if(!exactKeys(variance,['varianceId','invoiceDigest','usageDigest','ledgerDigest','providerTotalNanoUsd','ledgerTotalNanoUsd','varianceNanoUsd','status','recordedAt','recordDigest','signature'])||variance.invoiceDigest!==invoice.recordDigest||variance.usageDigest!==usage.recordDigest||variance.ledgerDigest!==record.recordDigest||BigInt(variance.providerTotalNanoUsd)-BigInt(variance.ledgerTotalNanoUsd)!==BigInt(variance.varianceNanoUsd)||variance.status!=='within-threshold'||strictTime(variance.recordedAt)<strictTime(invoice.acknowledgedAt))return deny('VARIANCE_EVIDENCE_INVALID');
        if(!exactKeys(reconciliation,['reconciliationId','predecessorInvoiceDigest','predecessorUsageDigest','predecessorLedgerDigest','varianceDigest','successorStatus','reconciledAt','recordDigest','signature'])||reconciliation.predecessorInvoiceDigest!==invoice.recordDigest||reconciliation.predecessorUsageDigest!==usage.recordDigest||reconciliation.predecessorLedgerDigest!==record.recordDigest||reconciliation.varianceDigest!==variance.recordDigest||reconciliation.successorStatus!=='reconciled'||strictTime(reconciliation.reconciledAt)<strictTime(invoice.acknowledgedAt)||strictTime(reconciliation.reconciledAt)-strictTime(invoice.acknowledgedAt)>86_400_000||strictTime(reconciliation.reconciledAt)>now)return deny('RECONCILIATION_LINEAGE_INVALID');
        derived='ALLOW';
      }
    }
    if(graph.claimedDecision!==derived)return deny('COST_DECISION_CONTRADICTION');
    if(derived==='DENY')return deny('FORECAST_OVER_AUTHORIZATION');
    const quotient=total/10_000_000n,remainder=total%10_000_000n,rounded=quotient+(remainder>5_000_000n||remainder===5_000_000n&&quotient%2n===1n?1n:0n);
    return {decision:'ALLOW',firstError:null,effects:zeroEffects(),aggregateNanoUsd:total.toString(),roundedCents:rounded.toString()};
  }catch(error){return deny(error.message);}
}

export const LIFECYCLE_EVENT_EXTRAS={
  'originator-session-opened':['sessionId','authenticatedAt'],'originator-draft-saved':['sessionId','commitId','contentSha256'],'originator-session-abandoned':['sessionId','reason','boundaryAt'],'record-committed':['recordSha256'],'item-closed':['closingRecordId','closingDecision'],'environment-retired':['releaseRailsRecordId','trafficDisabled','credentialsRevoked'],'run-terminal':['runId','terminalStatus'],'event-committed':['sourceEventId','sourceEventSha256'],'record-superseded':['predecessorRecordId','successorRecordId','successorSha256'],'corpus-source-authorized':['sourceAuthorityId','sourceObjectSha256','purpose','useAuthorizationId','useAuthorizationSha256','authorizedThrough','allowedDerivatives'],'corpus-use-authorization-revoked':['useAuthorizationId','revocationDecisionId','revokedAt','affectedCorpusVersions'],'corpus-sanitization-terminal':['corpusId','sourceObjectSha256','sanitizerRevision','inspectionRevision','result','reportSha256','rawWorkingCopyDisposition','rawDispositionReceiptSha256'],'corpus-version-accepted':['corpusId','corpusVersion','encryptedObjectSha256','manifestSha256','keyId','useAuthorizationId'],'corpus-version-superseded':['corpusId','corpusVersion','successorVersion','successorManifestSha256'],'corpus-retired':['corpusId','corpusVersion','retirementReason'],'hold-applied':['holdId','selectorsSha256','reasonAuthority'],'hold-released':['holdId','releaseAuthority'],'rebuild-requested':['requestId','selectorSha256','authorizationRecordId'],'reference-revocation-authorized':['authorizationRecordId','referenceInventorySha256','verificationBundleSha256','tombstoneRecordId'],'raw-working-policy-grant-signed':['policyGrantId','grantSha256','validFrom','expiresAt','safeguards','qualifiedOwnerSubject'],'derived-record-deleted':['parentCorpusId','parentCorpusVersion','derivedRecordId','derivedRecordClass','deletionReceiptSha256'],'expiry-due':['triggerEventId','computedExpiryAt'],'deletion-requested':['expiryEventId','copyInventorySha256','dispositionAuthorizationId'],'deletion-completed':['requestId','providerReceiptsSha256','allCopiesGone'],'tombstone-committed':['deletionEventId','tombstoneSha256'],'export-authorized':['exportAuthorizationId','selectorsSha256','purpose','recipient','expiresAt'],'export-completed':['exportId','exportSha256','inventorySha256','exportAuthorizationId']
};
export function makeLifecycleEvent(){throw new Error('SERIALIZED_FIXTURE_FACTORY_MOVED');}
export function makeHumanAuthority(){throw new Error('SERIALIZED_FIXTURE_FACTORY_MOVED');}
