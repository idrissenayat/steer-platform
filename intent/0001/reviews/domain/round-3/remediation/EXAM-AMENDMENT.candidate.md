# Gate 2 Exam amendment candidate: round-three executable closure

Item: `0001-flight-deck-foundation`

Status: **unsigned candidate; not canonical and not a Gate, build, release, production, migration, deletion, provider-access, or spending authorization**

Reviewed source target: commit `bcf472d4db9e4a95b483676869f01869423fdc95`, canonical Exam SHA-256 `84ad1d4c14d6614fe4b53509104e379e9ad6dff0462c96109901eefb1e02fd7f`. Frozen round-three exception brief SHA-256: `e87e433cbfe284ba592a79992b7cc7170c0e90bedefc273280a02999f5d56be6`.

This candidate does not edit the protected canonical Exam. If an authorized independent Exam author later incorporates it, that incorporation creates a new Exam revision and requires new exact-revision reviews and the human prerequisites stated below.

## 1. Exact supersession map

| Existing scope | Operation | Governing correction |
|---|---|---|
| Round-two section 3 permission schema reference and R4/R5 protected-path coverage | replace | section 2 and `PERMISSIONS-MANIFEST.candidate.json` version `steer-permissions-manifest/v12` |
| OR-10 and round-two section 4 sanitized-real oracle | replace executable contract | section 3 and `PRIVACY-DETECTOR-REGISTRY.candidate.json` |
| OR-22/A9.2 and round-two section 5 environment/evidence oracle | replace | section 4 and `ACCESSIBILITY-MATRIX-MANIFEST.candidate.json` |
| A8/A10.1 lifecycle trigger, human disposition authority, and raw grant | replace executable contract | sections 5 and 6 |
| A10.3 migration executable contract | replace | section 7 |
| A11.1 parsed spend semantics | replace executable contract | section 8 |
| A11.4 forecast and reconciliation timing | replace executable contract | section 9 |
| A12 provider-native recovery executable contract | replace | section 10 |
| Round-two validator current-working-tree Exam check | replace | section 11 |

All other clauses remain in force. A contradiction, missing artifact, stale digest, incomplete declared matrix, or failed semantic case blocks acceptance; it may not be waived by a passing aggregate.

## 2. Permission manifest and protected paths

The sole governing candidate permission artifact is `PERMISSIONS-MANIFEST.candidate.json`, exactly version `steer-permissions-manifest/v12`, bound to `TRUST-REGISTRY.candidate.json` by its exact SHA-256. It also binds the exact immutable target revision and the independently read `kit/policy/gates.json` path, bytes, and SHA-256. Every protected graph independently reads and byte-compares its governing authorization and retention policies; caller-supplied digests are never authority. Every validator MUST reject any other declared or supplied bytes before evaluating a request.

For protected App-authored Exam work, distinct negative rows MUST exercise `originator`, `platform-agent`, `worker`, `builder`, and a coherent `compromised-caller`. Retry and fallback are distinct pathways and MUST reuse the immutable actor, upstream credential, authority, delegation, assignment, selectors, exact path, target digest, request digest, idempotency key, replay head, and CAS reservation. A retry with identical committed bytes is `REPLAY_NOOP`; retry drift, fallback principal/provider substitution, or any non-Test-Agent origin denies before operational credential selection, installation-token exchange, provider request, or write. Every denial has the complete typed zero-effect ledger.

## 3. Sanitized-real graph and detector

The corpus graph contains exactly 20 unique, manifest-hashed prompt objects. It is closed and duplicate-free across source authority, source pool, use authorization, sanitizer run, independent inspection, prompt objects, encrypted corpus/version/key, baseline, shrink, provider/model/cache copies, raw working-copy inventory, per-copy disposition authorities and receipts, and production-versus-fixture destinations. Every edge binds organization, tenant, item, target Exam digest, policy digest, detector registry digest, sanitizer and inspector identities/revisions, source object/pool hashes, corpus/version, purpose, allowed derivative, destination, credential class, and applicable inventory digest. Missing, extra, duplicate, substituted, stale, cross-destination, or unbound nodes or edges reject the entire corpus.

Direct-identifier detection is determined only by the revision-bound `PRIVACY-DETECTOR-REGISTRY.candidate.json`. Input is Unicode NFKC, case folded, stripped of the complete governed Unicode `Cf` category (with no safe exceptions in this revision), and separator/obfuscation normalized as declared. Digit-only, extension, and international phone forms using either `+` or `00` country-code prefixes are recognized across non-US numbering plans with Unicode letter/number boundaries; seven through fifteen digits are accepted while sixteen-digit, embedded, and too-short precision-like strings remain non-matches. The closed policy classes are personal name, username/handle, email, phone, postal address, precise location, government identifier, account identifier, customer identifier, employee identifier, device identifier, IP address, MAC address, identity-bearing URL, biometric, signature, secret, token, credential, and free-text self-identification. Every class has positive, evasion, and boundary fixtures, including multiple non-US phone formats, isolate, embedding, and Arabic-letter-mark label splits. Detector or normalization drift is a hard failure.

The oracle recomputes named-entity, the declared normalized quasi-identifier key, every prompt's equivalence-class size, source-unique four-token result, `k >= 5`, top-1 linkage `<= 0.10`, and Wilson 95% upper bound `<= 0.25` from the frozen authorized source pool and prompt bytes; prompt objects contain no accepted `equivalenceClassSize` claim. Each temporary raw copy maps one-to-one to the complete source/corpus inventory and to one verified terminal receipt. An unrelated or partial receipt rejects.

## 4. Accessibility environment and exhaustive raw matrix

The environment registry is exactly:

1. Windows 11 + Chrome + JAWS;
2. Windows 11 + Firefox + NVDA;
3. macOS + Safari + VoiceOver;
4. iOS + Safari + VoiceOver;
5. Android + Chrome + TalkBack;
6. Windows 11 + Chrome, keyboard only; and
7. macOS + Safari, keyboard only.

Every OS, browser, and assistive-technology value is an exact nonempty version, with `none` permitted only for the two keyboard-only environments. Substitution, duplication, staleness, omission, or an unversioned component rejects the run.

`ACCESSIBILITY-MATRIX-MANIFEST.candidate.json` is the single route/Storybook surface, state, scenario, viewport, environment, and checkpoint registry. Its manifest, retrieved raw rows, and signed summary conform to the closed `ACCESSIBILITY-*` schemas. The oracle derives the full Cartesian key set for every manifest-applicable tuple and every `A01` through `A81`, and compares exact expected-key equality to the supplied serialized raw rows. Every row carries surface kind/ID, state, scenario, viewport dimensions, environment ID, OS/browser/AT names and versions, input mode, operator subject, start/end UTC timestamps, outcome, notes, evidence references, checkpoint-model digest, exact Exam revision, and exact implementation revision. Keys and evidence references are unique. Missing, extra, duplicate, substituted, or stale rows reject.

`not-applicable` requires nonempty checkpoint-specific human rationale and cited applicability evidence. All-not-applicable evidence is rejected. The signed summary is derived only after raw equality and contains counts and aggregate raw digest; claimed counts or decisions never override raw rows. The row batch is digest-bound by a separate signature. Verified identity, a current DHS Trusted Tester credential, current accessibility-specialist/platform-pod assignment, exact Exam and implementation revisions, and an independently signed provider result must all resolve to the same human subject and batch. The deterministic candidate fixture proves these controls but is not real Gate 3 manual evidence; a qualified platform-pod human must later execute and sign the actual UI matrix.

## 5. Exact policy lifecycle events and histories

Every lifecycle event is duplicate-key-rejecting RFC 8785 JSON conforming to `schemas/LIFECYCLE-EVENT.schema.json`, with `eventVersion=2` and the exact base and event-specific members ratified in retention policy v2. The semantic oracle additionally proves stable actor identity, active authority, organization/item/environment/class/revision/policy/correlation bindings, independently selected provider record/proof, server/provider timestamp authority, signature, strict ordering, unique event ID and unique canonical bytes before deriving expiry. Missing, extra, duplicate, malformed, stale, wrong-tenant, caller-timed, replayed, or equal-time out-of-order events produce `blocked-policy-conflict` and typed zero effects.

The complete 16-class policy table in `LIFECYCLE-POLICY-TABLE.candidate.json` and exact trigger/duration rules are executable. For each class the oracle derives the boundary from the signed trigger, then evaluates immutable retention, pre-expiry scheduling, active/released holds, reference revocation, parent caps, one complete current `HUMAN-AUTHORITY` decision graph per canonical tuple, and, for raw working data, a composed `RAW-POLICY-GRANT` that embeds that graph rather than a digest surrogate. It also requires a fresh exact inventory of at least two copies and three raw keys for raw working data, one protected immutable provider request, independently signed authoritative replay ledger, CAS head and winner reservation, provider-resource snapshot, provider-registry-selected terminal receipt per tuple, ordered aggregate receipt, and durable tombstone. Human identity, qualification, assignment, provider proof, replay and CAS are independently verified; the provider registry is byte-anchored to the candidate artifact and every provider key must be valid both when its evidence was signed and at the evaluation instant. Before, exact-boundary, one-second-after, and completed-disposition rows execute for every class; missing/duplicate/extra copies or receipts, reused request/idempotency, tuple/key/provider/policy/target mismatch, restored-version race, crash-before-aggregate, replay/CAS loss, malformed human or raw-grant graphs, caller signer, wrong/substituted provider registry or key, not-yet-valid or expired provider key, incomplete, early, stale, missing-grant, and missing-reference paths have typed zero effects. No caller-supplied boundary or boolean can substitute.

## 6. Human authority, rulings, dispositions, and raw grants

Every human ruling and destructive disposition authority uses `schemas/HUMAN-AUTHORITY.schema.json`. It binds verified immutable human subject, identity issuer and evidence digest, active hat, current qualification and assignment evidence/validity, session and authentication instant, server decision time, authorization-policy path/revision/digest, provider record, independently verified provider proof and trust-anchor digest, sequence, organization/tenant/item, target Exam revision/digest, retention policy path/digest, complete copy inventory and digest, holds/references, exact conditions, safeguards, allowed copy/provider set, source-original exclusion, terminal event, 60-second deadline, erase method, idempotency and CAS state. The signed human record and provider proof are verified with different independently selected trust anchors.

Absent, unverified, disabled, transferred, revoked, expired, unqualified, wrong-subject, wrong-hat, wrong-session, stale-policy/target, missing identity evidence, missing/invalid provider proof, replay, and race-loser authorities deny before provider or destructive effects. Replay state and CAS state are closed records conforming to `schemas/REPLAY-LEDGER.schema.json` and `schemas/CAS-HEAD.schema.json`, signed only by independent `replay-authority` and `cas-authority` trust domains and cross-bound to request, idempotency key, result, head lineage, current snapshot, target, and policy. `schemas/RAW-POLICY-GRANT.schema.json` carries all section 6 fields plus sanitizer/inspector revisions and permits only `RC-CORPUS-RAW-WORKING`; it cannot target a source-authority original.

The historical HR-01 records remain immutable evidence, but they do not satisfy this stronger envelope because they predate it. A new actual qualified-human ruling over final bytes is required after any canonical incorporation. The Test Agent cannot create, infer, or waive that ruling.

## 7. Forward-only migration execution

`schemas/MIGRATION-EVIDENCE.schema.json` closes the signed plan, separately signed destructive cleanup authorization, checkpoint journal, provider result, backup/PITR, restoration, and pre/post truth inventory. Opaque digests alone do not satisfy it. The plan names exact database/schema, from/to schema and application versions, columns/data operations, affected tenants, ordered batches/checkpoints, supported old/new reader and writer behaviors, application rollback states, backup, expected truth, and idempotency/CAS rules. Cleanup names exact destructive columns/data, tenants, expiry, backup/PITR evidence, rollback consequence, and human authority.

For every supported `(schema,old-app,new-app)` tuple, the executable matrix covers expand; all old/new reader/writer interleavings; every backfill batch/checkpoint; interruption immediately before and after each effect; retry and same-byte replay; two-runner winner/loser; application rollback before, during, and after backfill; contract refusal and authorized contract; stale/wrong-tenant/wrong-column cleanup; partial/unknown provider result; recovery; and restoration. Schema/application tuple, matrix digest, unique execution ID, phase/batch/checkpoint, plan, immutable provider request, journal, provider result, cleanup, backup/restoration, and pre/post truth form one signed successor graph. The oracle independently derives signed `schemaState`, reader and writer observations, rollback state, checkpoint state, recovery source, and transition digest for each matrix coordinate and byte-compares those values with the journal-bound pre/post state digests. Recovery and restore are separate signed state transitions with distinct version, phase, checkpoint, interleaving, rollback, execution, request, journal, backup/restoration, input digest, result digest, transition label, and expected typed effect. Static independently declared expectations reject byte-identical or result-identical semantic coverage. Contract cleanup consumes the complete human-authority graph and binds exact plan, backup, columns, data operations and tenants; a label or cleanup digest is never authority. Transplanted plan, request, journal, provider result, backup, cleanup, or truth cannot be accepted by resealing the outer record. The oracle compares exact data plus item, signature, attempt, audit, release, and evidence truth and requires one journaled result or a safe non-result. Denials have typed zero effects. Declared versus executed version, phase, interleaving, checkpoint, interruption, rollback, concurrency, idempotency, failure-kind, and transplant-kind sets MUST be equal.

## 8. Parsed spend successor and concurrency semantics

The validator parses and cryptographically verifies every signed spend record and provider proof before use. `periodKind=calendar-month-utc` means `periodStart` is exactly `YYYY-MM-01T00:00:00Z` and `periodEnd` is the first instant of the next UTC calendar month. The decision instant MUST be within effective/expiry and period bounds.

The predecessor chain is gapless, acyclic, forward-only, and ends at one current CAS winner. Every successor cumulative infrastructure and model total equals predecessor cumulative authorization plus the successor delta at each organization/product/pod/execution scope. The current decision additionally consumes an authoritative `REPLAY-LEDGER` record and authoritative CAS head and reservation signed by their dedicated trust domains. Those records bind exact immutable request bytes/digest, idempotency key, target, policy, current-head lineage, winner, result and evaluation time; consumer request bindings are independently recomputed before credential or paid request. Zeroing, rollback, fork, double count, superseded/revoked/stale record, concurrent loser, missing or ordinary-signer replay/CAS evidence, stale or drifted head/request, same-key/different-bytes replay, wrong tenant, malformed month, over-ceiling, or unapproved provider/SKU/cost-class rejects. A same-key/same-bytes committed replay is effect-free.

The `$1,000` infrastructure amount is only a Gate 1 ceiling; it never creates authority. Precision remains integer nanoUSD through allocation, reservation, accrual, usage, aggregation, and variance; banker rounding occurs only at the declared reporting boundary after aggregation.

## 9. Forecast and truthful post-close invoice reconciliation

Forecast decision is derived, never accepted: all signed assumptions, unit prices, quantities, five scope trees, infrastructure/model separation, current authorization, and Gate 1 ceiling are recomputed. Contradictory claimed decisions reject.

Usage `periodStart/periodEnd` is distinct from actual `usageStartedAt/usageEndedAt`, provider invoice `issuedAt`/`acknowledgedAt`, and reconciliation `reconciledAt`. Every accepted row conforms to the closed `schemas/COST-EVIDENCE.schema.json`; its authorization conforms to `schemas/SPEND-AUTHORIZATION.schema.json`. Forecast price proof and actual usage are independently signed by `provider-usage`, while immutable invoice bytes/digest and invoice facts are independently signed by `provider-invoice`. Usage timestamps equal the closed half-open period, invoice issue is at or after period close, and variance/reconciliation follows invoice acknowledgement no later than 24 hours. The oracle requires exact, duplicate-free multiplicity across ledger lines, provider usage records and provider invoice records, then compares the complete unique sets by provider/SKU/scope/authorization/price/units/nanoUSD and immutable record digests. Reordered complete multi-line sets pass; any duplicate ledger, usage, invoice, invoice ID, missing line or orphan record denies. The one-to-one graph binds provider/SKU, scopes, authorization, price revision, exact units and nanoUSD totals, target/implementation revisions, usage bytes, invoice bytes, ledger record, variance, predecessor/successor and timestamps. Missing/substituted proof, time or bytes, usage/invoice mismatch, orphan line, predecessor gap/fork, stale price reference, and late unavailable provider data deny. Before-close invoices reject; exactly-at-close and after-close invoices may pass.

## 10. Provider-native recovery

Every provider journal, exported record, restored record, and independent-verifier result validates against `schemas/PROVIDER-RECOVERY.schema.json`. Canonical serialized bytes bind the exact loss cut, corruption class, organization, tenant, repository/item, record class, provider/account/key, target and implementation revision, retention-policy path/digest, proof type, verified identity/policy digests, gapless cursor, stable provider record ID, complete decision bytes and digest, artifact digest, mapping, Git object bytes/digest, exact pre/post inventory, and retry identity. The recovery authority and every nested family record repeat and agree on that scope; generic proof labels, opaque identity strings, arbitrary policy digests, or caller-selected cut/class/revision values cannot authorize recovery.

The oracle executes all eight distinct loss cuts: before provider effect; after effect before acknowledgement; after acknowledgement before webhook; after webhook before fetch; after fetch before spool fsync; after spool fsync before WORM acknowledgement; after WORM before projection; and after projection. It derives reachable durability sources from the cut, derives inventory from the signed provider journal and restored bytes, checks byte equality through export/mapping/Git restoration, measures numeric RTO against the one-hour limit, and verifies a separately signed independent-verifier result using a distinct trust anchor. The exact twenty-five corruption classes are missing, stale, partial, corrupt, unavailable, wrong tenant, wrong key, duplicated, reordered, regressed provider timestamp, cursor gap, ID collision, many-to-one mapping, missing identity proof, artifact mismatch, policy mismatch, mapping loss, independent-verification loss, retry-request drift, arbitrary policy, opaque identity, generic decision proof, wrong loss-cut scope, wrong corruption-class scope, and wrong target/implementation revision scope. Every failed rehearsal has typed zero effects.

## 11. Immutable exact-revision validation

Historical packages verify their declared target bytes with `git show <declared-target-revision>:<declared-path>` and MUST NOT compare a historical digest to the current working tree. The round-three validator similarly reads the source canonical Exam and frozen evidence from commit `bcf472d4db9e4a95b483676869f01869423fdc95`. Full mode additionally hash-verifies every artifact in the new remediation manifest; fixtures-only mode executes the same declared semantic matrices without manifest hashing. Neither mode depends on a mutable current canonical-Exam digest.

`NORMATIVE-EXECUTION-INVENTORY.candidate.json` is separately anchored to the immutable Exam and retention-policy digests and enumerates the mandatory trust, authorization, privacy, accessibility, lifecycle, human, migration, recovery, spend, cost, and schema dimensions. Before declaring or executing a test, the validator proves every corresponding control dimension equals this normative inventory. The validator also compiles every local schema through `offline-schema-registry.candidate.mjs`, resolves all local `$ref` targets, and executes positive and hostile composed `RAW-POLICY-GRANT` instances.

The validator refuses duplicate fixture IDs, duplicate required kinds, undeclared execution, or a declared ID/kind/version/dimension that did not execute. A pass reports exact counts, matrix dimensions, and the immutable source target. It is technical candidate evidence only.

## 12. Round-three Critic R2 counterexample closure

The fixed R2 input remains immutable at SHA-256 `8016437ef28f488687feec2e4550f23dfb5cddf264d32547372b3a1f2703c9cb`. Its direct counterexamples are executable IDs, not prose-only claims: one-use false and extra/wildcard resource sets deny; every private trust domain rejects public signing; composed raw grant resolves offline and rejects null/wrong-class authority; coherent Exam/policy/assignment substitutions deny; wrong-source/expired/empty-derivative privacy authorities and all four named detector evasions reject or detect; migration signed-edge transplants block; missing spend roots and currency/rate substitutions deny; qualified accessibility identity/batch/provider substitutions reject; every lifecycle class traverses disposition evidence; and all eight recovery cuts plus twenty-five corruptions execute. Result digests and exact IDs are compared across the independent normative inventory, control declaration, and execution ledger.

## 13. Finding disposition and next state

The package maps every one of the 14 open round-three findings. Technical counterexamples are closed only if both validator modes and repository checks pass. `LEGAL-G2-R3-001` and `IRREV-G2-R3-001` retain the non-technical prerequisite for a new qualified-human ruling over any final incorporated policy/Exam bytes; that prerequisite is intentionally not fabricated here. After authorized incorporation, all seven domain reviews, exception brief, exact-revision Critic, actor-bound protection evidence, and any human Gate decision are new work.

This candidate does not claim Gate 2 or release eligibility and authorizes no commit or push.

## 14. Round-three Critic R3 counterexample closure

The fixed R3 input remains immutable at SHA-256 `b69371abfecaf274cc6bcf8ba636f27210651b36206c0906c82908dd560a5958`. Its two blockers and three majors are represented by direct executable fixtures inside the independent normative inventory and control ledger. Authorization and human-authority graphs reject substituted or missing policy bytes, ordinary-signer replay/CAS records, stale heads, missing replay fields, and null authoritative IDs. The 16-class lifecycle graph consumes the full human/protected-action/multi-copy/provider-receipt chain described above. Cost consumes provider usage and invoice proof plus complete lineage. Privacy derives k-equivalence from the frozen source pool and covers digit-only/Unicode-format-control boundaries. Migration recovery and restore have pinned unequal input/result digests and transitions. Both validator modes require exact declared/executed ID equality; a distinct declared R3 case cannot be marked by byte-identical migration evidence.

## 15. Round-three Critic R4 counterexample closure

The fixed R4 input remains immutable at SHA-256 `232f0a8f95c3af1ddc0e87be0f0deeef1b9d27055aa9b5271b368575685a6d68`. Its two blockers and four majors are executable, not documentary assertions. Lifecycle completion consumes the complete human-authority and composed raw-grant decision graphs plus independently anchored provider-key metadata at signing and evaluation time. Spend consumes authoritative replay and CAS records with exact request, idempotency, target, policy and current-head bindings. Cost proves exact multiplicity and unique complete ledger/usage/invoice sets, including a reordered two-line positive and duplicate negatives. Recovery binds authority and every nested record to exact cut, class, provider, tenant, item, revisions and policy and rejects generic proof. Migration derives distinct signed semantic state and transition evidence and requires full human authority for destructive cleanup. Privacy detects `+` and `00` non-US country-code formats while preserving longer, embedded and short boundary negatives. Exact declared/executed equality makes every R4 fixture mandatory in both validator modes.
