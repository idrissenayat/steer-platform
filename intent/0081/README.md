# 0081 · Mixed-era lifecycle history

An exact archived history prefix can now be followed by complete current-key
events under current-v2. Global ordering, identity uniqueness, current proof
validity and full lifecycle hold application/release are checked across both eras.
Mixed runtime keys must be distinct; the provider proof cannot alias the event key.

Read BRIEF, SPEC, PLAN, development ACCEPTANCE and EVIDENCE. This is offline
evidence, not a live history feed, deletion or independent gate approval.

