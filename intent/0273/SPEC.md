# Intent 0273 specification

1. Establish a signed synthetic human and current native Git grants before the
   immutable corpus snapshot. Use the real identity root and complete 0272 factory,
   not injected result services or an authentication bypass.
2. Review 34 native Brief/Spec sources through authenticated HTTP. Admit scope and
   development through the same factory, preserving exact source/draft/profile
   and shared-budget bindings. Repeated preparation must reuse the existing records.
3. Execute two recorded scope batches and the existing separate Architect/Test
   Agent SDK paths against synthetic responses. HTTP readers must verify and return
   the exact generated Brief/Spec/Exam; returned history cannot claim semantic quality.
4. Reconstruct the runtime and recover identical results without another model
   call/reservation. Save a human draft correction, then read retained lineage
   while current execution authority is denied. Check foreign product, current
   records denial and native Git grant revocation without leaking source content.
5. Retain at most one verified immutable authorization document at its exact
   commit within one explicit HTTP/MCP request only. Never share bytes across
   requests; unscoped lookups remain full read-through. Discard the snapshot on
   completion/failure and deny late work from the ended request. Every identity
   lookup must read the current Git head. Cache hits perform
   no asynchronous work after that read. Changed heads re-read/validate the source
   and check head stability; errors discard retained data. Pin source binding and
   methods; clone returned records so callers cannot mutate retained data. No TTL,
   principal/decision cache, database fallback or timeout increase.
   Owned identity/MCP shutdown closes the resolver after request drain, clears
   snapshots and disables async-context storage; later authorization must deny.
6. Test failed/moving sources, revocation, changed source bindings/methods and
   caller mutation. Regress existing identity and native-corpus behavior and run
   full types/build plus documentation/protected-source checks.
7. Keep the previous focused SQL cases. Distinguish direct worker execution from
   Temporal scheduling, synthetic provider responses from semantic model quality,
   and HTTP integration from browser/live I1–I6 acceptance. Candidate confirmation,
   fixed save/reopen through this root and actual policy/provider adoption remain
   separate. No real records activation, spending, credentials or Git runtime writes.
