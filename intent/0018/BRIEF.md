# Intent

Demonstrate that the selected identity provider, HTTP broker and durable session
store interoperate before activating real users. Separate component passes are
not enough to establish assembled behavior.

Use generated identities and secrets, pinned local containers, forced-RLS auth
storage and the same provider checks. Do not enable a real runtime, spend money,
access real memberships or claim browser-engine behavior from an HTTP driver.
