# Plan

1. Inspect both actual native Critic records and distinguish local R5 preflight.
2. Implement closed layouts, source bindings and complete finding-counter checks.
3. Integrate original native reads into the existing scoped policy collector.
4. Verify both unchanged originals, exact timing and hostile mutations, plus a
   native-Git policy chain that must remain blocked.
5. Run focused, architecture, type and full checks; document and publish the candidate.

## Next dependencies

The historical HOLD layouts are not a passing Critic contract. Complete approved
reviewer/runner provenance, prior-finding history, governed pin/Builder selection,
and actual commercial provider/approval bindings. Passing native review semantics
must be explicitly specified, not guessed from these records. No independent
Critic has reviewed the current implementation by virtue of this increment.

Then complete full action-time authority and opt-in runtime writer composition,
authenticated destination/current-head discovery, save/status UI, board and
revision-bound review. All five R5 findings remain open. Preserve the 0124 history
protection, 0120 retained retirement archive, model conversation, manual release
audit, human gates and signed pilot obligations. Development can continue while
live use remains closed; new authority requires the user's specific decision.

