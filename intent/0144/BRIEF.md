# 0144 — Native Critic HOLD source compatibility

The first usable journey requires revision-bound gate evidence before any live
writer can be configured. The policy collector currently reads only its compact
development Critic profile, not the two existing `steer-critic-review/v1` records.
Add source-faithful support for those native HOLD layouts without claiming that
historical tests, agent metadata or local remediation preflight approve a gate.

This is authorized offline development. Do not modify protected records, request
provider credentials, sign gates, enable writes or authorize spending/deployment.

