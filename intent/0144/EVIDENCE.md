# Evidence

Verification on 2026-09-06, Node 24.20.0 / pnpm 11.19.0.

- Focused native Critic, complete policy-collector and architectural boundary
  suites pass 32 groups. Adapter typecheck passes.
- Both unchanged native records preserve all metadata and their respective six
  and two open findings. Count reconstruction covers initial ranks, follow-up
  statuses and newly added findings, including duplicate IDs across both lists.
- Counter/source/task/scope substitutions, unknown formats and authority fields,
  invalid/late exact times, oversized and malformed bytes reject. Inherited/prior
  context claims and same-task Builder selection reduce claimed independence.
- Actual isolated native-Git collection preserves original report bytes and feeds
  a HOLD into the real policy evaluator, which blocks the gate despite passing
  historical test metadata. Head movement and admission negatives are exercised.
- An additional isolated zero-finding HOLD probe remains failed; no success is
  inferred from counters reaching zero or from historical passing validation.
- Full `pnpm check` exits zero: kit/scope audit, prototype checks and 88 tests,
  437 root assurance checks, all seven package typechecks and package tests
  (227 adapters, 79 API, 81 registry, 29 web, 21 data, 18 worker, 13 domain),
  prototype production build and all seven package builds pass. Turbo reused
  unchanged package results where shown; no architecture allowlist was weakened.

Tests read both original native Critic JSON files without modifying them. Expected
bindings in these compatibility checks come from the files themselves and are not
approved external provenance. Native Git integration uses isolated synthetic
artifacts/signatures and an adapted historical HOLD layout, not a live gate review.

No browser, real provider, gate signature, protected file, runtime writer, deployment
or spending action is part of this backend slice. Fixture cleanup removes only
owned temporary Git repositories. All five R5 findings, reviewer provenance,
finding-history completeness, governed bindings and live action authority remain
unfinished. These checks do not establish real pilot or Gate 2 acceptance.
