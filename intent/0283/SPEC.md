# Intent 0283 specification

1. Extend the existing joined identity/21-service fixture with explicit proposal
   continuation. Retain its 34 sources, two recorded scope reviews, separate
   Architect and Test Agent roles, human correction, six model reservations,
   four fixed Temporal workflows and restart/lost-acknowledgement recovery.
2. Choose the current canonical root Brief for the existing target and the exact
   existing proposal ID. Reject absent/ambiguous/noncanonical/versioned targets
   and missing proposals; never fall back to a first proposal or new item. Both
   drafting roles receive the exact reviewed direction.
3. Seed canonical Exam and hidden context only in this disposable scenario, before
   the original target and reviewed corpus snapshots. Neither drafting prompt may
   include the canonical Exam. Keep independent current continuation eligibility,
   original/current surface comparison and source permission checks mandatory.
4. Reassess after the human Brief correction. Preview/confirmation must bind the
   current reviewed head, different original target commit, exact parent manifest
   and pointer digest. Missing current continuation eligibility, denied sources
   and a wrong target digest must refuse without an original or save effect.
5. Save one six-file plan: four create-only candidate files, one compare-and-swap
   of the selected proposal pointer and one create-only operation receipt. Verify
   exact parent requirements, the complete file set, unchanged canonical/hidden
   and prior proposal blobs, and no root candidate-pointer change. The new pointer
   must retain the original target and refer to the exact prior pointer digest.
6. Reopen the previous proposal at its pre-save commit and the new proposal at its
   save commit, including after a later simulated root Brief change. Preserve the
   original target and previous bundle lineage. Lost HTTP/scheduler/provider
   replies, replay and restart retain one original, six reservations and one save;
   current read-policy/Git-grant denial remains effective.
7. Run focused choice/selector/native-continuation checks, the joined continuation
   and default new-distinct regressions, broad tests, typechecks, build, links,
   kit/token-scope audit and protected hashes. Distinguish focused SQL selections
   from the full SQL suite and prior runs from fresh evidence.
8. No production authority/configuration/credentials, paid model calls, live
   runtime GitHub writes/grants, records/D1 adoption, live migration, gate,
   deployment/release or user-data deletion. C10 earns four points only after
   verification. Real model quality, performance and governed live/UI acceptance
   remain open.
