# Brief

The overnight path now reads prior receipts and their exact projected Briefs, but
0157 ingested the source through test-only setup. Add the reusable projection seam
needed between authenticated receipt readback and normal derived storage. Do not
replace missing live writer governance with prototype lifecycle assumptions.
