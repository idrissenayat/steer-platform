# Brief

Close the three development mapping gaps left by 0108 using complete raw and
reference-specific controls. Preserve the source mismatch in the malformed raw
case explicitly; test the authority-array shape in the actual raw schema rather
than crediting a rejected obsolete field on an ordinary failed-run graph.

Scope: three exact synthetic GAP-01 IDs. No new runtime or decision oracle,
provider/store operation, protected change, signature, release, deployment or spending.
