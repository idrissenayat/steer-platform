# Specification

1. Register reference-missing, raw-grant-missing and malformed-raw-grant under
   LIFECYCLE-GRAPH-NEGATIVE. Keep the independent catalog and original source pins.
   Leave the historical 0108 source map unchanged; record its three successors here.
2. Every hook runs a complete positive and complete replay before the negative.
   Seal actual configuration, optional trusted runtime, graph bytes and explicit
   evaluation time. Check complete evidence digests and typed zero effects.
3. Raw controls use 0061 raw-v2 with 0073 preterminal proof and 0074 batch. Assert
   three copies, four protected actions, named terminal, grant before terminal,
   exact 60-second deadline, batch mode/plan/reservation and grant digests.
4. Missing raw grant removes the actual raw grant and human bundle. Malformed raw
   grant puts the frozen authority-array shape into the actual rawGrantBytes slot.
   Both deny through the complete graph. The frozen malformed case selected
   RC-FAILED-RUN; its promotion to the genuine raw schema is explicit, not hidden.
5. Reference controls use the narrow 0089 no-downgrade entry point and current-v5
   graph: original key records remain unchanged, historical bytes stay retained,
   and fresh synthetic 2029 proofs bind exact references, version/content hashes,
   removal receipts, qualified decision and named tombstone. Verify two copy paths,
   three actions, two references, runtime/history/reference and final evidence digests.
6. Removing referenceRevocationBytes while retaining the signed cleared-state claim
   returns retained-pending-safe-disposition with REFERENCE_EVIDENCE_REQUIRED.
   The claim alone never enables disposition. No real 2029 evidence is asserted.
7. Keep fixture construction private and expose only closed kind/variant inputs.
   Selecting the original September fixture clock does not change existing failed-run
   December controls or key lifetimes. Seal the now-executed reference fixture dependency.
8. Positive raw and all reference results explicitly report executionAuthorized=false.
   Original raw rejection has no such API field; preserve its typed zero effects
   and the outer ledger's false execution/live/independent flags without fabrication.
9. Save fresh quick/full reports, rerun focused/repository/full-profile checks and
   preserve earlier snapshots. These mappings do not credit class/boundary rows.

The cases are synthetic evidence verification, not real grants, cleanup, reference
removal, future observation or gate approval. All five formal findings remain open.
