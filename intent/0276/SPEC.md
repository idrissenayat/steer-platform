# Intent 0276 specification

1. Use the same authenticated identity/factory and 34-source corpus from 0275.
   Grant the two synthetic start tools before the reviewed Git snapshot. Keep
   explicit start authority separate from preparation and model dispatch.
2. Execute both initial and corrected scope assessments through authenticated
   `intent.scope.start`, one plan activity and two ordered batch activities, using
   the existing admitted manifest and exact recorded SDK requests/responses.
3. Execute Brief/Spec and Test Agent Exam through authenticated
   `intent.development.start` and two ordered activities. The Test Agent receives
   exact predecessor Brief/Spec and all permitted scope sources, not the Architect
   message or a generated Exam transcript. Do not insert direct model-step calls.
4. Denied start cannot schedule or change SQL/model effects. Recover a discarded
   scheduler acknowledgement through the same workflow without another start or
   model reservation. Scheduling is never document-readiness or semantic clearance.
5. Validate single-attempt activities, reference-only history, exact batch/role
   order, completed replay and repeated starts without model/reservation duplication.
   Use the existing owned worker service for shutdown; no resource is live by default.
6. Preserve all earlier human correction, stale assessment, exact confirmation,
   lost-response recovery, fixed save/reopen and current access-denial checks.
   Expect two scope workflows, one drafting workflow, one candidate-save workflow,
   six synthetic model requests/reservations and one disposable native Git commit.
7. Do not change production expiry, service deadlines, source coverage, current
   authority, cost/dispatch controls, protected sources, real profiles/credentials,
   user-owned files or any live authorization. Fixture membership may be renewed
   only before source capture; each HTTP call uses a fresh short-lived synthetic JWT.
8. Report failure and performance limits honestly. Passing composition does not
   adopt D1/current policy or complete real provider load, semantic quality, other
   dispositions, governed startup or signed-in I1–I6 acceptance.
9. Eliminate repeated current-scope traversal inside one private, read-only
   development-start validation. Bracket exact immutable reuse with full current
   reads; preserve every caller/source callback and all existing original, key,
   lifecycle, execution and start-policy checks. Reject changed, expired,
   superseded or historical results. Complete the final full read before any
   scheduler RPC; open a new window for each scheduler revalidation callback.
   Do not cache permission, extend deadlines or share the window across effects.
