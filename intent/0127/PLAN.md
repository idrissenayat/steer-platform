# Plan

1. Retain verified OIDC issuer/issuance context without a separate decode or changed
   public principal shape.
2. Retain verified browser session context after both source reads and token checks.
3. Bind membership observations to exact credentials/session, not timestamps alone.
4. Test signed-token/broker composition, same-instant substitutions, revocation,
   reconstruction, and exclusion of metadata from public HTTP results.
5. Run repository checks, then the isolated browser integration sequentially.
6. Record evidence, update project documents and publish the verified candidate.

## Next route

Implement the full gate/provider/source verifier and the request-bound writer
composition; absent approvals still fail closed. Do not confuse token issuance,
membership, provenance-only observation or normalized policy facts with a human
gate decision. The actual writer remains uninstalled and App access read-only.

Retain 0124 history/protection work, save/status UI and board integration, decision
review, model conversation, trusted systems context, the five R5 findings and 0120
archival work. No signed scope or release/deployment/spending boundary is waived.
