# Plan — Delivery and verification

1. Define the portable, separately granted metadata query and exact-source schema.
2. Implement keyless bounded SQL discovery with lifecycle, owner and current
   authority checks; expose only an uninstalled API factory.
3. Connect the existing authenticated editor transport, bounded reference pages,
   read-first resumption and explicit exact-reference pending recovery.
4. Test registry/transport/controller/actual React graph and disposable PostgreSQL
   HTTP isolation, pagination, source changes, lifecycle and permission loss.
5. Run regression, type/build, migration-history and governance checks; document
   observed outcomes, preserve signed hashes, commit/push only owned changes and
   verify the remote commit.

Next: server-bound assessment consumption by direction/draft preparation; retain
live records/model/semantic/human/save acceptance as distinct requirements.
