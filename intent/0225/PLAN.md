# Plan

1. Define portable exact draft request/acknowledgement/readback contracts.
2. Add current-human/owner-bound tools with repeated identity revalidation.
3. Compose existing encrypted SQL lifecycle/revision services without activation.
4. Verify through actual HTTP handlers and disposable SQL: exact large documents,
   restart, CAS conflicts, duplicate mutation recovery, lost acknowledgement,
   owner/key/policy denial, holds and concurrent editors.
5. Run tests/typecheck/boundary/kit checks; document results and verify pushed head.

The change crosses contracts, registry, data composition and HTTP admission/tests;
numbered evidence and project documentation add files beyond a narrow single-layer
increment. No stack, permission, migration or deployment expansion is included.

Next: editor transport/state integration, explicit unknown/conflict recovery and
current-authority development status/result retrieval. Real D1 adoption and other
existing activation approvals stay separate from these implementation steps.
