# Intent

The provider proves identity, not STEER authority. A valid token or durable
session must not outlive a revoked Git membership by using stale grants.
Compose the production resolver explicitly and demonstrate this through the
already verified browser/provider/storage boundary.

Use synthetic commits only. Keep the user's actual GitHub App permissions,
membership records, UI and deployment settings untouched.
