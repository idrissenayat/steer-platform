# Execution route

1. Inspect existing Git authority, browser identity and encrypted-session seams.
2. Add explicit Git-backed browser/bearer composition and startup path validation.
3. Add synthetic committed Git source and bearer boundary checks.
4. Extend actual Chromium/Keycloak/Postgres checks with committed revocations and
   fail-closed source/document failures.
5. Run affected integrations and root checks; record observed evidence.
6. Update implementation/identity guides, commit and push the candidate.

Next bounded increment: fail-closed ingress/resource controls and trusted runtime
configuration. Keep real access and public activation approval-gated.
