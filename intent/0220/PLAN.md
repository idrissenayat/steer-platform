# Plan

1. Project explicit role contexts and derive exact request/step digests.
2. Compose encrypted original and succeeded-result reads with current SQL state.
3. Verify source fidelity, fresh-context exclusion, real digest-bound reservations,
   mismatched/clarifying predecessors, edits, holds, expiry and late state changes.
4. Update evidence and push verified owned development changes.

Next: actual adapter-bound rendered request/response/usage capture and provider
provenance, then durable reference-only role activities with no paid retries.
Only acknowledged dispatch may reach the model adapter; preparation is not a
lease, permission, fresh-state token or provider replay guarantee. Finally wire
actual editor/API acknowledgement and recovery under adopted records controls.
