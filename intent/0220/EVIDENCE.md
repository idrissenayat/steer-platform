# Development evidence — 2026-09-08

Base: `3238ea8f6eb11c9df053aefd546f974f8a19d3b2`.

## Delivered boundary

The deterministic renderer preserves intent/clarification, original Brief/Spec,
direction and bounded evidence; Test Agent input adds the actual succeeded
Architect's Brief/Spec separately. Neither role receives an old Exam automatically;
the Test Agent does not receive Architect commentary or private conversation state.
Original Brief/Spec corrections remain visible so constraints are not lost.

The private reader composes actual encrypted original/source/result stores and
SQL operation state. The Architect step input must equal its actual rendered
request digest before it can serve as the Test Agent predecessor. Actual fixture
reservations use these digests, replacing arbitrary placeholders in this new chain.
Request preparation itself neither claims a step nor consumes a reservation.

## Verification

- **147/147** disposable PostgreSQL/native-Git/Temporal integration checks passed.
  The new chain uses actual request digests for its 3+2 synthetic-cost reservations,
  restores from actual encrypted originals and a succeeded checkpoint, and denies
  re-preparation after the one-way dispatch boundary. Negative cases cover arbitrary
  predecessor hashes, unanswered questions, newer edits, holds, revoked access,
  expired jobs, close and a quarantine written during final source restoration.
- **296/296** scoped units and local migration controls passed:
  domain 27 + registry 170 + data 50 + worker 47 + controls 2.
- Full prototype/eight-package typecheck, required kit (95 artifacts), workflow
  token-scope audit and `git diff --check` passed.
- Protected Architecture/Exam/accepted policy SHA-256 values remain
  `9e1783a5f9870e8a8a2595d23226efa804902b4c472e309bf9f924d8cf61dc65`,
  `84ad1d4c14d6614fe4b53509104e379e9ad6dff0462c96109901eefb1e02fd7f`, and
  `f8a9cb9acc90e2943181be428cb03bebcce64758a3ac19bf1243e3bbe3894e32`.

## Non-claims

No private packet entered the real editor/API, Temporal history or a model provider.
SQL/encryption/checkpoints and source reads are actual disposable components, while
identity, profiles, grants, keys and role output remain synthetic. No independent
agent or human acceptance verdict is claimed. Existing I1–I6 acceptance remains open.

The renderer reconstructs deterministic inputs from preserved originals/results;
it does not persist a rendered packet or observe what a provider received. Runtime
profile-to-renderer binding, provider request/response/usage provenance and durable
activities remain next. Current-read checks are observations, not an atomic claim
or paid dispatch permit; the future worker must recheck scope/budget and consume
the operation store's one-way acknowledged dispatch boundary.

Old Exam/commentary fields are excluded structurally, not detected/redacted inside
arbitrary user/evidence text. Included evidence remains untrusted source data and
incomplete coverage remains incomplete. No semantic novelty or adequacy verdict.

No schema/migration, real records-policy adoption, key/grant, model call, runtime
GitHub write, deployment, deletion or signed-source change. The nineteen-entry
development journal remains held against the seven-entry real baseline. User-owned
roadmap/output paths remain untouched and excluded.
