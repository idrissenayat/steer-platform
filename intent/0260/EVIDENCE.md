# Development evidence — 2026-09-08

Base: `dac551484597625b7426bb6354d459d59650623e`.

## Implemented capability

The actual final-review panel now connects retained run discovery to a package
preview. The new authenticated HTTP/MCP query composes latest preserved documents,
current final-scope review, the historical encrypted original, verified SDK output
for both roles and mandatory current destination/lifecycle evidence.

Pinned profiles and exact byte comparisons determine edit lineage. Identical
verified inputs reproduce the same package UUID, manifest, pointer and proposed
confirmation. The existing durable admission can consume the corresponding bundle
shape after confirmation; this query neither admits nor confirms it. No raw prompts,
private conversation, SDK exchange bodies or placeholder operation escape.

## Verification

Five contract tests cover reconstruction, existing bound-planner handoff, exact
human edits, profile/destination/committer changes, bad hashes/authority fields,
linked work, pre-pull corrections, new and corrected amendment proposals and
legacy-mapping refusal. Three authenticated HTTP/MCP/server tests cover no-store
responses, human-only grants, missing installation, late revocation, redacted
errors and bounded pending work on close.

Two production React tests cover explicit retained-run selection, no automatic
work, exact preview rendering, metadata validation, identity/visibility clearing,
late response suppression and accessibility. The axe check excludes color contrast
because JSDOM does not perform layout.

Nine focused PostgreSQL/history/SDK checks plus idempotent migration passed. The
extended 34-source/two-role scenario successfully produced the package through
the actual API and data/history services with synthetic authority. It verified
repeatability, stale edit flags and head/result/grant denials without more model
reservations. The final full-suite run also verified unchanged operation, step,
candidate-original and draft-revision counts during preview.

Final **1,209/1,209 regression tests** and **356/356 PostgreSQL 16.14 integration
checks** passed. The full run includes the strengthened composed package scenario;
it removed only its own disposable synthetic container and tmpfs data afterward.
All eight package typechecks plus prototype types and the optimized Next 16.3.4
build passed. The 95-artifact kit, workflow token-scope audit and `git diff --check`
passed; all **195 local links across eight checked documents** resolve. Protected
Architecture, Exam and accepted retention-policy hashes are unchanged.

A final UI correction clears the prior run list as well as the package when
preview verification fails. Both focused React tests and the full 1,209-test
regression rerun passed after that correction. No production validation or
authentication boundary was weakened to make the checks pass.

## Real-use boundary

The actual browser could not be inspected because the Mac was locked and automatic
unlock failed. No authentication bypass or alternative preview was used; passing
React tests do not claim signed-in visual or live repository acceptance.

The credential-safety skill preserved the resolved key decision without inspecting
or creating credentials. The complete installed Next client/server guide kept
private record/profile verification out of browser imports. D1 and the proposed
$5 first model budget remain inactive/unapproved. No real records migration,
paid model call, runtime Git save, gate, release or deployment occurred.

Durable human confirmation, immutable original admission/readback, workflow start,
existing-proposal selection and real destination/records/model/provider authority
remain open. The one-minute continuation loop stays active. User-owned roadmap
and outputs are untouched.
