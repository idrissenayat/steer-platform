# Specification

- Add a strict human-only `intent.candidate.save.preview` query shared by HTTP/MCP.
  Its own current grant, scope/subject/configuration and late identity checks apply.
- Restore exact latest draft bytes and current final-scope review. Bind the
  selected recorded generation original to both verified SDK roles and source.
- Derive profile/edit lineage server-side; never accept claims from browser text.
  Preserve unreviewed/stale Spec and Exam states, not invented acceptance.
- Require an explicit current destination/lifecycle port and recheck all source,
  draft, records, result and caller authority before returning the complete proposal.
- Reproduce the same bundle UUID/manifest/pointer from identical verified inputs.
  Produce a proposed confirmation binding without allocating an operation or
  exposing placeholder IDs/receipts, prompts or raw SDK exchanges.
- Preserve new, linked, pre-pull correction and amendment destination rules.
  Reject implicit legacy mappings and unselected existing-proposal corrections.
- In the actual UI, explicitly select a retained drafting run, enter an assigned
  new item ID where needed, and inspect the exact package. Never auto-generate,
  adopt, confirm or save. Clear stale private state on selection, context,
  permission, visibility, session and records expiry boundaries.
- Test production contracts, authenticated HTTP/MCP, actual React interactions
  and the composed disposable SQL/34-source/recorded-SDK journey. Distinguish
  synthetic authority, live UI visibility and actual user acceptance.
- No dependency changes, new migration, auth bypass, credential inspection,
  paid call, live records activation, runtime Git write, gate or deployment.

Durable confirmation/original admission/start and current real provider authority
are not completed by this preview increment.
