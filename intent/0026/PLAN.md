# Execution route

1. Read installed Next.js Server/Client, runtime environment and connection guidance.
2. Add disabled-by-default public view configuration and native pink/orange page.
3. Serve the actual production build through the isolated HTTPS test gateway.
4. Verify disabled/desktop/mobile/keyboard/accessibility and full native auth flow.
5. Review screenshots, fix observed issues, run root checks and push documentation/code.

Next: trusted local listener/profile integration and authenticated workspace/session
UI. The legacy Vite product remains separate until genuine parity/gates are met.
