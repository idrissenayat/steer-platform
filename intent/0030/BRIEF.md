# Intent

Complete the next local-runtime foundation step without plaintext configuration
files or implicit credential discovery. Bind encrypted service configuration to
a trusted scope, immutable named revision and exact digest. Keep data-key unwrap
behind a replaceable KMS/Vault-facing interface, preserving the existing stack.

