# Plan

1. Join full qualified parent history, exact archived child proof and fresh complete
   manifest/head assertions; enforce scope, closure and retention chronology.
2. Handle an explicit complete-empty manifest and calculate only a candidate P7Y
   boundary from later retirement/final deletion; never use item closure instead.
3. Exercise signature-valid semantic negatives, missing proofs, duplicate/orphan
   children, false empty claims, original/current binding, closure and clock limits.
4. Preserve existing mapped reports, run focused/repository/separate integration
   checks, document and publish the verified candidate branch.

## Next: qualified retirement before lifecycle admission

The source RETENTION-AND-RECORDS-POLICY.candidate.md (frozen HR-01-R2 SHA-256
f8a9cb9acc90e2943181be428cb03bebcce64758a3ac19bf1243e3bbe3894e32) specifies a
qualified records-owner decision commit for corpus-retired. The current 0082 schema
admits only hold-applied/hold-released, and 0084 archives those hold decisions.
Add a distinct qualified retirement schema/profile using the complete identity,
qualification, assignment, provider, replay and CAS checks. Bind exact parent/corpus,
event bytes, actor, decision time and predecessor/commit conditions, then retain and
revalidate the complete original decision with current independent witnesses. Do
not relabel a hold proof or the retirement event's actor/source strings as authority.
This is local candidate implementation; no new human signature is requested here.

After that prerequisite, bind the verified retirement and this manifest/history
component into separate future readiness/full provenance profiles. Retained evidence
must precede current state; full parent copies/actions/aggregate/tombstone and all
qualified holds remain mandatory. Avoid introducing a static import cycle:
0119 -> 0118 -> 0117 -> 0061 -> 0080 already exists as a dependency path. A future
0080/0061 admission must use a clean leaf/shared-core refactor or external composition,
with unchanged original policies/observations proved by regression. Never solve the
cycle by dropping required child/retirement verification or accepting request callbacks.
Only then map the four provenance source clocks; keep the frozen surrogate discrepancy
explicit in source reconciliation.

Current-v7 remains unimplemented. Twelve lifecycle, 3,614 migration and one schema
coordinate, all five formal R5 findings, normative/live integrations and independent/
protected review remain open. No provider mutation, deletion, gate signature,
release, deployment or spending is authorized. The next local prerequisite is
unblocked; actual live owner evidence and acceptance remain separately gated.
