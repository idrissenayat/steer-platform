# Plan

1. Pin sources and independently derive exact legacy and R5 required IDs.
2. Add code-owned hooks with actual calls, assertions and byte-digest observations.
3. Emit explicit passed/failed/uncovered counts and exact-ID execution records.
4. Add report versus require-complete commands; strict mode must reject incomplete coverage.
5. Test source-set equality, major-case preservation, actual hooks and CLI boundaries.
6. Run full checks, document, commit, push and verify the remote revision.

## Next within GAP-01

Connect existing corrected recovery and full human-authority fixtures to their
exact catalog IDs, reusing or extracting existing test-only fixture builders where
appropriate. Preserve positive controls, every native/current proof, exact scope
and the two expected unknown recovery cuts. Then connect remaining shared-action,
privacy/time, cost, lifecycle and migration cases. Report unsupported cases;
do not relabel an arbitrary rejection as the expected semantic execution.

Keep GAP-01 open until the full corrected ID/requirement map is reconciled.
GAP-02/03/04 handle mapped substantive missing behavior and GAP-05 real isolated
integration. The first 63 hooks do not replace the wider tests or certify the
entire oracle. Before any independent/protected readiness claim, require the
complete-coverage command and all remaining normative/integration obligations.
No protected edit, signature, provider mutation, deployment or spending is authorized.
