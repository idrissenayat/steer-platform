# 0121 — Authenticated stateless Brief preview

Read `BRIEF.md`, `SPEC.md`, `PLAN.md`, `ACCEPTANCE.md` and `EVIDENCE.md` in order.
Implementation: `packages/tool-registry/src/brief-preview.ts` and `src/index.ts`.
Tests: registry `test/brief-preview.test.ts` and API `test/mcp.test.ts`.

This first-journey development slice drafts only. It cannot save, confirm, sign,
retire or delete anything and adds no R5 catalog coverage. Production UI follows.
