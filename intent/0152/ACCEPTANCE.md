# Development acceptance

- Real Keycloak browser login and encrypted PostgreSQL session feed the actual runtime.
- Destination UI displays current native Git output from the production GitHub reader.
- The existing browser cookie works after destination runtime reconstruction.
- Removed grants clear metadata; restored grants permit a fresh observation.
- Real elapsed-time expiry clears metadata without clearing the unsaved draft.
- Generated App assertions are verified and only read-only provider calls are accepted.
- Owned runtimes close and the existing browser suite remains intact.
- Explicit browser and full checks pass with evidence boundaries documented.
