# Owned records read-set

Replace the test-only storage reader with a production-source, bounded records
owner needed by the whole-journey correction in 0298. Read permitted metadata
before independently authorizing each record and retrieving encrypted contents.
Retain complete final row/lifecycle comparison and ownership through cancellation.

This is internal read-only infrastructure, not application factory activation,
records adoption, model usage, a GitHub save or completion of C18/C22. Preserve
the existing stack, policy requirements, signed artifacts and 25-checkpoint basis.

Acceptance: focused owner/policy/race tests and the existing native synthetic
journey with this production reader, existing cryptographic/SDK checks and final
source checks. Report its cost separately from the unchanged application action.
