# 0311 — One owned generation read phase in candidate preview

Continue the actual intent-capture journey by removing separate reconstruction of
the same retained original and generation history inside a single read-only save
preview. Preserve all current permissions, exact documents and final readback.
Do not share the phase across confirmation effects, requests or human revisions.

Measure the actual preview/confirmation paths, not just an isolated session. This
is part of C22, with no change to the 25-checkpoint denominator, deadline or request
budget. It does not activate a runtime, grant model or GitHub access, spend money,
sign a gate or establish real-user completion.
