# Implementation plan

1. Add immutable operation/step schema, unique cost links and forced-RLS privileges.
2. Implement atomic claims, lease fencing, one-way dispatch and checkpoint readback.
3. Verify races, cross-process reconstruction, budget interaction and ambiguous
   commits against disposable PostgreSQL; run regression/type/control checks.
4. Hold unadopted real migrations, update documentation/evidence and publish the
   development branch without installing or activating the adapter.

Next: compose ownership with Temporal and candidate-bundle dispatch, current
authority, verified lifecycle/full-corpus scope and approved result storage. Keep
unknown outcomes blocked and avoid automatic paid activity retries. Draft-policy
adoption, spending and live runtime writes retain their separate approval boundaries.
