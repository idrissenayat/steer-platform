# Plan

1. Verify actual retained child receipt/full-graph bytes and provider-bound events.
2. Deny omitted, partial, swapped, borrowed, misbound and globally reused evidence.
3. Test complete/replay controls, signed semantic negatives, closed configuration,
   explicit time and no-effect boundaries; preserve all prior ledger observations.
4. Refresh reports for shared fixture hashes, run repository and separate full
   integration checks, document and publish only the verified candidate branch.

## Next

Compose a narrowly admitted future provenance profile. Bind the complete authoritative
parent manifest and history to these exact child events and retained evidence bytes,
then require fresh current archival witnesses and revocation checks for those bytes.
Never re-run original proof at an invented past time and call it future trust. Preserve
one retirement, complete closure, later retirement/final-deletion plus seven years,
and the explicit empty-complete-manifest path. Add source mappings only after the
entire provenance composition actually executes.

Twelve lifecycle coordinates remain, including deletion-evidence final-tombstone
semantics and sanitized key plus residual-copy erasure; 3,614 migration and one
obsolete schema case remain unmapped. Normative variations, real runtime/store
integration, independent/protected review and GAP-01–06/M1–M8 remain open. No new
provider access, deletion, signature, release, deployment or spending is authorized.
