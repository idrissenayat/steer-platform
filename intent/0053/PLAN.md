# Implementation plan

1. Verify published 0052; inspect installed Next.js server/client-boundary guidance.
2. Bind fixed org/repository display context; extract portable canonical contracts.
3. Reuse bounded read transport, add catalog membership reader, then render the
   read-only Brief library and side panel with retained pink/orange design.
4. Test native negative cases and actual Keycloak/Git/PostgreSQL/Chromium flows;
   inspect desktop/mobile screenshots and run root/frozen-install checks.
5. Update guides/evidence, publish the candidate branch, verify remote equality.

Next: exact-reference deep links and browser navigation, then governed lifecycle
and richer intent detail. Continue safe increments; gates and paid/provider
operations retain their independent authorization boundaries.
