# Plan

1. Add portable destination contracts and the shared authorized query.
2. Compose opt-in fixed scope with the existing read-only Git reader.
3. Test scope injection, revocation, identity/time boundaries and missing services.
4. Verify changing native Git heads through the actual adapter and HTTP/MCP parity.
5. Run full checks, document limits and publish the candidate branch increment.

## Next

Connect read-only destination observation to the authoring UI with clear unavailable
and stale-result behavior. Preserve the closed writer until full action-time gate
authority, actual approved provider/runner/source bindings and write installation
scope are satisfied. Then complete confirmation/save/status composition, verified
lifecycle projections, board and revision-bound review. Retain the full Phase 1
obligations, all five open R5 findings, independent review, human gates, retention
archive, protected operation history, model conversation and release/pilot work.
