# Execution route

1. Inspect the assembled harness and browser/TLS prerequisites.
2. Add a mature pinned Playwright test dependency without policy exemptions.
3. Serve real HTTPS test forms and register the dynamic synthetic callback.
4. Verify Chromium navigation, secure cookies, CSRF, reconstruction, replay and logout.
5. Recheck combined/provider-only harness modes, frozen install and root checks.
6. Document observed evidence and remaining limits; commit/push candidate.

Next: authoritative Git-derived membership and trusted runtime composition,
including ingress resource/rate controls. Do not activate public login implicitly.
