# Brief

Authors need to see configured repository targets without entering arbitrary paths
or treating a projection revision as the current Git head. Connect increment 0148
to the existing authenticated Brief authoring screen. Keep unavailable, cleared and
expired results explicit, with no save, approval, model or provider enablement.

This is a local development increment, not a change to the signed acceptance scope.
