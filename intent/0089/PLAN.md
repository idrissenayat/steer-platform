# Plan

1. Add explicit reference-only current-v5 runtime and narrow entry point.
2. Compose complete qualified history, retained content and revocation/removal.
3. Bind complete version/copy hashes, state and independently authorized tombstone.
4. Exercise first/replay, holds, expiry, missing/tampered evidence and isolation.
5. Run regressions and full checks; document, commit, push and verify remote.

## Next

Reconcile the remaining normative/source/class/trust-era inventory with concrete
public entry points and current tests. Address the next bounded uncovered path,
including remaining migration compatibility/concurrency/checkpoint evidence.
Do not confuse an offline candidate verifier with a live source adapter or an
executing lifecycle transaction. Preserve all five open findings until the whole
corrected package is independently reviewed and protected incorporation occurs.

No signature, protected edit, provider mutation, release, deployment or spending
is authorized by this plan. Existing human approvals are not renewed by code.
