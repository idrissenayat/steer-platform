# Intent 0278 specification

1. Count requests at the synthetic provider transport used only by the actual
   identity runtime. Keep its issuer, JWT/key verification, fresh Git head,
   immutable authorization-document validation and service construction unchanged.
2. Reconcile that count against the shared native fixture per HTTP action. Emit
   only fixed category counts for identity and remaining repository reads; no
   request paths, identities, headers, bodies, credentials or provider responses.
3. Preserve request/init references, original promises, responses and failures.
   Diagnostics must not supply a principal, service result, authorization decision,
   budget admission or repository response. Invalid partitions must fail the test.
4. Run the same 34-source/two-scope/independent-drafting/confirmation/save/reopen
   journey, including restart and lost-acknowledgement checks. Keep six synthetic
   model requests/reservations and one disposable native Git commit.
5. Report exact origin measurements, limits and the next bounded engineering
   change. A diagnosis is not a speed improvement or signed-in UI acceptance.
   No production contract, live policy, D1, model spend, runtime GitHub write,
   credential, deployment, signature, release, deletion or user-owned file changes.

Review the [next-change investigation](INVESTIGATION.md) before implementation.
Its request-owned corpus verification proposal is not part of this diagnostic
checkpoint's delivered production behavior.
