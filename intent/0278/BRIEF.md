# Intent 0278 — Attribute authenticated journey read traffic

Separate identity revalidation from corpus/destination traffic in the same joined
authenticated workflow. Use fixed synthetic transport ownership, not shared URL
guesses or cached authorization. Quantify the remaining bottleneck before changing
any other callback or freshness contract. See [specification](SPEC.md) and
[evidence](EVIDENCE.md).
