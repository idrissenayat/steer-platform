# Specification

1. Register only R5:PREFLIGHT-R3-R5-001:reproduction:1. Preserve all 4,036 required
   IDs and source pins. Keep formal findings and normative/integration review open.
2. Execute the frozen RC-FAILED-RUN complete graph and its exact trigger through
   the frozen graph and event oracles. Preserve deleted-tombstoned versus
   EVENT_SCHEMA_INVALID. Label/assert the graph's hypothetical lifecycle counter;
   it is not a real deletion or corrected execution.
3. Execute the existing complete original-era createLifecycleGraphVerifier on the
   same record class with a valid September 4 run-terminal event, earlier committed
   history and a December 3 synthetic evaluation. Assert the exact 90-day boundary,
   two copies, three protected actions and complete evidence digest. Do not change
   original key windows or equate this synthetic future clock with elapsed real time.
4. Verify complete positive and replay controls before crediting the negative
   expectations. Bind actual installed configuration, graph bytes and evaluation
   time. Replay verifies all three paths without an effect.
5. Reject the exact old graph and surrogate in the corrected graph; omit twelve
   required event fields and reject bad current/prior provider proofs, wrong record,
   wrong policy/history digest and active hold. Most semantic variants rebuild
   signatures and history commitments. Event-ID/provider-ID omissions and the
   exact surrogate are envelope-level negatives, not rebuilt-lineage evidence.
6. Require all ten shared proofs separately for copy-1, copy-2 and the tombstone.
   Reject wrong human terminal scope, a losing reservation and wrong provider
   receipt lineage on each path, plus exact-expiry evidence.
7. Every corrected result has typed zero effects. The original-era graph API does
   not expose executionAuthorized; do not fabricate that field. The outer ledger
   continues to state executionAuthorized=false and no live provider use.
8. Export only closed fixture variants. Signing and the inherited generic/raw/current
   fixture helpers remain private and unused by these cases. No live credential,
   provider, CAS store, deletion or journal interface is introduced.
9. Save new quick/full snapshots, rerun repository and full-profile checks, and
   retain earlier snapshots. Many observations credit this exact R5 ID only once.

This is one full original-era graph, not coverage of the 94 remaining lifecycle
matrix IDs, future key eras, raw grants or referenced-evidence retirement.
