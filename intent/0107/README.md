# 0107 · Complete lifecycle surrogate-trigger counterexample

The final R5 reproduction now runs through the complete original-era failed-run
lifecycle graph. It reproduces the frozen inconsistency: the old graph accepts
a surrogate that its event schema rejects. A complete corrected control verifies
the same class and 90-day boundary, both copies, human/shared-action proofs,
provider receipts, aggregate and separate tombstone. Hostile variants deny.

All nine R5 reproduction IDs now have mapped development executions. This does
not close the five formal findings, the remaining normative matrices or Gate 2.
The synthetic clock crosses September 4 to December 3, 2026; no actual 90-day
observation, deletion or future operational evidence is claimed.

Read BRIEF.md, SPEC.md, PLAN.md, development ACCEPTANCE.md and EVIDENCE.md.
Quick/full reports are execution snapshots, never imported proof. Use the
existing r5:coverage commands for fresh runs.
