# Intent 0274 specification

1. Extend the existing authenticated generation case without replacing concrete
   services, bypassing identity or reducing its 34-source native corpus.
2. After a human correction, verify the prior assessment is rejected for the
   latest exact draft. The denial cannot create an operation, original or model
   reservation or reveal private document bytes.
3. Prepare and execute two new recorded scope batches for the corrected revision
   using actual admission, encrypted SQL and existing SDK worker paths. Keep
   semantic quality explicitly unverified; no paid model calls.
4. Through authenticated HTTP, perform final save review and exact package preview.
   Preserve the older two-role generation lineage and identify only the changed
   Brief as edited. Mark conformance/Exam review stale, never approved.
5. Require explicit human confirmation. Simulate a lost client acknowledgement by
   discarding the first HTTP response, reconstruct the owned identity/factory and
   resubmit the identical command. It must recover one exact encrypted original
   and one save operation without another model reservation or scheduling.
6. Revoke the separate synthetic candidate policy and withhold preview while
   preserving encrypted bytes. Close all three owned runtimes and their resources.
7. No protected document, real profile, credential, migration, D1 adoption, gate,
   model spending, runtime GitHub write, deployment or user-owned file changes.
   This is HTTP integration, not browser acceptance, scheduling or a real save.
8. Resolve the [measured read-amplification failure](INVESTIGATION.md) without
   reducing the corpus, raising service deadlines or accepting `unknown` as a
   completed confirmation. Preserve equivalent current/late revalidation and
   add focused regression coverage for any changed verification composition.
9. Bound historical-scope reuse to one private read-only generation projection.
   Require first/final full authoritative reads, exact target/reader/evidence
   agreement, current caller/source checks on every reuse and fail-closed cleanup.
   No cross-request reuse, current-clearance substitution, write or dispatch.
