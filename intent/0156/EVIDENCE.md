# Development evidence

`pnpm test:auth:browser` passed all 41 checks using Chromium 151.0.7922.34,
Keycloak 26.7.3, actual HTTPS/Next.js, encrypted PostgreSQL sessions and owned native
temporary Git. The new check does not intercept browser replies: the existing
production GitHub store validates actual marker/Brief blobs, parent/tree structure,
operation-path history and base/head comparison through the narrow no-network
provider transport. The transport verifies generated test App assertions and
requires read-only Contents/Metadata scope; it has no network or write fallback.

The browser received the exact seeded receipt, then actual not-found for a distinct
operation. After later membership commits and identity-service reconstruction,
the same receipt still referred to the original creation revision. Committed
status-grant denial returned HTTP 403 and made no additional provider reads;
restoration allowed exact readback again. Every created request writer closed.
The different current draft's facts/digest stayed unchanged, the disabled save
control remained disabled, and local/session storage remained empty.

The fixture seeder exclusively creates two new leaves and verifies the exact staged
paths. Hashes/revision come from actual test bytes and native Git, but the request
digest and history are explicitly synthetic. No platform save, provider operation,
human confirmation or approval is claimed. This check composes the existing
identity service and shares the parent-owned PostgreSQL store; it is not a newly
installed production runtime profile. The existing 0155 intercepted outcome tests
remain separate presentation-only evidence. No UI source was changed in 0156.

API type checking and full `pnpm check` passed on Node 24.20.0/pnpm 11.19.0:
95 kit artifacts, workflow-scope audit, seven package typechecks, 88 prototype
tests, 437 root controls, all workspace tests (including 270 adapter and 87 API
tests), and all seven package/prototype builds. Eligible Turbo steps were cached.
Whitespace checks passed; protected `intent/0001` and `.github` stayed unchanged.
Existing keyboard/mobile/automated accessibility checks passed in the browser run;
no new independent visual/qualified accessibility review is claimed.

Only owned test Chromium/HTTPS services, PostgreSQL/Keycloak containers, temporary
data and generated credentials were cleaned up. No real credentials, permissions,
provider write, live writer, gate signature, deployment, release or spending changed.
All five R5 findings, qualified/independent protected review and signed Phase 1
obligations remain open. The implementation loop continues with exact receipt-to-
Brief reader navigation under current catalog/access checks.
