# Implementation plan

1. Add coherent read-only role metadata and the combined historical projection.
2. Bind the recorded verifier and strict human HTTP/MCP contract, default closed.
3. Connect explicit original-document comparison in the actual editor using only
   public contracts; keep backend data and provider imports out of client code.
4. Verify complete, partial, expired, superseded, changing and revoked-access
   scenarios; run regressions, SQL checks, types, build and document checks.
5. Record limits, commit/push only owned changes and verify the remote commit.
6. Continue all-revision discovery and final-save preparation/confirmation/start.
