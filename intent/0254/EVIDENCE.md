# Development evidence — 2026-09-08

Base: `e43490e10d3eec7cbf4759f8c0788d0cd64dfc3b`.

## Capability

The actual authenticated conversation now has a read-only final-save review
action. The server restores exact preserved documents and current recorded scope;
the browser checks all document hashes and selected direction without sending
document prose, rerunning generation, admitting an operation or claiming a save.
See [the guide](../../docs/FINAL-CANDIDATE-SAVE-REVIEW.md).

## Completed verification

Focused 19/19 HTTP/MCP/native-Git/client/actual-React/boundary checks pass.
All-package typechecks and optimized Next.js 16.3.4 production build pass.
Final **1,156/1,156 regressions** and **337/337 PostgreSQL 16.14 integration checks**
pass. The new SQL case restores exact encrypted documents across separate
reader/pool constructions, rejects revoked keys and leaves operation, original,
reservation and draft-revision counts unchanged. Native Git checks reject a
concurrent newly added intent before releasing a final review. The SQL runner
removed only its own synthetic PostgreSQL container and tmpfs data.

All 190 local links across 11 increment-related documents resolve. Protected
Architecture, canonical Exam and retention-policy SHA-256 values are unchanged.
Prototype/all-package typechecks, kit (95 artifacts), workflow token-scope audit
and whitespace checks pass. Actual React tests use synthetic HTTP and axe
WCAG A/AA rules with contrast disabled, not real-browser visual or human acceptance.

The expanded actual-conversation test now preserves generated document fields,
reviews corrected scope and checks the bundle while generation is disabled. Its
draft HTTP fixture was corrected to use the production scope fingerprint helper
with exact Brief/Spec fields instead of incorrectly passing Exam into scope input.
No production document validator was weakened. Two older explicit UI test loaders
were extended to compile the new real component rather than stubbing it. A strict-schema ordering mismatch
in the new selection comparison was corrected before the focused run passed.

## Authority boundary

No default backend installation, records-policy adoption, real migration, key
inspection, paid model call, runtime Git write, gate, auth bypass, deployment or
release. Native Git and disposable SQL/Temporal fixtures use synthetic authorities
and test-owned temporary data only. User files remain untouched.

The credential skill preserved the resolved key decision without reinspection or
another prompt. Installed Next.js client-boundary instructions were read under
the web AGENTS directive. Existing colors/controls and scope-review contracts are
reused; no dependency or database schema was added. The one-minute loop continues toward
the remaining save preparation/confirmation/start and real acceptance boundaries.
