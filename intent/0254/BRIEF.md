# Brief

After correcting the generated Brief, Spec and Exam, the human needs to review
the final preserved bundle against existing scope without starting another
drafting run. Earlier source/direction checks cannot silently apply to new text.

Connect the existing records, repository-wide source review and recorded
assessment services to a human-only final-review query and the actual UI. Bind
all three document bytes, current source snapshot and explicit direction while
preserving the distinction between read-only review and save confirmation.

No live records activation, model spend, item allocation, GitHub write, silent
merge, author attribution, gate or acceptance inference. Uncertainty remains
visible; current human work stays unchanged. See [Spec](SPEC.md).
