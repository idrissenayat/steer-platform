# Execution route

1. Inspect the normalized identity contract and provider's current documentation.
2. Pin the selected image digest and add the isolated local TLS/realm harness.
3. Verify genuine provider tokens against normalized identity and shared API.
4. Run typechecks, root checks and opt-in integration under Node 24.
5. Record actual results and limits, update delivery docs, commit and push.
