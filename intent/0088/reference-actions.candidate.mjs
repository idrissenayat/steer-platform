// Explicit trusted profile entry point; implementation remains the shared verifier.
export { createReferenceProtectedActionVerifier, referenceManifestBytes as manifestBytes,
  referenceManifestDigest as manifestDigest } from '../0060/protected-actions.candidate.mjs';
