# Development acceptance

- Brief and operation marker originate in actual HTTP-confirmed native Git creation.
- Worker reads current status, not a prebuilt receipt, and uses separate projector identity.
- Catalog and exact read return the created revision/digest/content after projection.
- Recovery/duplicate handling cannot produce a second Git mutation or projection event.
- Current grants, source curation and exact revision/digest remain mandatory.
- Actual isolated integration and regressions pass; no live capability or gate claim.
