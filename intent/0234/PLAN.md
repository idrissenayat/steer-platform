# Plan

1. Render exact private scope-review requests from verified current intent and
   whole-target evidence; pin instructions, model settings and response-model policy.
2. Add a distinct semantic role and provider-free exchange verifier at the existing
   Mastra edge, reusing the recorded transport and preserving drafting compatibility.
3. Verify actual SDK serialization with synthetic transport: acknowledgements,
   source/citation binding, changed policy, invalid output, incomplete aggregation,
   cancellation, and no automatic provider retries.
4. Run focused/full regression, disposable PostgreSQL/Temporal compatibility,
   type/build and repository checks. Record observed outcomes; preserve signed
   hashes and user files; commit/push and verify the remote branch.
5. Continue the distinct durable batch/records/reservation/Temporal composition.
   Do not activate this adapter through fixture authority or drafting-role costs.
