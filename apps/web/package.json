{
  "name": "@steer/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-learn.ts && next dev -H 127.0.0.1",
    "build": "node scripts/generate-learn.ts && next build",
    "start": "next start -H 127.0.0.1",
    "typecheck": "next typegen && node scripts/generate-learn.ts && tsc --noEmit",
    "test": "node --test",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "@steer/tool-registry": "workspace:*",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-markdown": "10.1.0"
  },
  "devDependencies": {
    "@types/node": "25.0.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "typescript": "7.0.2"
  }
}
